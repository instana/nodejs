import { fromInstanaIds } from "./create";
import { createEmptyUnsampled } from "./create";
export declare const parse: (traceParentRaw: string, traceStateRaw: string) => import("./W3cTraceContext");
export { fromInstanaIds as create, createEmptyUnsampled };

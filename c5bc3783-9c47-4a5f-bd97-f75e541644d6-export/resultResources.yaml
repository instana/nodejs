---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: app-preview-pr-binding
spec:
  params:
    - name: pipeline-name
      value: app-preview-pr-pipeline
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: app-preview-pr-finish
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: evaluate,prepare
spec:
  params:
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the name of the PR's repo target
    - name: base-repo-name
      description: name of the base repo
    - name: base-repo-owner
      description: owner of the base repo
    - name: pr-url
      description: PR url
    - name: pr-html-url
      description: HTML url of the PR
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: REPOS
      description: all repos from the pr-start
      default: '[]'
    - name: ARTIFACTS
      type: array
      description: all artifacts from the code-build
      default: []
    - name: start-exitcode
      default: '0'
  volumes:
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: configmap
        mountPath: /config/$(params.configmap)
      - name: secret
        mountPath: /config/$(params.secret)
      - name: toolchain
        mountPath: /toolchain
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: evaluate
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      onError: continue
      env:
        - name: STEP_NAME
          value: evaluate
        - name: STAGE_WORKSPACE
          value: /stage-app-preview-finish
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: HEAD_REPO
          value: $(params.head-repo)
        - name: HEAD_BRANCH
          value: $(params.head-branch)
        - name: HEAD_SHA
          value: $(params.head-sha)
        - name: BASE_REPO
          value: $(params.base-repo)
        - name: BASE_BRANCH
          value: $(params.base-branch)
        - name: BASE_REPO_OWNER
          value: $(params.base-repo-owner)
        - name: BASE_REPO_NAME
          value: $(params.base-repo-name)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: PR_HTML_URL
          value: $(params.pr-html-url)
        - name: start_exit_code
          value: $(params.start-exitcode)
      args:
        - $(params.ARTIFACTS[*])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        set -e -o pipefail

        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          set_env fatal_error "true"
          exit ${start_exit_code}
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        echo -n "${HEAD_REPO}" >> /config/head-repo

        echo -n "${HEAD_BRANCH}" >> /config/head-branch

        echo -n "${HEAD_SHA}" >> /config/head-sha

        echo -n "${BASE_REPO}" >> /config/base-repo

        echo -n "${BASE_BRANCH}" >> /config/base-branch

        echo -n "${BASE_REPO_OWNER}" >> /config/base-repo-owner

        echo -n "${BASE_REPO_NAME}" >> /config/base-repo-name


        echo -n "${HEAD_BRANCH}" >> /config/branch

        echo -n "${BASE_BRANCH}" >> /config/target-branch

        echo -n "${HEAD_SHA}" >> /config/commit-sha

        echo -n "${BASE_REPO}" >> /config/repository

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${PR_HTML_URL}" >> /config/pr-html-url

        set_env head-repo "${HEAD_REPO}"

        set_env head-branch "${HEAD_BRANCH}"

        set_env head-sha "${HEAD_SHA}"

        set_env base-repo "${BASE_REPO}"

        set_env base-branch "${BASE_BRANCH}"

        set_env base-repo-owner "${BASE_REPO_OWNER}"

        set_env base-repo-name "${BASE_REPO_NAME}"

        set_env branch "${HEAD_BRANCH}"

        set_env target-branch "${BASE_BRANCH}"

        set_env commit-sha "${HEAD_SHA}"

        set_env repository "${BASE_REPO}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env pr-html-url "${PR_HTML_URL}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          # loop through all the arguments - PRCODECHECKSTATUS, ARTIFACTS
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done

          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        export PATH="$PATH:/opt/cocoa/bin"

        set_env STAGE_TEST_STATUS "$(get_env STAGE_UNIT_TEST_STATUS "skipped")"


        . "${ONE_PIPELINE_PATH}/internal/task_start"

        . "${ONE_PIPELINE_PATH}"/internal/pr_finish


        # Send slack message using compliance-base-image script

        "${COMMONS_PATH}/communications/post_message"
        "one_pipeline_app_preview_$(get_env "pipeline_namespace" "")"
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: tmp
          mountPath: /tmp
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
      script: >
        #!/bin/bash


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi

        echo "Preparing custom finish step"

        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "app-preview-pr-finish steps: ${stages[*]}"

        echo "Number of steps in app-preview-pr-finish: ${#stages[@]}"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: run-stage
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: tmp
          mountPath: /tmp
      env:
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        finish_evaluate_exit_code=$(cat $(steps.step-evaluate.exitCode.path))

        export STAGE_SCRIPT_PATH="stage_script_runner_run_stage"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?

        if [ $finish_evaluate_exit_code -ne 0 ]; then
          echo "Failing the finish stage as \"evaluate\" step failed with error code $finish_evaluate_exit_code"
        fi

        if [ "$PIPELINE_STATUS" != "Succeeded" -a "$PIPELINE_STATUS" !=
        "Completed" ]; then
          echo "Pipeline run failed. Please verify the status of previous stages"
          exit 1
        fi

        exit $(($exit_code + $finish_evaluate_exit_code))
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: app-preview-pr-listener
spec:
  triggers:
    - bindings:
        - name: pr-binding
        - name: app-preview-pr-binding
      template:
        name: app-preview-pr-template
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: app-preview-pr-listener-gitlab
spec:
  triggers:
    - bindings:
        - name: pr-binding-gitlab
        - name: app-preview-pr-binding
      template:
        name: app-preview-pr-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: app-preview-pr-pipeline
spec:
  params:
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the name of the PR's repo target
    - name: base-repo-name
      description: name of the base repo
    - name: base-repo-owner
      description: owner of the base repo
    - name: pr-url
      description: PR url
    - name: pr-html-url
      description: PR HTML url
    - name: pr-title
      description: title of PR
    - name: default-branch
      description: default target branch
      default: ''
    - name: action
      description: status of PR
    - name: base_ref
      description: target branch of PR
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: repository-integration
      description: the repo integration name
    - name: compliance-baseimage
      description: pipeline baseimage to run most of the built-in pipeline code
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: dind-image
      description: Base image to run sidecars
    - name: detect-secrets-image
      description: Base image to detect Secrets
    - name: enable-image-signing
      description: Enable image signing for app preview PR pipeline
      default: 'false'
  tasks:
    - name: pr-start
      taskRef:
        name: pr-start
      params:
        - name: head-branch
          value: $(params.head-branch)
        - name: head-sha
          value: $(params.head-sha)
        - name: head-repo
          value: $(params.head-repo)
        - name: base-branch
          value: $(params.base-branch)
        - name: base-repo
          value: $(params.base-repo)
        - name: base-repo-name
          value: $(params.base-repo-name)
        - name: base-repo-owner
          value: $(params.base-repo-owner)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: pr-html-url
          value: $(params.pr-html-url)
        - name: pr-title
          value: $(params.pr-title)
        - name: default-branch
          value: $(params.default-branch)
        - name: action
          value: $(params.action)
        - name: base_ref
          value: $(params.base_ref)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: final-stage
          value: finish
    - name: pr-code-checks
      taskRef:
        name: app-preview-pr-code-checks
      runAfter:
        - pr-start
      params:
        - name: stage
          value:
            - checks-setup
            - detect-secrets
            - compliance-checks
            - static-scan
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: head-branch
          value: $(params.head-branch)
        - name: head-sha
          value: $(params.head-sha)
        - name: head-repo
          value: $(params.head-repo)
        - name: base-branch
          value: $(params.base-branch)
        - name: base-repo
          value: $(params.base-repo)
        - name: base-repo-name
          value: $(params.base-repo-name)
        - name: base-repo-owner
          value: $(params.base-repo-owner)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(tasks.pr-start.results.pr-url)
        - name: pr-html-url
          value: $(tasks.pr-start.results.pr-html-url)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: REPOS
          value: $(tasks.pr-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.pr-start.results.task-exit-code)
    - name: pr-code-build
      taskRef:
        name: app-preview-pr-code-build
      runAfter:
        - pr-start
      params:
        - name: stage
          value:
            - setup
            - unit-test
            - build-artifact
            - sign-artifact
            - scan-artifact
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: head-branch
          value: $(params.head-branch)
        - name: head-sha
          value: $(params.head-sha)
        - name: head-repo
          value: $(params.head-repo)
        - name: base-branch
          value: $(params.base-branch)
        - name: base-repo
          value: $(params.base-repo)
        - name: base-repo-name
          value: $(params.base-repo-name)
        - name: base-repo-owner
          value: $(params.base-repo-owner)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(tasks.pr-start.results.pr-url)
        - name: pr-html-url
          value: $(tasks.pr-start.results.pr-html-url)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: REPOS
          value: $(tasks.pr-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.pr-start.results.task-exit-code)
        - name: enable-image-signing
          value: $(params.enable-image-signing)
    - name: pr-deploy-checks
      taskRef:
        name: app-preview-pr-deploy-checks
      runAfter:
        - pr-code-build
      params:
        - name: stage
          value:
            - deploy
            - acceptance-test
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: head-branch
          value: $(params.head-branch)
        - name: head-sha
          value: $(params.head-sha)
        - name: head-repo
          value: $(params.head-repo)
        - name: base-branch
          value: $(params.base-branch)
        - name: base-repo
          value: $(params.base-repo)
        - name: base-repo-name
          value: $(params.base-repo-name)
        - name: base-repo-owner
          value: $(params.base-repo-owner)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(tasks.pr-start.results.pr-url)
        - name: pr-html-url
          value: $(tasks.pr-start.results.pr-html-url)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: ARTIFACTS
          value:
            - $(tasks.pr-code-build.results.ARTIFACTS)
        - name: start-exitcode
          value: $(tasks.pr-start.results.task-exit-code)
  finally:
    - name: app-preview-pr-finish
      taskRef:
        name: app-preview-pr-finish
      params:
        - name: stage
          value: finish
        - name: image
          value: ''
        - name: script
          value: ''
        - name: configmap
          value: ''
        - name: secret
          value: ''
        - name: abort-on-failure
          value: ''
        - name: dind
          value: ''
        - name: image-pull-policy
          value: ''
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: detect-secrets-image
          value: $(params.detect-secrets-image)
        - name: dind-image
          value: $(params.dind-image)
        - name: pipeline-status
          value: $(tasks.status)
        - name: head-sha
          value: $(params.head-sha)
        - name: head-branch
          value: $(params.head-branch)
        - name: head-repo
          value: $(params.head-repo)
        - name: base-branch
          value: $(params.base-branch)
        - name: base-repo
          value: $(params.base-repo)
        - name: base-repo-name
          value: $(params.base-repo-name)
        - name: base-repo-owner
          value: $(params.base-repo-owner)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(tasks.pr-start.results.pr-url)
        - name: pr-html-url
          value: $(tasks.pr-start.results.pr-html-url)
        - name: pr-title
          value: $(params.pr-title)
        - name: default-branch
          value: $(params.default-branch)
        - name: REPOS
          value: $(tasks.pr-start.results.REPOS)
        - name: ARTIFACTS
          value:
            - $(tasks.pr-code-build.results.ARTIFACTS)
        - name: start-exitcode
          value: $(tasks.pr-start.results.task-exit-code)
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: app-preview-pr-template
spec:
  params:
    - name: app-name
      description: The name of your app
    - name: registry-region
      description: The IBM Cloud region for image registry
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the url of the PR's repo target
    - name: base-repo-name
      description: the name of the PR's repo target
    - name: base-repo-owner
      description: the owner of the PR's repo target
    - name: pr-url
      description: url of the PR
    - name: pr-html-url
      description: HTML url of the PR
    - name: pr-title
      description: title of the PR
    - name: default-branch
      description: default target branch
      default: ''
    - name: action
      description: status of PR
    - name: base_ref
      description: target branch of PR
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: repository-integration
      description: the repo integration name
    - name: icr-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: pipeline-name
      description: The name of the pipeline to run
      default: pr-pipeline
    - name: pipeline-display-name
      description: The display name of the pipeline
      default: app-preview-pr-pipelinerun
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: detect-secrets-image
      description: Base image to detect Secrets
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/detect-secrets:0.13.1.ibm.64.dss-redhat-ubi@sha256:e855dcf18944f1d92e567a8d6979caa089592915250c225c8642ff89a54a1c72
    - name: cos-bucket-name
      description: >-
        Bucket name in your Cloud Object Storage instance, used as an Evidence
        Locker
      default: ''
    - name: cos-endpoint
      description: >-
        Endpoint of your Cloud Object Storage instance, used as an Evidence
        Locker
      default: ''
    - name: doi-toolchain-id
      description: toolchain ID
    - name: doi-environment
      description: doi target environment, this can be overridden
      default: ''
    - name: enable-image-signing
      description: Enabling image signing for app preview pr pipeline
      default: 'false'
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.icr-dockerconfigjson)
      metadata:
        name: icr-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: docker-build-sa
      secrets:
        - name: artifactory-pull-secret
        - name: onepipeline-pull-secret
        - name: one-pipeline-pull-secret
        - name: pipeline-pull-secret
        - name: icr-pull-secret
    - apiVersion: v1
      kind: ConfigMap
      metadata:
        name: trigger
        labels:
          app.kubernetes.io/version: 1.0.0
      data: {}
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: $(tt.params.pipeline-name)
        serviceAccountName: docker-build-sa
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
            - name: icr-pull-secret
        params:
          - name: app-name
            value: $(tt.params.app-name)
          - name: registry-region
            value: $(tt.params.registry-region)
          - name: head-branch
            value: $(tt.params.head-branch)
          - name: head-sha
            value: $(tt.params.head-sha)
          - name: head-repo
            value: $(tt.params.head-repo)
          - name: base-branch
            value: $(tt.params.base-branch)
          - name: base-repo
            value: $(tt.params.base-repo)
          - name: base-repo-name
            value: $(tt.params.base-repo-name)
          - name: base-repo-owner
            value: $(tt.params.base-repo-owner)
          - name: commit-timestamp
            value: $(tt.params.commit-timestamp)
          - name: pr-url
            value: $(tt.params.pr-url)
          - name: pr-html-url
            value: $(tt.params.pr-html-url)
          - name: pr-title
            value: $(tt.params.pr-title)
          - name: default-branch
            value: $(tt.params.default-branch)
          - name: action
            value: $(tt.params.action)
          - name: base_ref
            value: $(tt.params.base_ref)
          - name: repository-integration
            value: $(tt.params.repository-integration)
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: cos-bucket-name
            value: $(tt.params.cos-bucket-name)
          - name: cos-endpoint
            value: $(tt.params.cos-endpoint)
          - name: doi-toolchain-id
            value: $(tt.params.doi-toolchain-id)
          - name: doi-environment
            value: $(tt.params.doi-environment)
          - name: enable-image-signing
            value: $(tt.params.enable-image-signing)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
          - name: detect-secrets-image
            value: $(tt.params.detect-secrets-image)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: async-stage-binding
spec:
  params:
    - name: stage
      value: $(body.stage)
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: async-stage-listener
spec:
  triggers:
    - binding:
        name: async-stage-binding
      template:
        name: async-stage-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: async-stage-pipeline
spec:
  params:
    - name: stage
      description: Stage in the config file
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: compliance-baseimage
      description: pipeline baseimage to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
  tasks:
    - name: async-stage
      taskRef:
        name: async-stage
      params:
        - name: stage
          value:
            - $(params.stage)
            - finish
        - name: async-stage
          value: $(params.stage)
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: async-stage-template
spec:
  params:
    - name: stage
      description: stage task that will run here
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .one-pipeline.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: stage-$(tt.params.stage)
      spec:
        pipelineRef:
          name: async-stage-pipeline
        params:
          - name: stage
            value: $(tt.params.stage)
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: cc-binding
spec:
  params: null
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: cc-listener
spec:
  triggers:
    - binding:
        name: cc-binding
      template:
        name: cc-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: cc-pipeline
spec:
  params:
    - name: branch
      description: the branch for the git repo
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: pr url
      default: ''
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
    - name: detect-secrets-image
      description: Base image to detect Secrets
    - name: pipeline-name
      description: The name of the pipeline to run
      default: cc-pipeline
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
  tasks:
    - name: cc-start
      taskRef:
        name: cc-start
      params:
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
    - name: cc-repo-scan
      taskRef:
        name: cc-repo-scan
      runAfter:
        - cc-start
      params:
        - name: stage
          value:
            - repo-scan-setup
            - detect-secrets
            - static-scan
            - compliance-checks
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: pipeline-name
          value: $(params.pipeline-name)
        - name: dind-image
          value: $(params.dind-image)
    - name: cc-artifact-scan
      taskRef:
        name: cc-artifact-scan
      runAfter:
        - cc-start
      params:
        - name: stage
          value:
            - artifact-scan-setup
            - scan-artifact
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: pipeline-name
          value: $(params.pipeline-name)
        - name: dind-image
          value: $(params.dind-image)
    - name: cc-deployment-scan
      taskRef:
        name: cc-deployment-scan
      runAfter:
        - cc-start
      params:
        - name: stage
          value:
            - deployment-scan-setup
            - dynamic-scan
            - acceptance-test
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: pipeline-name
          value: $(params.pipeline-name)
        - name: dind-image
          value: $(params.dind-image)
  finally:
    - name: cc-finish
      taskRef:
        name: cc-finish
      params:
        - name: stage
          value: finish
        - name: image
          value: ''
        - name: script
          value: ''
        - name: configmap
          value: ''
        - name: secret
          value: ''
        - name: abort-on-failure
          value: ''
        - name: dind
          value: ''
        - name: image-pull-policy
          value: ''
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: detect-secrets-image
          value: $(params.detect-secrets-image)
        - name: pipeline-name
          value: $(params.pipeline-name)
        - name: pipeline-status
          value: $(tasks.status)
        - name: static-scan-state
          value: $(tasks.cc-repo-scan.results.static-scan-state)
        - name: CCREPOSCANSSTATUS
          value:
            - $(tasks.cc-repo-scan.results.CCREPOSCANSSTATUS)
        - name: dynamic-scan-state
          value: $(tasks.cc-deployment-scan.results.dynamic-scan-state)
        - name: compliance-checks-state
          value: $(tasks.cc-repo-scan.results.compliance-checks-state)
        - name: CCARTIFACTSCANSTATUS
          value:
            - $(tasks.cc-artifact-scan.results.CCARTIFACTSCANSTATUS)
        - name: scan-artifact-state
          value: $(tasks.cc-artifact-scan.results.scan-artifact-state)
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: cc-template
spec:
  params:
    - name: app-name
      description: The name of your app
    - name: registry-region
      description: The IBM Cloud region for image registry
    - name: branch
      description: the branch for the git repo
      default: master
    - name: repository-integration
      description: the repo integration name
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: slack-notifications
      description: toggles slack notifications
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .one-pipeline.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .one-pipeline.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: icr-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: cos-bucket-name
      description: >-
        Bucket name in your Cloud Object Storage instance, used as an Evidence
        Locker
      default: ''
    - name: cos-endpoint
      description: >-
        Endpoint of your Cloud Object Storage instance, used as an Evidence
        Locker
      default: ''
    - name: doi-toolchain-id
      description: toolchain ID
    - name: doi-environment
      description: doi target environment, this can be overridden
      default: ''
    - name: one-pipeline-config
      description: Configuration file to be used to customize pipeline behavior
      default: .one-pipeline.yaml
    - name: one-pipeline-config-repo
      description: repository url of the one-pipeline config
    - name: one-pipeline-config-branch
      description: the branch of the one-pipeline config
    - name: pipeline-name
      description: The name of the pipeline to run
      default: cc-pipeline
    - name: pipeline-display-name
      description: The display name of the pipeline
      default: cc-pipelinerun
    - name: repository
      description: app repo url
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: detect-secrets-image
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/detect-secrets:0.13.1.ibm.64.dss-redhat-ubi@sha256:e855dcf18944f1d92e567a8d6979caa089592915250c225c8642ff89a54a1c72
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.icr-dockerconfigjson)
      metadata:
        name: icr-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: docker-build-sa
      secrets:
        - name: artifactory-pull-secret
        - name: onepipeline-pull-secret
        - name: one-pipeline-pull-secret
        - name: pipeline-pull-secret
        - name: icr-pull-secret
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: $(tt.params.pipeline-name)
        serviceAccountName: docker-build-sa
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
            - name: icr-pull-secret
        params:
          - name: app-name
            value: $(tt.params.app-name)
          - name: registry-region
            value: $(tt.params.registry-region)
          - name: branch
            value: $(tt.params.branch)
          - name: repository-integration
            value: $(tt.params.repository-integration)
          - name: slack-notifications
            value: $(tt.params.slack-notifications)
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: pipeline-name
            value: $(tt.params.pipeline-name)
          - name: commit-id
            value: $(tt.params.commit-id)
          - name: commit-timestamp
            value: $(tt.params.commit-timestamp)
          - name: cos-bucket-name
            value: $(tt.params.cos-bucket-name)
          - name: cos-endpoint
            value: $(tt.params.cos-endpoint)
          - name: doi-toolchain-id
            value: $(tt.params.doi-toolchain-id)
          - name: doi-environment
            value: $(tt.params.doi-environment)
          - name: one-pipeline-config
            value: $(tt.params.one-pipeline-config)
          - name: one-pipeline-config-repo
            value: $(tt.params.one-pipeline-config-repo)
          - name: one-pipeline-config-branch
            value: $(tt.params.one-pipeline-config-branch)
          - name: repository
            value: $(tt.params.repository)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
          - name: detect-secrets-image
            value: $(tt.params.detect-secrets-image)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: cd-binding
spec:
  params:
    - name: target-environment-ref
      value: $(body.ref)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: cd-change-request-binding
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: cd-change-request-listener
spec:
  triggers:
    - binding:
        name: cd-change-request-binding
      template:
        name: cd-change-request-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: cd-change-request-pipeline
spec:
  params:
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
  tasks:
    - name: cd-start
      taskRef:
        name: cd-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
    - name: cd-deploy
      taskRef:
        name: cd-change-request-deploy
      runAfter:
        - cd-start
      params:
        - name: stage
          value:
            - setup
            - verify-peer-review
            - verify-artifact
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
  finally:
    - name: cd-finish
      taskRef:
        name: cd-change-request-finish
      params:
        - name: stage
          value:
            - finish
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: pipeline-status
          value: $(tasks.status)
        - name: change-request-state
          value: $(tasks.cd-deploy.results.change-request-state)
        - name: CDDEPLOYPROPERTIES
          value:
            - $(tasks.cd-deploy.results.CDDEPLOYPROPERTIES)
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: cd-change-request-template
spec:
  params:
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: icr-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-name
      description: The name of the pipeline to run
      default: cd-change-request-pipeline
    - name: pipeline-display-name
      description: The display name of the pipeline
      default: cd-change-request-pipelinerun
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.icr-dockerconfigjson)
      metadata:
        name: icr-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: docker-build-sa
      secrets:
        - name: artifactory-pull-secret
        - name: onepipeline-pull-secret
        - name: one-pipeline-pull-secret
        - name: pipeline-pull-secret
        - name: icr-pull-secret
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: cd-change-request-pipeline
        serviceAccountName: docker-build-sa
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
            - name: icr-pull-secret
        params:
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: cd-listener
spec:
  triggers:
    - binding:
        name: cd-binding
      template:
        name: cd-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: cd-pipeline
spec:
  params:
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
    - name: rollback-enabled
      description: Trigger a rollback if deployment fails
      default: '0'
  tasks:
    - name: cd-start
      taskRef:
        name: cd-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
    - name: cd-deploy
      taskRef:
        name: cd-deploy
      runAfter:
        - cd-start
      params:
        - name: stage
          value:
            - setup
            - verify-peer-review
            - verify-artifact
            - deploy
            - acceptance-test
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
  finally:
    - name: cd-finish
      taskRef:
        name: cd-finish
      params:
        - name: stage
          value:
            - rollback-if-needed
            - post-deploy
            - finish
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: pipeline-status
          value: $(tasks.status)
        - name: change-request-state
          value: $(tasks.cd-deploy.results.change-request-state)
        - name: deployment-status
          value: $(tasks.cd-deploy.results.deployment-status)
        - name: acceptance-tests-state
          value: $(tasks.cd-deploy.results.acceptance-test-state)
        - name: rollback-enabled
          value: $(params.rollback-enabled)
        - name: CDDEPLOYPROPERTIES
          value:
            - $(tasks.cd-deploy.results.CDDEPLOYPROPERTIES)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: cd-rollback-binding
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: cd-rollback-listener
spec:
  triggers:
    - binding:
        name: cd-rollback-binding
      template:
        name: cd-rollback-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: cd-rollback-pipeline
spec:
  params:
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
    - name: rollback-enabled
      description: Trigger a rollback if deployment fails
      default: '0'
  tasks:
    - name: cd-start
      taskRef:
        name: cd-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
    - name: cd-rollback-deploy
      taskRef:
        name: cd-rollback-deploy
      runAfter:
        - cd-start
      params:
        - name: stage
          value:
            - setup
            - deploy
            - acceptance-test
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
  finally:
    - name: cd-finish
      taskRef:
        name: cd-finish
      params:
        - name: stage
          value:
            - finish
            - rollback-if-needed
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: pipeline-status
          value: $(tasks.status)
        - name: change-request-state
          value: $(tasks.cd-rollback-deploy.results.change-request-state)
        - name: deployment-status
          value: $(tasks.cd-rollback-deploy.results.deployment-status)
        - name: acceptance-tests-state
          value: $(tasks.cd-rollback-deploy.results.acceptance-test-state)
        - name: rollback-enabled
          value: $(params.rollback-enabled)
        - name: CDDEPLOYPROPERTIES
          value:
            - $(tasks.cd-rollback-deploy.results.CDDEPLOYPROPERTIES)
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: cd-rollback-template
spec:
  params:
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-name
      description: The name of the pipeline to run
      default: cd-rollback-pipeline
    - name: pipeline-display-name
      description: The display name of the pipeline
      default: cd-rollback-pipelinerun
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: rollback-enabled
      description: Trigger a rollback if deployment fails
      default: '0'
    - name: volume-storage
      description: Define the desired storage capacity for a PersistentVolumeClaim (PVC)
      default: 25Gi
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: cd-rollback-pipeline
        workspaces:
          - name: artifacts
            volumeClaimTemplate:
              spec:
                resources:
                  requests:
                    storage: $(tt.params.volume-storage)
                volumeMode: Filesystem
                accessModes:
                  - ReadWriteOnce
        params:
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
          - name: rollback-enabled
            value: $(tt.params.rollback-enabled)
          - name: volume-storage
            value: $(tt.params.volume-storage)
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: cd-template
spec:
  params:
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: icr-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-name
      description: The name of the pipeline to run
      default: cd-pipeline
    - name: pipeline-display-name
      description: The display name of the pipeline
      default: cd-pipelinerun
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: rollback-enabled
      description: Trigger a rollback if deployment fails
      default: '0'
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.icr-dockerconfigjson)
      metadata:
        name: icr-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: docker-build-sa
      secrets:
        - name: artifactory-pull-secret
        - name: onepipeline-pull-secret
        - name: one-pipeline-pull-secret
        - name: pipeline-pull-secret
        - name: icr-pull-secret
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: $(tt.params.pipeline-name)
        serviceAccountName: docker-build-sa
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
            - name: icr-pull-secret
        params:
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
          - name: rollback-enabled
            value: $(tt.params.rollback-enabled)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: ci-binding-gitlab
spec:
  params:
    - name: branch
      value: $(body.ref)
    - name: commit-id
      value: $(body.checkout_sha)
    - name: repository
      value: $(body.repository.git_http_url)
    - name: commit-timestamp
      value: $(body.commits[0].timestamp)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: ci-binding
spec:
  params:
    - name: branch
      value: $(body.ref)
    - name: commit-id
      value: $(body.head_commit.id)
    - name: commit-timestamp
      value: $(body.head_commit.timestamp)
    - name: repository
      value: $(body.repository.html_url)
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: ci-listener-gitlab
spec:
  triggers:
    - binding:
        name: ci-binding-gitlab
      template:
        name: ci-template
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: ci-listener
spec:
  triggers:
    - binding:
        name: ci-binding
      template:
        name: ci-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: ci-pipeline
spec:
  params:
    - name: branch
      description: the branch for the git repo
      default: ''
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: pr url
      default: ''
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: compliance-baseimage
      description: Pipeline baseimage to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
    - name: detect-secrets-image
      description: Base image to detect Secrets
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
  tasks:
    - name: ci-start
      taskRef:
        name: ci-start
      params:
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: final-stage
          value: finish
    - name: code-checks
      taskRef:
        name: code-checks
      runAfter:
        - ci-start
      params:
        - name: stage
          value:
            - checks-setup
            - detect-secrets
            - compliance-checks
            - peer-review
            - static-scan
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: code-build
      taskRef:
        name: code-build
      runAfter:
        - ci-start
      params:
        - name: stage
          value:
            - setup
            - unit-test
            - build-artifact
            - sign-artifact
            - scan-artifact
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: deploy-checks
      taskRef:
        name: deploy-checks
      runAfter:
        - code-build
      params:
        - name: stage
          value:
            - deploy
            - dynamic-scan
            - acceptance-test
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: ARTIFACTS
          value:
            - $(tasks.code-build.results.ARTIFACTS)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: deploy-release
      runAfter:
        - code-checks
        - deploy-checks
      taskRef:
        name: deploy-release
      params:
        - name: stage
          value: release
        - name: image
          value: ''
        - name: script
          value: ''
        - name: configmap
          value: ''
        - name: secret
          value: ''
        - name: abort-on-failure
          value: ''
        - name: dind
          value: ''
        - name: image-pull-policy
          value: ''
        - name: run-after
          value: ''
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: ARTIFACTS
          value:
            - $(tasks.code-build.results.ARTIFACTS)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: DEPLOY_ARTIFACTS
          value: $(tasks.deploy-checks.results.ARTIFACTS)
        - name: CODECHECKSTATUS
          value: $(tasks.code-checks.results.CODECHECKSTATUS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
  finally:
    - name: code-ci-finish
      taskRef:
        name: ci-finish
      params:
        - name: stage
          value: finish
        - name: image
          value: ''
        - name: script
          value: ''
        - name: configmap
          value: ''
        - name: secret
          value: ''
        - name: abort-on-failure
          value: ''
        - name: dind
          value: ''
        - name: image-pull-policy
          value: ''
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: detect-secrets-image
          value: $(params.detect-secrets-image)
        - name: pipeline-status
          value: $(tasks.status)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: CODECHECKSTATUS
          value:
            - $(tasks.code-checks.results.CODECHECKSTATUS)
        - name: BUILDARTIFACTS
          value:
            - $(tasks.code-build.results.ARTIFACTS)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: ci-template
spec:
  params:
    - name: app-name
      description: The name of your app
    - name: registry-region
      description: The IBM Cloud region for image registry
    - name: branch
      description: the branch for the git repo
      default: master
    - name: repository-integration
      description: the repo integration name
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: slack-notifications
      description: toggles slack notifications
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: icr-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: cos-bucket-name
      description: >-
        Bucket name in your Cloud Object Storage instance, used as an Evidence
        Locker
      default: ''
    - name: cos-endpoint
      description: >-
        Endpoint of your Cloud Object Storage instance, used as an Evidence
        Locker
      default: ''
    - name: doi-toolchain-id
      description: toolchain ID
    - name: doi-environment
      description: doi target environment, this can be overridden
      default: ''
    - name: pipeline-config
      description: Configuration file to be used to customize pipeline behavior
      default: .pipeline-config.yaml
    - name: pipeline-config-repo
      description: repository url of the pipeline config
    - name: pipeline-config-branch
      description: the branch of the pipeline config
    - name: pipeline-name
      description: The name of the pipeline to run
      default: ci-pipeline
    - name: pipeline-display-name
      description: The name of the pipeline to display in the UI
      default: ci-pipelinerun
    - name: repository
      description: app repo url
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: detect-secrets-image
      description: Base image to detect Secrets
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/detect-secrets:0.13.1.ibm.64.dss-redhat-ubi@sha256:e855dcf18944f1d92e567a8d6979caa089592915250c225c8642ff89a54a1c72
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.icr-dockerconfigjson)
      metadata:
        name: icr-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: docker-build-sa
      secrets:
        - name: artifactory-pull-secret
        - name: onepipeline-pull-secret
        - name: one-pipeline-pull-secret
        - name: pipeline-pull-secret
        - name: icr-pull-secret
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: $(tt.params.pipeline-name)
        serviceAccountName: docker-build-sa
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
            - name: icr-pull-secret
        params:
          - name: app-name
            value: $(tt.params.app-name)
          - name: registry-region
            value: $(tt.params.registry-region)
          - name: branch
            value: $(tt.params.branch)
          - name: repository-integration
            value: $(tt.params.repository-integration)
          - name: slack-notifications
            value: $(tt.params.slack-notifications)
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: commit-id
            value: $(tt.params.commit-id)
          - name: commit-timestamp
            value: $(tt.params.commit-timestamp)
          - name: cos-bucket-name
            value: $(tt.params.cos-bucket-name)
          - name: cos-endpoint
            value: $(tt.params.cos-endpoint)
          - name: doi-toolchain-id
            value: $(tt.params.doi-toolchain-id)
          - name: doi-environment
            value: $(tt.params.doi-environment)
          - name: pipeline-config
            value: $(tt.params.pipeline-config)
          - name: pipeline-config-repo
            value: $(tt.params.pipeline-config-repo)
          - name: pipeline-config-branch
            value: $(tt.params.pipeline-config-branch)
          - name: repository
            value: $(tt.params.repository)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
          - name: detect-secrets-image
            value: $(tt.params.detect-secrets-image)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: concert-aggregator-binding
spec:
  params:
    - name: pipeline-name
      value: concert-aggregator-pipeline
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: concert-aggregator-listener
spec:
  triggers:
    - binding:
        name: concert-aggregator-binding
      template:
        name: concert-aggregator-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: concert-aggregator-pipeline
spec:
  params:
    - name: compliance-baseimage
      description: pipeline baseimage to run most of the built-in pipeline code
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: dind-image
      description: Base image to run sidecars
  tasks:
    - name: concert-aggregator
      taskRef:
        name: concert-aggregator
      params:
        - name: stage
          value: concert-aggregator
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: concert-aggregator-template
spec:
  params:
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-config
      description: Configuration file to be used to customize pipeline behavior
      default: .pipeline-config.yaml
    - name: pipeline-config-repo
      description: Repository url of the pipeline config
    - name: pipeline-config-branch
      description: Branch of the pipeline config
    - name: pipeline-name
      description: The name of the pipeline to run
      default: concert-aggregator-pipeline
    - name: pipeline-display-name
      description: The name of the pipeline to display in the UI
      default: concert-aggregator-pipelinerun
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: inventory-repo
      description: Repository url of the pipeline inventory
    - name: inventory-branch
      description: Branch of the pipeline inventory
    - name: concert-endpoint-url
      description: Concert Endpoint Url
    - name: concert-instance-id
      description: Concert Instance Id
    - name: concert-api-key
      description: Concert API Key
    - name: concert-application-name
      description: Concert Application Name
    - name: concert-application-version
      description: Concert Application Version
    - name: application-metadata-file-path
      description: >-
        Path of the file storing application metadata relative to pipeline
        config root directory
    - name: icr-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.icr-dockerconfigjson)
      metadata:
        name: icr-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: docker-build-sa
      secrets:
        - name: artifactory-pull-secret
        - name: onepipeline-pull-secret
        - name: one-pipeline-pull-secret
        - name: pipeline-pull-secret
        - name: icr-pull-secret
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: $(tt.params.pipeline-name)
        serviceAccountName: docker-build-sa
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
            - name: icr-pull-secret
        params:
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: pipeline-name
            value: $(tt.params.pipeline-name)
          - name: pipeline-config
            value: $(tt.params.pipeline-config)
          - name: pipeline-config-repo
            value: $(tt.params.pipeline-config-repo)
          - name: pipeline-config-branch
            value: $(tt.params.pipeline-config-branch)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: inventory-repo
            value: $(tt.params.inventory-repo)
          - name: inventory-branch
            value: $(tt.params.inventory-branch)
          - name: concert-endpoint-url
            value: $(tt.params.concert-endpoint-url)
          - name: concert-instance-id
            value: $(tt.params.concert-instance-id)
          - name: concert-api-key
            value: $(tt.params.concert-api-key)
          - name: concert-application-name
            value: $(tt.params.concert-application-name)
          - name: concert-application-version
            value: $(tt.params.concert-application-version)
          - name: application-metadata-file-path
            value: $(tt.params.application-metadata-file-path)
          - name: dind-image
            value: $(tt.params.dind-image)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: dev-mode-cd-binding
spec:
  params:
    - name: pipeline-name
      value: dev-mode-cd-pipeline
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: dev-mode-cd-listener
spec:
  triggers:
    - bindings:
        - name: cd-binding
        - name: dev-mode-cd-binding
      template:
        name: cd-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: dev-mode-cd-pipeline
spec:
  params:
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
  tasks:
    - name: cd-start
      taskRef:
        name: cd-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
    - name: dev-mode-cd-deploy
      taskRef:
        name: dev-mode-cd-deploy
      runAfter:
        - cd-start
      params:
        - name: stage
          value:
            - setup
            - deploy
            - acceptance-test
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
  finally:
    - name: prod-finish
      taskRef:
        name: dev-mode-cd-finish
      params:
        - name: stage
          value:
            - post-deploy
            - finish
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: pipeline-status
          value: $(tasks.status)
        - name: deployment-status
          value: $(tasks.dev-mode-cd-deploy.results.deployment-status)
        - name: acceptance-tests-state
          value: $(tasks.dev-mode-cd-deploy.results.acceptance-test-state)
        - name: CDDEPLOYPROPERTIES
          value:
            - $(tasks.dev-mode-cd-deploy.results.CDDEPLOYPROPERTIES)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: dev-mode-binding
spec:
  params:
    - name: pipeline-name
      value: dev-mode-pipeline
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: dev-mode-listener
spec:
  triggers:
    - bindings:
        - name: ci-binding
        - name: dev-mode-binding
      template:
        name: ci-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: dev-mode-pipeline
spec:
  params:
    - name: branch
      description: the branch for the git repo
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: pr url
      default: ''
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: compliance-baseimage
      description: Pipeline baseimage to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
  tasks:
    - name: ci-start
      taskRef:
        name: ci-start
      params:
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: final-stage
          value: finish
    - name: devmode-code-build
      taskRef:
        name: devmode-code-build
      params:
        - name: stage
          value:
            - setup
            - unit-test
            - build-artifact
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: devmode-deploy-checks
      taskRef:
        name: devmode-deploy-checks
      runAfter:
        - devmode-code-build
      params:
        - name: stage
          value:
            - deploy
            - acceptance-test
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: ARTIFACTS
          value:
            - $(tasks.devmode-code-build.results.ARTIFACTS)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: devmode-deploy-release
      taskRef:
        name: devmode-deploy-release
      runAfter:
        - devmode-deploy-checks
      params:
        - name: stage
          value: release
        - name: image
          value: ''
        - name: script
          value: ''
        - name: configmap
          value: ''
        - name: secret
          value: ''
        - name: abort-on-failure
          value: ''
        - name: dind
          value: ''
        - name: image-pull-policy
          value: ''
        - name: run-after
          value: ''
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: ARTIFACTS
          value:
            - $(tasks.devmode-code-build.results.ARTIFACTS)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
  finally:
    - name: code-ci-finish
      taskRef:
        name: dev-mode-ci-finish
      params:
        - name: stage
          value: finish
        - name: image
          value: ''
        - name: script
          value: ''
        - name: configmap
          value: ''
        - name: secret
          value: ''
        - name: abort-on-failure
          value: ''
        - name: dind
          value: ''
        - name: image-pull-policy
          value: ''
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: pipeline-status
          value: $(tasks.status)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: dind
spec:
  volumes:
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: memdisk-monitor
spec:
  params:
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
  sidecars:
    - name: memdisk-monitor
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - name: event-parameters
          mountPath: /trigger-payload
        - name: workspace
          mountPath: /workspace/app
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
      script: |
        #!/usr/bin/bash

        # Capture initial CPU stats
        prev=($(head -n1 /proc/stat))
        prev_idle=${prev[4]}
        prev_total=0
        for value in "${prev[@]:1}"; do
          prev_total=$((prev_total + value))
        done

        echo "Monitoring Memory, Disk Storage, and CPU"

        while true; do
          # Capture memory usage
          total_mem=$(awk '/MemTotal/ {print $2}' /proc/meminfo)
          avail_mem=$(awk '/MemAvailable/ {print $2}' /proc/meminfo)
          mem_usage=$(awk -v a=$avail_mem -v t=$total_mem 'BEGIN {printf "%.1f%%", 100*(1 - a/t)}')

          # Capture disk usage
          disk_usage=$(df -h / | awk 'NR==2 {print $5}')

          # Capture current CPU stats
          curr=($(head -n1 /proc/stat))
          curr_idle=${curr[4]}
          curr_total=0
          for value in "${curr[@]:1}"; do
            curr_total=$((curr_total + value))
          done

          # Calculate deltas
          idle_diff=$((curr_idle - prev_idle))
          total_diff=$((curr_total - prev_total))

          # Calculate CPU usage
          cpu_usage=$(awk -v i=$idle_diff -v t=$total_diff 'BEGIN {printf "%.1f%%", 100*((t - i)/t)}')

          # Output Memory, Disk, and CPU usage
          echo "[Memory: $mem_usage, Disk: $disk_usage, CPU: $cpu_usage]"

          # Update previous values
          prev_idle=$curr_idle
          prev_total=$curr_total

          # Wait before next check
          sleep 30
        done
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: operational-binding
spec:
  params:
    - name: pipeline-name
      value: operational-pipeline
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: operational-listener
spec:
  triggers:
    - bindings:
        - name: operational-binding
      template:
        name: operational-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: operational-pipeline
spec:
  params:
    - name: compliance-baseimage
      description: pipeline baseimage to run most of the built-in pipeline code
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: dind-image
      description: Base image to run sidecars
    - name: pipeline-name
      description: Name of the pipeline
      default: operational-pipeline
  tasks:
    - name: change-request
      taskRef:
        name: core-change-request
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: pipeline-name
          value: $(params.pipeline-name)
        - name: stage
          value: change-request
    - name: operational-execute
      taskRef:
        name: operational-execute
      runAfter:
        - change-request
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: next-stage
          value: operational-finish
        - name: stage
          value: execute
        - name: image
          value: ''
        - name: script
          value: ''
        - name: configmap
          value: ''
        - name: secret
          value: ''
        - name: abort-on-failure
          value: ''
        - name: dind
          value: ''
        - name: image-pull-policy
          value: ''
        - name: dind-image
          value: $(params.dind-image)
        - name: change-request-id
          value: $(tasks.change-request.results.change-request-id)
  finally:
    - name: operational-finish
      taskRef:
        name: operational-finish
      params:
        - name: stage
          value: finish
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: pipeline-name
          value: $(params.pipeline-name)
        - name: dind-image
          value: $(params.dind-image)
        - name: pipeline-status
          value: $(tasks.status)
        - name: change-request-id
          value: $(tasks.change-request.results.change-request-id)
        - name: manual-change-request
          value: $(tasks.change-request.results.manual-change-request)
        - name: operational-execute-exit-code
          value:
            - $(tasks.operational-execute.results.operational-execute-exit-code)
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: operational-template
spec:
  params:
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-config
      description: Configuration file to be used to customize pipeline behavior
      default: .pipeline-config.yaml
    - name: pipeline-config-repo
      description: repository url of the pipeline config
    - name: pipeline-config-branch
      description: the branch of the pipeline config
    - name: pipeline-name
      description: The name of the pipeline to run
      default: operational-pipeline
    - name: pipeline-display-name
      description: The name of the pipeline to display in the UI
      default: operational-pipelinerun
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: docker-build-sa
      secrets:
        - name: pipeline-pull-secret
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: $(tt.params.pipeline-name)
        serviceAccountName: docker-build-sa
        podTemplate:
          imagePullSecrets:
            - name: pipeline-pull-secret
        params:
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: pipeline-name
            value: $(tt.params.pipeline-name)
          - name: pipeline-config
            value: $(tt.params.pipeline-config)
          - name: pipeline-config-repo
            value: $(tt.params.pipeline-config-repo)
          - name: pipeline-config-branch
            value: $(tt.params.pipeline-config-branch)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: podman
spec:
  volumes:
    - name: podman
      emptyDir: {}
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: pr-binding-gitlab
spec:
  params:
    - name: head-branch
      value: $(body.object_attributes.source_branch)
    - name: head-sha
      value: $(body.object_attributes.last_commit.id)
    - name: head-repo
      value: $(body.object_attributes.source.web_url)
    - name: base-branch
      value: $(body.object_attributes.target_branch)
    - name: base-repo
      value: $(body.object_attributes.target.web_url)
    - name: base-repo-name
      value: $(body.object_attributes.target.name)
    - name: base-repo-owner
      value: $(body.user.username)
    - name: commit-timestamp
      value: $(body.object_attributes.last_commit.timestamp)
    - name: pr-url
      value: $(body.object_attributes.url)
    - name: pr-html-url
      value: $(body.object_attributes.url)
    - name: pr-title
      value: $(body.object_attributes.title)
    - name: default-branch
      value: $(body.project.default_branch)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: pr-binding-merge-queue
spec:
  params:
    - name: head-branch
      value: $(body.merge_group.head_ref)
    - name: head-sha
      value: $(body.merge_group.head_sha)
    - name: head-repo
      value: $(body.repository.html_url)
    - name: pr-url
      value: ''
    - name: pr-html-url
      value: ''
    - name: base-branch
      value: $(body.merge_group.base_ref)
    - name: base-repo
      value: $(body.repository.html_url)
    - name: base-repo-name
      value: $(body.repository.name)
    - name: base-repo-owner
      value: $(body.repository.owner.login)
    - name: commit-timestamp
      value: $(body.merge_group.head_commit.timestamp)
    - name: base_ref
      value: $(body.merge_group.base_ref)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: pr-binding
spec:
  params:
    - name: head-branch
      value: $(body.pull_request.head.ref)
    - name: head-sha
      value: $(body.pull_request.head.sha)
    - name: head-repo
      value: $(body.pull_request.head.repo.html_url)
    - name: base-branch
      value: $(body.pull_request.base.ref)
    - name: base-repo
      value: $(body.pull_request.base.repo.html_url)
    - name: base-repo-name
      value: $(body.pull_request.base.repo.name)
    - name: base-repo-owner
      value: $(body.pull_request.base.repo.owner.login)
    - name: pr-title
      value: $(body.pull_request.title)
    - name: default-branch
      value: $(body.pull_request.head.repo.default_branch)
    - name: action
      value: $(body.action)
    - name: base_ref
      value: $(body.merge_group.base_ref)
    - name: commit-timestamp
      value: $(body.pull_request.head.repo.pushed_at)
    - name: pr-url
      value: $(body.pull_request.url)
    - name: pr-html-url
      value: $(body.pull_request.html_url)
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: pr-listener-gitlab
spec:
  triggers:
    - binding:
        name: pr-binding-gitlab
      template:
        name: pr-template
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: pr-listener-merge-queue
spec:
  triggers:
    - binding:
        name: pr-binding-merge-queue
      template:
        name: pr-template
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: pr-listener
spec:
  triggers:
    - binding:
        name: pr-binding
      template:
        name: pr-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: pr-pipeline
spec:
  params:
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the url of the PR's repo target
    - name: base-repo-name
      description: name of the base repo
    - name: base-repo-owner
      description: owner of the base repo
    - name: pr-url
      description: url of the PR
    - name: pr-html-url
      description: HTML url of the PR
    - name: pr-title
      description: title of the PR
    - name: default-branch
      description: default target branch
      default: ''
    - name: action
      description: status of PR
    - name: base_ref
      description: target branch of PR
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: repository-integration
      description: the repo integration name
    - name: compliance-baseimage
      description: pipeline baseimage to run most of the built-in pipeline code
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: dind-image
      description: Base image to run sidecars
    - name: detect-secrets-image
      description: Base image to detect Secrets
  tasks:
    - name: pr-start
      taskRef:
        name: pr-start
      params:
        - name: head-branch
          value: $(params.head-branch)
        - name: head-sha
          value: $(params.head-sha)
        - name: head-repo
          value: $(params.head-repo)
        - name: base-branch
          value: $(params.base-branch)
        - name: base-repo
          value: $(params.base-repo)
        - name: base-repo-name
          value: $(params.base-repo-name)
        - name: base-repo-owner
          value: $(params.base-repo-owner)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-title
          value: $(params.pr-title)
        - name: default-branch
          value: $(params.default-branch)
        - name: action
          value: $(params.action)
        - name: base_ref
          value: $(params.base_ref)
        - name: pr-url
          value: $(params.pr-url)
        - name: pr-html-url
          value: $(params.pr-html-url)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: final-stage
          value: finish
    - name: pr-code-checks
      taskRef:
        name: pr-code-checks
      runAfter:
        - pr-start
      params:
        - name: stage
          value:
            - checks-setup
            - detect-secrets
            - unit-test
            - compliance-checks
        - name: image
          value: []
        - name: script
          value: []
        - name: configmap
          value: []
        - name: secret
          value: []
        - name: abort-on-failure
          value: []
        - name: dind
          value: []
        - name: image-pull-policy
          value: []
        - name: head-branch
          value: $(params.head-branch)
        - name: head-sha
          value: $(params.head-sha)
        - name: head-repo
          value: $(params.head-repo)
        - name: base-branch
          value: $(params.base-branch)
        - name: base-repo
          value: $(params.base-repo)
        - name: base-repo-name
          value: $(params.base-repo-name)
        - name: base-repo-owner
          value: $(params.base-repo-owner)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(tasks.pr-start.results.pr-url)
        - name: pr-html-url
          value: $(tasks.pr-start.results.pr-html-url)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: dind-image
          value: $(params.dind-image)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: REPOS
          value: $(tasks.pr-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.pr-start.results.task-exit-code)
  finally:
    - name: code-pr-finish
      taskRef:
        name: pr-finish
      params:
        - name: stage
          value: finish
        - name: image
          value: ''
        - name: script
          value: ''
        - name: configmap
          value: ''
        - name: secret
          value: ''
        - name: abort-on-failure
          value: ''
        - name: dind
          value: ''
        - name: image-pull-policy
          value: ''
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: detect-secrets-image
          value: $(params.detect-secrets-image)
        - name: dind-image
          value: $(params.dind-image)
        - name: pipeline-status
          value: $(tasks.status)
        - name: head-branch
          value: $(params.head-branch)
        - name: head-sha
          value: $(params.head-sha)
        - name: head-repo
          value: $(params.head-repo)
        - name: base-branch
          value: $(params.base-branch)
        - name: base-repo
          value: $(params.base-repo)
        - name: base-repo-name
          value: $(params.base-repo-name)
        - name: base-repo-owner
          value: $(params.base-repo-owner)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-title
          value: $(params.pr-title)
        - name: default-branch
          value: $(params.default-branch)
        - name: action
          value: $(params.action)
        - name: base_ref
          value: $(params.base_ref)
        - name: pr-url
          value: $(tasks.pr-start.results.pr-url)
        - name: pr-html-url
          value: $(tasks.pr-start.results.pr-html-url)
        - name: PRCODECHECKSTATUS
          value:
            - $(tasks.pr-code-checks.results.PRCODECHECKSTATUS)
        - name: REPOS
          value: $(tasks.pr-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.pr-start.results.task-exit-code)
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: pr-template
spec:
  params:
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the url of the PR's repo target
    - name: base-repo-name
      description: the name of the PR's repo target
    - name: base-repo-owner
      description: the owner of the PR's repo target
    - name: pr-url
      description: url of the PR
    - name: pr-html-url
      description: HTML url of the PR
    - name: pr-title
      description: title of the PR
      default: <none>
    - name: default-branch
      description: default target branch
      default: ''
    - name: action
      description: status of PR
    - name: base_ref
      description: target branch of PR
      default: <none>
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: repository-integration
      description: the repo integration name
    - name: icr-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: pipeline-name
      description: The name of the pipeline to run
      default: pr-pipeline
    - name: pipeline-display-name
      description: The display name of the pipeline
      default: pr-pipelinerun
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: detect-secrets-image
      description: Base image to detect Secrets
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/detect-secrets:0.13.1.ibm.64.dss-redhat-ubi@sha256:e855dcf18944f1d92e567a8d6979caa089592915250c225c8642ff89a54a1c72
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.icr-dockerconfigjson)
      metadata:
        name: icr-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: docker-build-sa
      secrets:
        - name: artifactory-pull-secret
        - name: onepipeline-pull-secret
        - name: one-pipeline-pull-secret
        - name: pipeline-pull-secret
        - name: icr-pull-secret
    - apiVersion: v1
      kind: ConfigMap
      metadata:
        name: trigger
        labels:
          app.kubernetes.io/version: 1.0.0
      data: {}
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: $(tt.params.pipeline-name)
        serviceAccountName: docker-build-sa
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
            - name: icr-pull-secret
        params:
          - name: head-branch
            value: $(tt.params.head-branch)
          - name: head-sha
            value: $(tt.params.head-sha)
          - name: head-repo
            value: $(tt.params.head-repo)
          - name: base-branch
            value: $(tt.params.base-branch)
          - name: base-repo
            value: $(tt.params.base-repo)
          - name: base-repo-name
            value: $(tt.params.base-repo-name)
          - name: base-repo-owner
            value: $(tt.params.base-repo-owner)
          - name: commit-timestamp
            value: $(tt.params.commit-timestamp)
          - name: pr-url
            value: $(tt.params.pr-url)
          - name: pr-title
            value: $(tt.params.pr-title)
          - name: default-branch
            value: $(tt.params.default-branch)
          - name: action
            value: $(tt.params.action)
          - name: base_ref
            value: $(tt.params.base_ref)
          - name: pr-html-url
            value: $(tt.params.pr-html-url)
          - name: repository-integration
            value: $(tt.params.repository-integration)
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
          - name: detect-secrets-image
            value: $(tt.params.detect-secrets-image)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: promotion-binding
spec:
  params:
    - name: priority
      value: $(body.priority)
    - name: assignee
      value: $(body.assignee)
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: promotion-listener
spec:
  triggers:
    - binding:
        name: promotion-binding
      template:
        name: promotion-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: promotion-pipeline
spec:
  params:
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: priority
      description: priority of the change request
    - name: assignee
      description: >-
        functional id or an email who will be assigned to the change request in
        service now
    - name: dind-image
      description: Base image to run sidecars
  tasks:
    - name: inventory-promotion
      taskRef:
        name: inventory-promotion
      params:
        - name: stage
          value: finish
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: priority
          value: $(params.priority)
        - name: assignee
          value: $(params.assignee)
        - name: dind-image
          value: $(params.dind-image)
        - name: image
          value: ''
        - name: script
          value: ''
        - name: configmap
          value: ''
        - name: secret
          value: ''
        - name: abort-on-failure
          value: ''
        - name: dind
          value: ''
        - name: image-pull-policy
          value: ''
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: promotion-template
spec:
  params:
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .one-pipeline.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .one-pipeline.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-display-name
      description: The display name of the pipeline
      default: promotion-pipelinerun
    - name: priority
      description: priority of the change request
    - name: assignee
      description: >-
        functional id or an email who will be assigned to the change request in
        service now
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: promotion-pipeline
        params:
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: priority
            value: $(tt.params.priority)
          - name: assignee
            value: $(tt.params.assignee)
          - name: dind-image
            value: $(tt.params.dind-image)
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: promotion-validation-binding-gitlab
spec:
  params:
    - name: pr-url
      value: $(body.object_attributes.url)
    - name: head-sha
      value: $(body.object_attributes.last_commit.id)
    - name: target-environment
      value: $(body.object_attributes.target_branch)
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: promotion-validation-binding
spec:
  params:
    - name: pr-url
      value: $(body.pull_request.html_url)
    - name: head-sha
      value: $(body.pull_request.head.sha)
    - name: target-environment
      value: $(body.pull_request.base.ref)
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: promotion-validation-listener-gitlab
spec:
  triggers:
    - binding:
        name: promotion-validation-binding-gitlab
      template:
        name: promotion-validation-template
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: promotion-validation-listener
spec:
  triggers:
    - binding:
        name: promotion-validation-binding
      template:
        name: promotion-validation-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: promotion-validation-pipeline
spec:
  params:
    - name: pr-url
      description: url of the PR
    - name: head-sha
      description: head-sha PR to update statuses
    - name: target-environment
      description: target-environment of the promotion
    - name: compliance-baseimage
      description: pipeline baseimage to run most of the built-in pipeline code
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: dind-image
      description: Base image to run sidecars
  tasks:
    - name: inventory-validation
      taskRef:
        name: inventory-validation
      params:
        - name: pr-url
          value: $(params.pr-url)
        - name: head-sha
          value: $(params.head-sha)
        - name: target-environment
          value: $(params.target-environment)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: final-stage
          value: finish
        - name: dind-image
          value: $(params.dind-image)
        - name: abort-on-failure
          value: ''
        - name: stage
          default: ''
        - name: dind
          value: ''
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: promotion-validation-template
spec:
  params:
    - name: pr-url
      description: url of the PR
    - name: head-sha
      description: head-sha of the PR to update statuses
    - name: target-environment
      description: target-environment of the promotion
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: repository-integration
      description: the repo integration name
    - name: artifactory-dockerconfigjson
      description: dockerconfigjson for pullsecrets
      default: eyJhdXRocyI6e319
    - name: onepipeline-dockerconfigjson
      description: dockerconfigjson for images used in .one-pipeline.yaml
      default: eyJhdXRocyI6e319
    - name: one-pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .one-pipeline.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-display-name
      description: The display name of the pipeline
      default: promotion-validation-pipelinerun
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.artifactory-dockerconfigjson)
      metadata:
        name: artifactory-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.onepipeline-dockerconfigjson)
      metadata:
        name: onepipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.one-pipeline-dockerconfigjson)
      metadata:
        name: one-pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: promotion-validation-pipeline
        params:
          - name: pr-url
            value: $(tt.params.pr-url)
          - name: head-sha
            value: $(tt.params.head-sha)
          - name: target-environment
            value: $(tt.params.target-environment)
          - name: repository-integration
            value: $(tt.params.repository-integration)
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
        podTemplate:
          imagePullSecrets:
            - name: artifactory-pull-secret
            - name: onepipeline-pull-secret
            - name: one-pipeline-pull-secret
            - name: pipeline-pull-secret
---
apiVersion: tekton.dev/v1
kind: TriggerBinding
metadata:
  name: simple-binding
spec:
  params:
    - name: pipeline-name
      value: simple-pipeline
    - name: head-branch
      value: $(body.pull_request.head.ref)
    - name: head-sha
      value: $(body.pull_request.head.sha)
    - name: head-repo
      value: $(body.pull_request.head.repo.html_url)
    - name: base-branch
      value: $(body.pull_request.base.ref)
    - name: base-repo
      value: $(body.pull_request.base.repo.html_url)
    - name: base-sha
      value: $(body.pull_request.base.sha)
    - name: pr-url
      value: $(body.pull_request.url)
    - name: branch
      value: $(body.ref)
    - name: commit-sha
      value: $(body.head_commit.id)
---
apiVersion: tekton.dev/v1
kind: EventListener
metadata:
  name: simple-listener
spec:
  triggers:
    - bindings:
        - name: simple-binding
      template:
        name: simple-template
---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: simple-pipeline
spec:
  params:
    - name: compliance-baseimage
      description: pipeline baseimage to run most of the built-in pipeline code
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the url of the PR's repo target
    - name: base-sha
      description: base commit
    - name: pr-url
      description: url of the PR
    - name: branch
      description: the branch for the git repo
      default: ''
    - name: commit-sha
      description: commit id
    - name: dind-image
      description: Base image to run sidecars
  tasks:
    - name: simple-execute
      taskRef:
        name: simple-execute
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: branch
          value: $(params.branch)
        - name: commit-sha
          value: $(params.commit-sha)
        - name: head-branch
          value: $(params.head-branch)
        - name: head-sha
          value: $(params.head-sha)
        - name: head-repo
          value: $(params.head-repo)
        - name: base-branch
          value: $(params.base-branch)
        - name: base-repo
          value: $(params.base-repo)
        - name: base-sha
          value: $(params.base-sha)
        - name: pr-url
          value: $(params.pr-url)
        - name: next-stage
          value: execute
        - name: stage
          value: execute
        - name: image
          value: ''
        - name: script
          value: ''
        - name: configmap
          value: ''
        - name: secret
          value: ''
        - name: abort-on-failure
          value: ''
        - name: dind
          value: ''
        - name: image-pull-policy
          value: ''
        - name: dind-image
          value: $(params.dind-image)
---
apiVersion: tekton.dev/v1
kind: TriggerTemplate
metadata:
  name: simple-template
spec:
  params:
    - name: pipeline-debug
      description: toggles debug mode for the pipeline
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: head-branch
      description: source branch of the PR
      default: ''
    - name: head-sha
      description: commit id
      default: ''
    - name: head-repo
      description: the name of the PR's repo source
      default: ''
    - name: base-branch
      description: target branch of the PR
      default: ''
    - name: base-repo
      description: the url of the PR's repo target
      default: ''
    - name: base-sha
      description: base commit
      default: ''
    - name: pr-url
      description: url of the PR
      default: ''
    - name: branch
      description: the branch for the git repo
      default: master
    - name: commit-sha
      description: commit id
      default: ''
    - name: pipeline-dockerconfigjson
      description: dockerconfigjson for images used in .pipeline-config.yaml
      default: eyJhdXRocyI6e319
    - name: pipeline-config
      description: Configuration file to be used to customize pipeline behavior
      default: .pipeline-config.yaml
    - name: pipeline-config-repo
      description: repository url of the pipeline config
    - name: pipeline-config-branch
      description: the branch of the pipeline config
    - name: pipeline-name
      description: The name of the pipeline to run
      default: simple-pipeline
    - name: pipeline-display-name
      description: The name of the pipeline to display in the UI
      default: simple-pipelinerun
    - name: compliance-baseimage
      description: one-pipeline baseimage to run most of the built-in pipeline code
      default: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
  resourcetemplates:
    - apiVersion: v1
      kind: Secret
      data:
        .dockerconfigjson: $(tt.params.pipeline-dockerconfigjson)
      metadata:
        name: pipeline-pull-secret
      type: kubernetes.io/dockerconfigjson
    - apiVersion: v1
      kind: ServiceAccount
      metadata:
        name: docker-build-sa
      secrets:
        - name: pipeline-pull-secret
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        name: $(tt.params.pipeline-display-name)
      spec:
        pipelineRef:
          name: $(tt.params.pipeline-name)
        serviceAccountName: docker-build-sa
        podTemplate:
          imagePullSecrets:
            - name: pipeline-pull-secret
        params:
          - name: pipeline-debug
            value: $(tt.params.pipeline-debug)
          - name: collect-metrics
            value: $(tt.params.collect-metrics)
          - name: pipeline-name
            value: $(tt.params.pipeline-name)
          - name: head-branch
            value: $(tt.params.head-branch)
          - name: head-sha
            value: $(tt.params.head-sha)
          - name: head-repo
            value: $(tt.params.head-repo)
          - name: base-branch
            value: $(tt.params.base-branch)
          - name: base-repo
            value: $(tt.params.base-repo)
          - name: base-sha
            value: $(tt.params.base-sha)
          - name: pr-url
            value: $(tt.params.pr-url)
          - name: branch
            value: $(tt.params.branch)
          - name: commit-sha
            value: $(tt.params.commit-sha)
          - name: pipeline-config
            value: $(tt.params.pipeline-config)
          - name: pipeline-config-repo
            value: $(tt.params.pipeline-config-repo)
          - name: pipeline-config-branch
            value: $(tt.params.pipeline-config-branch)
          - name: compliance-baseimage
            value: $(tt.params.compliance-baseimage)
          - name: dind-image
            value: $(tt.params.dind-image)
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: step-extension
spec:
  steps:
    - name: step-extension
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-step-extension
      env:
        - name: STAGE
          value: step-extension
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_step_extension
        - name: STAGE_ABORT_ON_FAILURE
          value: 'false'
        - name: STEP_NAME
          value: step-extension
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-step-extension
      script: >
        #!/bin/sh

        export STAGE_SCRIPT_PATH="stage_script_runner_step_extension"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: app-preview-pr-code-build
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the name of the PR's repo target
    - name: base-repo-name
      description: name of the base repo
    - name: base-repo-owner
      description: owner of the base repo
    - name: pr-url
      description: PR url
    - name: pr-html-url
      description: HTML url of the PR
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'true'
        - 'false'
        - 'true'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      description: all repos from the pr-start
      default: '[]'
    - name: start-exitcode
      default: '0'
    - name: enable-image-signing
      description: Enabling image signing for app preview pr pipeline
      default: 'false'
  results:
    - name: IMAGES
      type: string
      default: ''
    - name: ARTIFACTS
      type: string
      default: '[]'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: docker-socket
        mountPath: /var/run/
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: toolchain
        mountPath: /toolchain
      - name: tmp
        mountPath: /tmp
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: ENABLE_IMAGE_SIGNING
        value: $(params.enable-image-signing)
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
      env:
        - name: STEP_NAME
          value: start
        - name: HEAD_REPO
          value: $(params.head-repo)
        - name: HEAD_BRANCH
          value: $(params.head-branch)
        - name: HEAD_SHA
          value: $(params.head-sha)
        - name: BASE_REPO
          value: $(params.base-repo)
        - name: BASE_BRANCH
          value: $(params.base-branch)
        - name: BASE_REPO_OWNER
          value: $(params.base-repo-owner)
        - name: BASE_REPO_NAME
          value: $(params.base-repo-name)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: PR_HTML_URL
          value: $(params.pr-html-url)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
        - name: start_exit_code
          value: $(params.start-exitcode)
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        # start

        echo "app-preview-pr-code-build steps: ${stages[*]}"

        echo "Number of steps in app-preview-pr-code-build: ${#stages[@]}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        echo -n "${HEAD_REPO}" >> /config/head-repo

        echo -n "${HEAD_BRANCH}" >> /config/head-branch

        echo -n "${HEAD_SHA}" >> /config/head-sha

        echo -n "${BASE_REPO}" >> /config/base-repo

        echo -n "${BASE_BRANCH}" >> /config/base-branch

        echo -n "${BASE_REPO_OWNER}" >> /config/base-repo-owner

        echo -n "${BASE_REPO_NAME}" >> /config/base-repo-name


        echo -n "${HEAD_BRANCH}" >> /config/branch

        echo -n "${BASE_BRANCH}" >> /config/target-branch

        echo -n "${HEAD_SHA}" >> /config/commit-sha

        echo -n "${BASE_REPO}" >> /config/repository

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${PR_HTML_URL}" >> /config/pr-html-url

        set_env head-repo "${HEAD_REPO}"

        set_env head-branch "${HEAD_BRANCH}"

        set_env head-sha "${HEAD_SHA}"

        set_env base-repo "${BASE_REPO}"

        set_env base-branch "${BASE_BRANCH}"

        set_env base-repo-owner "${BASE_REPO_OWNER}"

        set_env base-repo-name "${BASE_REPO_NAME}"

        set_env branch "${HEAD_BRANCH}"

        set_env target-branch "${BASE_BRANCH}"

        set_env commit-sha "${HEAD_SHA}"

        set_env repository "${BASE_REPO}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env pr-html-url "${PR_HTML_URL}"


        . "${ONE_PIPELINE_PATH}/internal/task_start"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        status_checks_prefix="$(get_env branch-protection-status-check-prefix
        "tekton")"

        if [ -e /tekton/steps/step-unit-test ]; then
          set-commit-status \
            --repository "$(load_repo app-repo url)" \
            --commit-sha "$(load_repo app-repo commit)" \
            --state "pending" \
            --description "Running unit tests..." \
            --context "${status_checks_prefix}/${TASK_NAME}/code-unit-tests" \
            --task-name "$(get_env UNIT_TESTS_TASK_NAME)" \
            --step-name "$(get_env UNIT_TESTS_STEP_NAME)"
        fi


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: docker-socket
          mountPath: /var/run/
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: unit-test
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_detect_secrets
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: unit-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_unit_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: build-artifact
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_compliance_checks
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: build-artifact
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_build_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$STAGE" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: sign-artifact
      when:
        - input: $(params.enable-image-signing)
          operator: in
          values:
            - 'true'
      image: $(params.image[3])
      imagePullPolicy: $(params.image-pull-policy[3])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[3])
        - name: secret
          mountPath: /config/$(params.secret[3])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[3])
      env:
        - name: STAGE
          value: $(params.stage[3])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_sign_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STEP_NAME
          value: sign-artifact
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[3])
      script: >
        #!/bin/sh

        export STAGE_SCRIPT_PATH="stage_script_runner_sign_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: scan-artifact
      image: $(params.image[4])
      imagePullPolicy: $(params.image-pull-policy[4])
      workingDir: /workspace/app
      onError: continue
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[4])
        - name: secret
          mountPath: /config/$(params.secret[4])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_static_scan
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: scan-artifact
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_scan_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi


        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[4])
        - name: secret
          mountPath: /config/$(params.secret[4])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: CONTAINERIZE_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: SCAN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STEP_NAME
          value: finalize
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      script: >
        #!/usr/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true

        stages=$(/usr/local/bin/list_stage_names)


        if [ "$(get_env STAGE_UNIT_TEST_STATUS "")" != "" ]; then
          unit_test_description="Unit tests finished running."
        else
          unit_test_description="Pipeline failed before checking this check"
        fi


        setup_exit_code=$(cat $(steps.step-setup.exitCode.path))

        if [[ "$stages" == *"unit-test"* ]]; then
          test_exit_code=$(cat $(steps.step-unit-test.exitCode.path))
        fi

        if [[ "$stages" == *"build-artifact"* ]]; then
          build_artifact_exit_code=$(cat $(steps.step-build-artifact.exitCode.path))
        fi

        if [[ "$stages" == *"scan-artifact"* ]]; then
          scan_artifact_exit_code=$(cat $(steps.step-scan-artifact.exitCode.path))
        fi


        if [ "$test_exit_code" == "0" ]; then
            UNIT_TESTS_STATE="success"
        else
            UNIT_TESTS_STATE="failure"
        fi


        if [ "$build_artifact_exit_code" == "0" ]; then
            BUILD_ARTIFACT_STATE="success"
        else
            BUILD_ARTIFACT_STATE="failure"
        fi


        if [ "$scan_artifact_exit_code" == "0" ]; then
            SCAN_ARTIFACT_STATE="success"
        else
            SCAN_ARTIFACT_STATE="failure"
        fi

        status_checks_prefix="$(get_env branch-protection-status-check-prefix
        "tekton")"

        while IFS= read -r STEP; do
          case "${STEP}" in
            ".unit-test")
              set-commit-status \
                  --repository "$(load_repo app-repo url)" \
                  --commit-sha "$(load_repo app-repo commit)" \
                  --state "$(get_env STAGE_UNIT_TEST_STATUS "failure")" \
                  --description "${unit_test_description}" \
                  --context "${status_checks_prefix}/${TASK_NAME}/code-unit-tests" \
                  --target-url "${PIPELINE_RUN_URL//\?/\/${TASK_NAME}\/unit-test\?}"
            ;;
            ".build-artifact")
              set-commit-status \
                  --repository "$(load_repo app-repo url)" \
                  --commit-sha "$(load_repo app-repo commit)" \
                  --state "${BUILD_ARTIFACT_STATE}" \
                  --description "build-artifact step ${BUILD_ARTIFACT_STATE}." \
                  --context "${status_checks_prefix}/${TASK_NAME}/build-artifact" \
                  --target-url "${PIPELINE_RUN_URL//\?/\/${TASK_NAME}\/build-artifact\?}"
              ;;
            ".scan-artifact")
              set-commit-status \
                  --repository "$(load_repo app-repo url)" \
                  --commit-sha "$(load_repo app-repo commit)" \
                  --state "${SCAN_ARTIFACT_STATE}" \
                  --description "Artifact(s) scanned." \
                  --context "${status_checks_prefix}/${TASK_NAME}/scan-artifact" \
                  --target-url "${PIPELINE_RUN_URL//\?/\/${TASK_NAME}\/scan-artifact\?}"
              ;;
          esac
        done <<< "$stages"
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: app-preview-pr-code-checks
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the name of the PR's repo target
    - name: base-repo-name
      description: name of the base repo
    - name: base-repo-owner
      description: owner of the base repo
    - name: pr-url
      description: PR url
    - name: pr-html-url
      description: HTML url of the PR
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'true'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      description: all repos from the pr-start
      default: '[]'
    - name: start-exitcode
      default: '0'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: docker-socket
        mountPath: /var/run/
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: toolchain
        mountPath: /toolchain
      - name: tmp
        mountPath: /tmp
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
      env:
        - name: STEP_NAME
          value: start
        - name: HEAD_REPO
          value: $(params.head-repo)
        - name: HEAD_BRANCH
          value: $(params.head-branch)
        - name: HEAD_SHA
          value: $(params.head-sha)
        - name: BASE_REPO
          value: $(params.base-repo)
        - name: BASE_BRANCH
          value: $(params.base-branch)
        - name: BASE_REPO_OWNER
          value: $(params.base-repo-owner)
        - name: BASE_REPO_NAME
          value: $(params.base-repo-name)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: PR_HTML_URL
          value: $(params.pr-html-url)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
        - name: start_exit_code
          value: $(params.start-exitcode)
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          set_env fatal_error "true"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        # start

        echo "app-preview-pr-code-check steps: ${stages[*]}"

        echo "Number of steps in app-preview-pr-code-check: ${#stages[@]}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        echo -n "${HEAD_REPO}" >> /config/head-repo

        echo -n "${HEAD_BRANCH}" >> /config/head-branch

        echo -n "${HEAD_SHA}" >> /config/head-sha

        echo -n "${BASE_REPO}" >> /config/base-repo

        echo -n "${BASE_BRANCH}" >> /config/base-branch

        echo -n "${BASE_REPO_OWNER}" >> /config/base-repo-owner

        echo -n "${BASE_REPO_NAME}" >> /config/base-repo-name


        echo -n "${HEAD_BRANCH}" >> /config/branch

        echo -n "${BASE_BRANCH}" >> /config/target-branch

        echo -n "${HEAD_SHA}" >> /config/commit-sha

        echo -n "${BASE_REPO}" >> /config/repository

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${PR_HTML_URL}" >> /config/pr-html-url

        set_env head-repo "${HEAD_REPO}"

        set_env head-branch "${HEAD_BRANCH}"

        set_env head-sha "${HEAD_SHA}"

        set_env base-repo "${BASE_REPO}"

        set_env base-branch "${BASE_BRANCH}"

        set_env base-repo-owner "${BASE_REPO_OWNER}"

        set_env base-repo-name "${BASE_REPO_NAME}"

        set_env branch "${HEAD_BRANCH}"

        set_env target-branch "${BASE_BRANCH}"

        set_env commit-sha "${HEAD_SHA}"

        set_env repository "${BASE_REPO}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env pr-html-url "${PR_HTML_URL}"


        . "${ONE_PIPELINE_PATH}/internal/task_start"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: checks-setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: docker-socket
          mountPath: /var/run/
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: checks-setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_checks_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: detect-secrets
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_detect_secrets
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: detect-secrets
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_detect_secrets"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: compliance-checks
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_compliance_checks
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: compliance-checks
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_compliance_checks"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi


        exit $exit_code
    - name: static-scan
      image: $(params.image[3])
      imagePullPolicy: $(params.image-pull-policy[3])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[3])
        - name: secret
          mountPath: /config/$(params.secret[3])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[3])
      env:
        - name: STAGE
          value: $(params.stage[3])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_static_scan
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STEP_NAME
          value: static-scan
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[3])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_static_scan"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi


        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[3])
        - name: secret
          mountPath: /config/$(params.secret[3])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[3])
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: DETECT_SECRETS_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: COMPLIANCE_CHECKS_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STATIC_SCAN_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STEP_NAME
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[3])
      script: >
        #!/usr/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        FATAL_ERROR="$(/usr/local/bin/get_env fatal_error "false")"

        if [ "$FATAL_ERROR" = "true" ]; then
          /usr/local/bin/set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        stages=$(/usr/local/bin/list_stage_names)


        setup_exit_code=$(cat $(steps.step-checks-setup.exitCode.path))

        if [[ "$stages" == *"detect-secrets"* ]]; then
          detect_secrets_exit_code=$(cat $(steps.step-detect-secrets.exitCode.path))
        fi

        if [[ "$stages" == *"compliance-checks"* ]]; then
          compliance_checks_exit_code=$(cat $(steps.step-compliance-checks.exitCode.path))
        fi

        if [[ "$stages" == *"static-scan"* ]]; then
          static_scan_exit_code=$(cat $(steps.step-static-scan.exitCode.path))
        fi


        if [ "$detect_secrets_exit_code" == "0" ]; then
          DETECT_SECRET_SCAN_STATE="success"
        else
          DETECT_SECRET_SCAN_STATE="failure"
        fi


        if [ "$compliance_checks_exit_code" == "0" ]; then
          COMPLIANCE_CHECKS_SCAN_STATE="success"
        else
          COMPLIANCE_CHECKS_SCAN_STATE="failure"
        fi


        if [ "$static_scan_exit_code" == "0" ]; then
            STATIC_SCAN_STATE="success"
        else
            STATIC_SCAN_STATE="failure"
        fi

        status_checks_prefix="$(get_env branch-protection-status-check-prefix
        "tekton")"

        while IFS= read -r STEP; do
          case "${STEP}" in
            ".detect-secrets")
              set-commit-status \
                  --repository "$(load_repo app-repo url)" \
                  --commit-sha "$(load_repo app-repo commit)" \
                  --state "${DETECT_SECRET_SCAN_STATE}" \
                  --description "Code detect secret scan done." \
                  --context "${status_checks_prefix}/${TASK_NAME}/code-detect-secrets" \
                  --target-url "${PIPELINE_RUN_URL//\?/\/${TASK_NAME}\/detect-secrets\?}"
            ;;
            ".static-scan")
              set-commit-status \
                  --repository "$(load_repo app-repo url)" \
                  --commit-sha "$(load_repo app-repo commit)" \
                  --state "${STATIC_SCAN_STATE}" \
                  --description "Code static scan done." \
                  --context "${status_checks_prefix}/${TASK_NAME}/code-static-scan" \
                  --target-url "${PIPELINE_RUN_URL//\?/\/${TASK_NAME}\/static-scan\?}"
              ;;
          esac
        done <<< "$stages"
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: app-preview-pr-deploy-checks
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the name of the PR's repo target
    - name: base-repo-name
      description: name of the base repo
    - name: base-repo-owner
      description: owner of the base repo
    - name: pr-url
      description: PR url
    - name: pr-html-url
      description: HTML url of the PR
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: ARTIFACTS
      type: array
      description: all artifacts from the code-build
      default: []
    - name: start-exitcode
      default: '0'
  results:
    - name: IMAGES
      type: string
      default: ''
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: docker-socket
        mountPath: /var/run/
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: toolchain
        mountPath: /toolchain
      - name: tmp
        mountPath: /tmp
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
      env:
        - name: STEP_NAME
          value: start
        - name: HEAD_REPO
          value: $(params.head-repo)
        - name: HEAD_BRANCH
          value: $(params.head-branch)
        - name: HEAD_SHA
          value: $(params.head-sha)
        - name: BASE_REPO
          value: $(params.base-repo)
        - name: BASE_BRANCH
          value: $(params.base-branch)
        - name: BASE_REPO_OWNER
          value: $(params.base-repo-owner)
        - name: BASE_REPO_NAME
          value: $(params.base-repo-name)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: PR_HTML_URL
          value: $(params.pr-html-url)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
        - name: start_exit_code
          value: $(params.start-exitcode)
      args:
        - $(params.ARTIFACTS[*])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done
        fi

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          set_env fatal_error "true"
          exit ${start_exit_code}
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" = "true" ]; then
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        echo "app-preview-pr-deploy-checks steps: ${stages[*]}"

        echo "Number of steps in app-preview-pr-deploy-checks: ${#stages[@]}"


        # start


        export PATH="$PATH:/opt/cocoa/bin"

        echo -n "${HEAD_REPO}" >> /config/head-repo

        echo -n "${HEAD_BRANCH}" >> /config/head-branch

        echo -n "${HEAD_SHA}" >> /config/head-sha

        echo -n "${BASE_REPO}" >> /config/base-repo

        echo -n "${BASE_BRANCH}" >> /config/base-branch

        echo -n "${BASE_REPO_OWNER}" >> /config/base-repo-owner

        echo -n "${BASE_REPO_NAME}" >> /config/base-repo-name


        echo -n "${HEAD_BRANCH}" >> /config/branch

        echo -n "${BASE_BRANCH}" >> /config/target-branch

        echo -n "${HEAD_SHA}" >> /config/commit-sha

        echo -n "${BASE_REPO}" >> /config/repository

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${PR_HTML_URL}" >> /config/pr-html-url

        set_env head-repo "${HEAD_REPO}"

        set_env head-branch "${HEAD_BRANCH}"

        set_env head-sha "${HEAD_SHA}"

        set_env base-repo "${BASE_REPO}"

        set_env base-branch "${BASE_BRANCH}"

        set_env base-repo-owner "${BASE_REPO_OWNER}"

        set_env base-repo-name "${BASE_REPO_NAME}"

        set_env branch "${HEAD_BRANCH}"

        set_env target-branch "${BASE_BRANCH}"

        set_env commit-sha "${HEAD_SHA}"

        set_env repository "${BASE_REPO}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env pr-html-url "${PR_HTML_URL}"

        set_env pipeline_namespace "pr"


        . "${ONE_PIPELINE_PATH}/internal/task_start"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: deploy
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_deploy
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_deploy"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: acceptance-test
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_acceptance_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: acceptance-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_acceptance_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: collect-evidence
        - name: DEPLOY_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: ACCEPTANCE_TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/usr/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        FATAL_ERROR="$(/usr/local/bin/get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          /usr/local/bin/set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        stages=$(/usr/local/bin/list_stage_names)


        if [[ "$stages" == *"deploy"* ]]; then
          deploy_exit_code=$(cat $(steps.step-deploy.exitCode.path))
        fi

        if [[ "$stages" == *"acceptance-test"* ]]; then
          acceptance_test_exit_code=$(cat $(steps.step-acceptance-test.exitCode.path))
        fi


        if [ "$deploy_exit_code" == "0" ]; then
            APP_PREVIEW_STATE="success"
        else
            APP_PREVIEW_STATE="failure"
        fi


        if [ "$acceptance_test_exit_code" == "0" ]; then
            ACCEPTANCE_TESTS_STATE="success"
        else
            ACCEPTANCE_TESTS_STATE="failure"
        fi

        status_checks_prefix="$(get_env branch-protection-status-check-prefix
        "tekton")"

        while IFS= read -r STEP; do
          case "${STEP}" in
            ".deploy")
              set-commit-status \
                  --repository "$(load_repo app-repo url)" \
                  --commit-sha "$(load_repo app-repo commit)" \
                  --state "${APP_PREVIEW_STATE}" \
                  --description "Application preview deployed." \
                  --context "${status_checks_prefix}/${TASK_NAME}/app-preview" \
                  --target-url "${PIPELINE_RUN_URL//\?/\/${TASK_NAME}\/deploy\?}"
              ;;
            ".acceptance-test")
              set-commit-status \
                  --repository "$(load_repo app-repo url)" \
                  --commit-sha "$(load_repo app-repo commit)" \
                  --state "${ACCEPTANCE_TESTS_STATE}" \
                  --description "Acceptance tests ${ACCEPTANCE_TESTS_STATE} running." \
                  --context "${status_checks_prefix}/${TASK_NAME}/acceptance-tests" \
                  --target-url "${PIPELINE_RUN_URL//\?/\/${TASK_NAME}\/acceptance-test\?}"
              ;;
          esac
        done <<< "$stages"
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: async-stage
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: start,evaluate
    devops.cloud.ibm.com/finish-step: finish
spec:
  params:
    - name: stage
      description: Stage in the config file
      type: array
      default:
        - ''
        - ''
    - name: async-stage
      description: async stage name
      default: owasp-zap
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      type: array
      default:
        - 'null'
        - 'null'
  results:
    - name: IMAGES
      type: string
      default: ''
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: docker-socket
        mountPath: /var/run/
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: $(params.dind)
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: toolchain
          mountPath: /toolchain
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
        - name: STEP_NAME
          value: setup
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
      script: >
        #!/usr/bin/env bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        TASK_NAME_ORIGINAL="${TASK_NAME}"


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        if [ "$(get_env pipeline-error-status "")" == "failed" ]; then
          exit 1
        fi

        env

        export PATH="$PATH:/opt/cocoa/bin"

        . "${ONE_PIPELINE_PATH}/internal/async_stage_setup"


        STAGE=$(params.async-stage)

        stage_script="/tmp/stage-script-$STAGE"

        stage_script_runner="${WORKSPACE}/stage_script_runner"

        touch $stage_script

        if [ ! -f ${stage_script_runner} ]; then
          echo "Creating stage script runner"
          echo \#\!/bin/bash > "${stage_script_runner}"
          echo >> "${stage_script_runner}"
          echo "${stage_script}" >> "${stage_script_runner}"
        fi

        cat ${stage_script_runner}


        chmod +x ${stage_script_runner}

        yq_tool="yq"

        if command -v yq4 > /dev/null; then
          # use yq4 as the yq tool - as this the yq binary name in compliance-baseimage
          yq_tool="yq4"
        fi

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        script=$($yq_tool -p yaml -o json "$config_file" | jq -j --arg task
        "$STAGE" --arg stage "run-stage" '(.[$stage]?.script //
        (.tasks[$task].steps[]? | select(.name == $stage) | .script) //
        (.finally[$task]?.steps[]? | select(.name == $stage) | .script)) //
        null')

        if [ "$script" == "null" ]; then
          step_name=${STAGE}
          task_name=${TASK_NAME_ORIGINAL}
        else
          step_name="run-stage"
          task_name=${STAGE}
        fi

        generate_user_script "${stage_script}" script "${step_name}"
        "${task_name}"

        chmod +x ${stage_script}


        # generate stage script for custom finish


        stage_script="/tmp/stage-script-finish"

        stage_script_runner="${WORKSPACE}/stage_script_runner_finish"

        touch $stage_script

        if [ ! -f ${stage_script_runner} ]; then
          echo "Creating stage script runner"
          echo \#\!/bin/bash > "${stage_script_runner}"
          echo >> "${stage_script_runner}"
          echo "${stage_script}" >> "${stage_script_runner}"
        fi

        cat ${stage_script_runner}


        chmod +x ${stage_script_runner}

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        generate_user_script "${stage_script}" script "finish"
        "${TASK_NAME_ORIGINAL}"

        chmod +x ${stage_script}


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: run-stage
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      onError: continue
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_RUN_AFTER
          value: $(params.run-after[0])
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_run_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0)
        }' | sed 's/-/_/g')"

        run_stage_exit_code=$(get_env STAGE_"${current_run_stage}"_EXIT_CODE)


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$(params.stage[0])" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi

        exit $run_stage_exit_code
    - name: evaluate
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: working-dir
          mountPath: /stage-finish
      env:
        - name: STEP_NAME
          value: evaluate
        - name: STAGE_WORKSPACE
          value: /stage-finish
        - name: ON_ERROR
          value: ''
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export PATH="$PATH:/opt/cocoa/bin"

        if "${ONE_PIPELINE_PATH}/internal/async_stage_finish"; then
          finish_exit_code=0
        else
          finish_exit_code=$?
        fi
        exit $finish_exit_code
    - name: finish
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-finish
      env:
        - name: STEP_NAME
          value: finish
        - name: STAGE_WORKSPACE
          value: /stage-finish
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: $(params.run-after[1])
      script: >
        #!/bin/bash


        export PATH="$PATH:/opt/cocoa/bin"

        export finish_evaluate_exit_code

        finish_evaluate_exit_code=$(cat $(steps.step-evaluate.exitCode.path))

        run_stage_exit_code=$(cat $(steps.step-run-stage.exitCode.path))

        export STAGE_SCRIPT_PATH="stage_script_runner_finish"


        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?


        exit_status=$(($exit_code + $finish_evaluate_exit_code +
        run_stage_exit_code))


        if [ $exit_status -gt 0 ]; then
          echo "Pipeline run failed. Please verify the status of previous stages"
          exit 1
        fi

        exit 0
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cc-artifact-scan
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-name
      description: The name of the running pipeline
      default: ''
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
  results:
    - name: scan-artifact-state
      description: artifact scan state
      default: Failed
    - name: CCARTIFACTSCANSTATUS
      description: cc artifact scan status
      default: '[]'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        value: $(params.pipeline-name)
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "Failed" > $(results.scan-artifact-state.path)

        echo -n "[]" > $(results.CCARTIFACTSCANSTATUS.path)


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cc-artifact-scan steps: ${stages[*]}"

        echo "Number of steps in cc-artifact-scan: ${#stages[@]}"


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"

        set_env pipeline_namespace "cc"

        . "${ONE_PIPELINE_PATH}/internal/cc_start"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file" cc
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: artifact-scan-setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: artifact-scan-setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_artifact_scan_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit ${exit_code}
    - name: scan-artifact
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_scan_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: scan-artifact
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_scan_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit ${exit_code}
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: SCAN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: finalize-and-upload
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/usr/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          pipelinectl serialize STAGE_SCAN_ARTIFACT_STATUS > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.CCARTIFACTSCANSTATUS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cc-deployment-scan
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-name
      description: The name of the running pipeline
      default: ''
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
  results:
    - name: dynamic-scan-state
      description: dynamic scan state
      default: Failed
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        value: $(params.pipeline-name)
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # start

        echo -n "Failed" > "$(results.dynamic-scan-state.path)"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cc-deployment-scan steps: ${stages[*]}"

        echo "Number of steps in cc-deployment-scan: ${#stages[@]}"


        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        set_env pipeline_namespace "cc"

        . "${ONE_PIPELINE_PATH}/internal/cc_start"


        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file" cc
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: deployment-scan-setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: deployment-scan-setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_deployment_scan_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit ${exit_code}
    - name: dynamic-scan
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_dynamic_scan
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: dynamic-scan
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_dynamic_scan"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit ${exit_code}
    - name: acceptance-test
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_acceptance_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: acceptance-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_acceptance_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit ${exit_code}
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: DYNAMIC_SCAN_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: ACCEPTANCE_TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/usr/bin/bash


        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cc-finish
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: evaluate,prepare
spec:
  params:
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-name
      description: The name of the running pipeline
      default: cc-pipeline
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: static-scan-state
      description: static-scan state
      default: ''
    - name: dynamic-scan-state
      description: dynamic scan state
      default: ''
    - name: compliance-checks-state
      description: compliance check state
      default: ''
    - name: scan-artifact-exit-status
      description: scan artifact exit status
      default: ''
    - name: CCREPOSCANSSTATUS
      description: cc repo scan status
      type: array
      default: []
    - name: scan-artifact-state
      description: scan artifact state
      default: ''
    - name: CCARTIFACTSCANSTATUS
      description: artifact scan status
      type: array
      default: []
  volumes:
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: configmap
        mountPath: /config/$(params.configmap)
      - name: secret
        mountPath: /config/$(params.secret)
      - name: toolchain
        mountPath: /toolchain
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        value: $(params.pipeline-name)
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: evaluate
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      onError: continue
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: evaluate
      args:
        - $(params.CCREPOSCANSSTATUS[*])
        - $(params.CCARTIFACTSCANSTATUS[*])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export PATH="$PATH:/opt/cocoa/bin"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
          cleanup_data_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done
        fi


        set_env pipeline_namespace "cc"

        . "${ONE_PIPELINE_PATH}/internal/cc_start"

        . "${ONE_PIPELINE_PATH}/internal/cc_finish"
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: tmp
          mountPath: /tmp
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
      script: >
        #!/bin/bash


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        echo "Preparing custom finish step"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config)"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file" cc
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: run-stage
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: tmp
          mountPath: /tmp
      env:
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        finish_evaluate_exit_code=$(cat $(steps.step-evaluate.exitCode.path))

        export finish_evaluate_exit_code

        export STAGE_SCRIPT_PATH="stage_script_runner_run_stage"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?

        if [ $finish_evaluate_exit_code -ne 0 ]; then
          echo "Failing the finish stage as \"evaluate\" step failed with error code $finish_evaluate_exit_code"
        fi

        if [ "$PIPELINE_STATUS" != "Succeeded" -a "$PIPELINE_STATUS" !=
        "Completed" ]; then
          echo "Pipeline run failed. Please verify the status of previous stages"
          exit 1
        fi

        exit $(($exit_code + $finish_evaluate_exit_code))
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cc-repo-scan
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'true'
        - 'true'
        - 'true'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-name
      description: The name of the running pipeline
      default: ''
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
  results:
    - name: static-scan-state
      description: static scan status
      default: Failed
    - name: compliance-checks-state
      description: compliance check status
      default: Failed
    - name: CCREPOSCANSSTATUS
      description: cc repo scan status
      default: '[]'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        value: $(params.pipeline-name)
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.CCREPOSCANSSTATUS.path)

        echo -n "Failed" > "$(results.static-scan-state.path)"

        echo -n "Failed" > "$(results.compliance-checks-state.path)"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cc-repo-scan steps: ${stages[*]}"

        echo "Number of steps in cc-repo-scan: ${#stages[@]}"


        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        set_env pipeline_namespace "cc"

        . "${ONE_PIPELINE_PATH}/internal/cc_start"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file" cc
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: repo-scan-setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: repo-scan-setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_repo_scan_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit ${exit_code}
    - name: detect-secrets
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_detect_secrets
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: detect-secrets
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_detect_secrets"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit ${exit_code}
    - name: static-scan
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_static_scan
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: static-scan
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_static_scan"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit ${exit_code}
    - name: compliance-checks
      image: $(params.image[3])
      imagePullPolicy: $(params.image-pull-policy[3])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[3])
        - name: secret
          mountPath: /config/$(params.secret[3])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[3])
      env:
        - name: STAGE
          value: $(params.stage[3])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_compliance_checks
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STEP_NAME
          value: compliance-checks
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[3])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_compliance_checks"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit ${exit_code}
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[3])
        - name: secret
          mountPath: /config/$(params.secret[3])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[3])
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: DETECT_SECRETS_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STATIC_SCAN_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: COMPLIANCE_CHECKS_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STEP_NAME
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[3])
      script: >
        #!/usr/bin/bash


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          pipelinectl serialize DETECT_SECRETS_STATUS > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.CCREPOSCANSSTATUS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cc-start
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: start
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: next-stage
      default: ''
    - name: final-stage
      default: ''
  results:
    - name: image
      description: Docker image to be used in the specified stage.
    - name: script
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: configmap
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: secret
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: working-dir
      description: one-pipeline working dir, the location of one-pipeline.yaml
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: dind
      description: Launch Docker-in-Docker sidecar
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: stage-skip
      description: Whether the stage needs to skipped if possible
    - name: final-stage-image
      description: Docker image to be used in the specified stage.
    - name: final-stage-script
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: final-stage-configmap
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: final-stage-secret
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: final-stage-abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: final-stage-dind
      description: Launch Docker-in-Docker sidecar
    - name: final-stage-image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: final-stage-skip
      description: Whether the stage needs to skipped if possible
    - name: run-after
      description: >-
        Specify the previous stage that needs to be complete before starting
        next stage
  volumes:
    - name: config
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: COMPLIANCE_BASEIMAGE
        value: $(params.compliance-baseimage)
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      onError: continue
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export PATH="$PATH:/opt/cocoa/bin"
        BRANCH=${BRANCH#"refs/heads/"}
        BRANCH=${BRANCH#"refs/tags/"}
        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi
        echo -n "$BRANCH" >> /config/branch
        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration
        echo -n "${COMMIT_ID}" >> /config/commit-sha
        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp
        echo -n "${PR_URL}" >> /config/pr-url
        echo -n "${REPOSITORY}" >> /config/repository
        set_env branch "$BRANCH"
        set_env repository-integration "${REPOSITORY_INTEGRATION}"
        set_env commit-sha "${COMMIT_ID}"
        set_env commit-timestamp "${COMMIT_TIMESTAMP}"
        set_env pr-url "${PR_URL}"
        set_env repository "${REPOSITORY}"

        set_env pipeline_namespace "cc"
        . "${ONE_PIPELINE_PATH}/internal/pipeline_start"
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cd-change-request-deploy
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start,change-request
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
  results:
    - name: change-request-id
      description: The id of the created change request
      default: Failed
    - name: change-request-state
      description: change request status
      default: Failed
    - name: CDDEPLOYPROPERTIES
      type: string
      default: '[]'
    - name: skip-peer-review
      description: Whether to skip peer review
      default: 'false'
    - name: skip-artifact-verification
      description: Whether to skip peer review
      default: 'false'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"


        # Initialize task results

        echo -n "[]" > $(results.CDDEPLOYPROPERTIES.path)

        echo -n "Failed" > "$(results.change-request-state.path)"

        echo -n "false" > "$(results.skip-peer-review.path)"

        echo -n "false" > "$(results.skip-artifact-verification.path)"

        export skip_peer_review="$(results.skip-peer-review.path)"

        export
        skip_artifact_verification="$(results.skip-artifact-verification.path)"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cd-deploy steps: ${stages[*]}"

        echo "Number of steps in cd-deploy: ${#stages[@]}"


        set_env pipeline_namespace "cd"

        # start

        if "${ONE_PIPELINE_PATH}/internal/cd_start"; then
          cd_start_exit_code=0
        else
          cd_start_exit_code=$?
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-cd-pipeline-config.yaml" v11 "$config_file" cd
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare

        exit $cd_start_exit_code
    - name: setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app)
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: dind-image
          value: $(params.dind-image)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        soft_abort=$(/opt/cocoa/bin/pipelinectl get_env soft-abort "false")

        if [ "$soft_abort" = "true" ]; then
          echo "soft aborting pipeline due to no-delta"
          exit 0
        fi

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: verify-peer-review
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_verify_peer_review
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: verify-peer-review
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        soft_abort=$(/opt/cocoa/bin/pipelinectl get_env soft-abort "false")

        if [ "$soft_abort" = "true" ]; then
          echo "soft aborting pipeline due to no-delta"
          exit 0
        fi

        export STAGE_SCRIPT_PATH="stage_script_runner_verify_peer_review"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: verify-artifact
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_verify_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: verify-artifact
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        soft_abort=$(/opt/cocoa/bin/pipelinectl get_env soft-abort "false")

        if [ "$soft_abort" = "true" ]; then
          echo "soft aborting pipeline due to no-delta"
          exit 0
        fi

        export STAGE_SCRIPT_PATH="stage_script_runner_verify_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: change-request
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-change-request
      env:
        - name: STAGE
          value: change-request
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_change_request
        - name: STAGE_ABORT_ON_FAILURE
          value: stopAndFail
        - name: STEP_NAME
          value: change-request
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-change-request
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log
        soft_abort=$(/opt/cocoa/bin/pipelinectl get_env soft-abort "false")
        if [ "$soft_abort" == "true" ]; then
          echo "soft aborting pipeline due to no-delta"
          exit 0
        fi
        export CR_ID_PATH="$(results.change-request-id.path)"
        . "${ONE_PIPELINE_PATH}/internal/cd_change_request"
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: VERIFY_PEER_REVIEW_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: VERIFY_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: CHANGE_REQUEST_STAGE_ABORT_ON_FAILURE
          value: stopAndFail
        - name: STEP_NAME
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      script: >
        #!/usr/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        "${ONE_PIPELINE_PATH}/tools/publish_evidence"

        "${ONE_PIPELINE_PATH}"/internal/inventory/delete_tag
        "RUN-${PIPELINE_RUN_ID}"


        stages=$(/usr/local/bin/list_stage_names)

        # capture exit code of each step

        echo "checking the exit status code"

        setup_exit_code=$(cat $(steps.step-setup.exitCode.path))

        change_request_exit_code=$(cat
        $(steps.step-change-request.exitCode.path))

        if [[ "$stages" == *"peer-review"* ]]; then
          verify_peer_review_exit_code=$(cat $(steps.step-verify-peer-review.exitCode.path))
        fi

        if [[ "$stages" == *"verify-artifact"* ]]; then
          verify_artifact_exit_code=$(cat $(steps.step-verify-artifact.exitCode.path))
        fi


        if [ "$change_request_exit_code" == "0" ]; then
           echo -n "Succeeded" > "$(results.change-request-state.path)"
        else
           echo -n "Failed" > "$(results.change-request-state.path)"
        fi


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-deployments $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.CDDEPLOYPROPERTIES.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cd-change-request-finish
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: evaluate,prepare
    devops.cloud.ibm.com/finish-step: finish
spec:
  params:
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: CDDEPLOYPROPERTIES
      type: array
      description: Status of all properties set in serialize of cd-deploy stage
      default: []
  results:
    - name: skip-peer-review
      description: Whether to skip peer review
      default: 'false'
    - name: skip-artifact-verification
      description: Whether to skip peer review
      default: 'false'
  volumes:
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: toolchain
        mountPath: /toolchain
      - mountPath: /var/run/
        name: docker-socket
      - name: tmp
        mountPath: /tmp
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: pipelinectl
          mountPath: /usr/share/cocoa
      env:
        - name: STEP_NAME
          value: prepare
      args:
        - $(params.CDDEPLOYPROPERTIES[*])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
          cleanup_data_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          # loop through all the arguments - CDDEPLOYPROPERTIES
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done
        fi


        #prepare

        # set_env pipeline_namespace "cd"

        # start

        echo -n "false" > "$(results.skip-peer-review.path)"

        echo -n "false" > "$(results.skip-artifact-verification.path)"

        export skip_peer_review="$(results.skip-peer-review.path)"

        export
        skip_artifact_verification="$(results.skip-artifact-verification.path)"

        if "${ONE_PIPELINE_PATH}/internal/cd_start"; then
          prepare_exit_code=0
        else
          prepare_exit_code=$?
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-cd-pipeline-config.yaml" v11 "$config_file" cd
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: evaluate
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      onError: continue
      env:
        - name: STEP_NAME
          value: evaluate
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi
        soft_abort=$(/opt/cocoa/bin/pipelinectl get_env soft-abort "false")
        if [ "$soft_abort" == "true" ]; then
          echo "soft aborting pipeline due to no-delta"
          exit 0
        fi
        set_env pipeline-execution-status "successful_deployment"
        echo "Calling cd_finish..."
        . "${ONE_PIPELINE_PATH}/internal/cd_finish"
    - name: finish
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/configmap
        - name: secret
          mountPath: /config/secret
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" -eq 1 ]; then
          pwd
          set -x
        fi


        finish_evaluate_exit_code=$(cat $(steps.step-evaluate.exitCode.path))

        export finish_evaluate_exit_code

        export STAGE_SCRIPT_PATH="stage_script_runner_finish"


        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?


        exit_status=$(($exit_code + $finish_evaluate_exit_code))


        if [ $exit_status -gt 0 ] || [ "$PIPELINE_STATUS" != "Succeeded" -a
        "$PIPELINE_STATUS" != "Completed" ]; then
          echo "Pipeline run failed. Please verify the status of previous stages"
          exit 1
        fi

        exit 0
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cd-deploy
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start,change-request
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: rollback-enabled
      description: Trigger a rollback if deployment fails
      default: '0'
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
  results:
    - name: change-request-id
      description: The id of the created change request
      default: Failed
    - name: change-request-state
      description: change request status
      default: Failed
    - name: deployment-status
      description: prod deployment status
      default: Failed
    - name: acceptance-test-state
      description: acceptance test status
      default: Failed
    - name: CDDEPLOYPROPERTIES
      type: string
      default: '[]'
    - name: skip-peer-review
      description: Whether to skip peer review
      default: 'false'
    - name: skip-artifact-verification
      description: Whether to skip peer review
      default: 'false'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"


        # Initialize task results

        echo -n "[]" > $(results.CDDEPLOYPROPERTIES.path)

        echo -n "Failed" > "$(results.change-request-state.path)"

        echo -n "Failed" > "$(results.deployment-status.path)"

        echo -n "Failed" > "$(results.acceptance-test-state.path)"

        echo -n "false" > "$(results.skip-peer-review.path)"

        echo -n "false" > "$(results.skip-artifact-verification.path)"

        export skip_peer_review="$(results.skip-peer-review.path)"

        export
        skip_artifact_verification="$(results.skip-artifact-verification.path)"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cd-deploy steps: ${stages[*]}"

        echo "Number of steps in cd-deploy: ${#stages[@]}"


        set_env pipeline_namespace "cd"

        # start

        . "${ONE_PIPELINE_PATH}/internal/cd_start"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-cd-pipeline-config.yaml" v11 "$config_file" cd
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app)
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: dind-image
          value: $(params.dind-image)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: verify-peer-review
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_verify_peer_review
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: verify-peer-review
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" -eq 1 ]; then
            pwd
            set -x
        fi

        export STAGE_SCRIPT_PATH="stage_script_runner_verify_peer_review"

        skip_peer_review=$(/opt/cocoa/bin/pipelinectl get_env skip-peer-review
        "false")

        if [ "$skip_peer_review" = "true" ]; then
          exit 0
        fi

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: verify-artifact
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_verify_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: verify-artifact
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" -eq 1 ]; then
            pwd
            set -x
        fi

        export STAGE_SCRIPT_PATH="stage_script_runner_verify_artifact"

        skip_artifact_verification=$(/opt/cocoa/bin/pipelinectl get_env
        skip-artifact-verification "false")

        if [ "$skip_artifact_verification" = "true" ]; then
          exit 0
        fi

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: change-request
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-change-request
      env:
        - name: STAGE
          value: change-request
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_change_request
        - name: STAGE_ABORT_ON_FAILURE
          value: stopAndFail
        - name: STEP_NAME
          value: change-request
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-change-request
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log
        export CR_ID_PATH="$(results.change-request-id.path)"
        . "${ONE_PIPELINE_PATH}/internal/cd_change_request"
    - name: deploy
      image: $(params.image[3])
      imagePullPolicy: $(params.image-pull-policy[3])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[3])
        - name: secret
          mountPath: /config/$(params.secret[3])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[3])
      env:
        - name: STAGE
          value: $(params.stage[3])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_deploy
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STEP_NAME
          value: deploy
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[3])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_deploy"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: acceptance-test
      image: $(params.image[4])
      imagePullPolicy: $(params.image-pull-policy[4])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[4])
        - name: secret
          mountPath: /config/$(params.secret[4])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_acceptance_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: acceptance-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_acceptance_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: VERIFY_PEER_REVIEW_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: VERIFY_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: CHANGE_REQUEST_STAGE_ABORT_ON_FAILURE
          value: stopAndFail
        - name: DEPLOYMENT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: ACCEPTANCE_TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      script: >
        #!/usr/bin/bash


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        stages=$(/usr/local/bin/list_stage_names)

        # capture exit code of each step

        echo "checking the exit status code"

        setup_exit_code=$(cat $(steps.step-setup.exitCode.path))

        change_request_exit_code=$(cat
        $(steps.step-change-request.exitCode.path))

        if [[ "$stages" == *"peer-review"* ]]; then
          verify_peer_review_exit_code=$(cat $(steps.step-verify-peer-review.exitCode.path))
        fi

        if [[ "$stages" == *"verify-artifact"* ]]; then
          verify_artifact_exit_code=$(cat $(steps.step-verify-artifact.exitCode.path))
        fi

        if [[ "$stages" == *"deploy"* ]]; then
          deployment_exit_code=$(cat $(steps.step-deploy.exitCode.path))
        fi

        if [[ "$stages" == *"acceptance-test"* ]]; then
          acceptance_test_exit_code=$(cat $(steps.step-acceptance-test.exitCode.path))
        fi


        if [ "$change_request_exit_code" == "0" ]; then
           echo -n "Succeeded" > "$(results.change-request-state.path)"
        else
           echo -n "Failed" > "$(results.change-request-state.path)"
        fi


        if [ "$deployment_exit_code" == "0" ]; then
           echo -n "Succeeded" > "$(results.deployment-status.path)"
        else
           echo -n "Failed" > "$(results.deployment-status.path)"
        fi


        if [ "$acceptance_test_exit_code" == "0" ]; then
           echo -n "Succeeded" > "$(results.acceptance-test-state.path)"
        else
           echo -n "Failed" > "$(results.acceptance-test-state.path)"
        fi


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-deployments $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.CDDEPLOYPROPERTIES.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cd-finish
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: evaluate,prepare
    devops.cloud.ibm.com/finish-step: finish
spec:
  params:
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: deployment-status
      description: Deployment status
      default: ''
    - name: acceptance-tests-state
      description: Acceptance tests status
      default: ''
    - name: rollback-enabled
      description: Trigger a rollback if deployment fails
      default: '0'
    - name: CDDEPLOYPROPERTIES
      type: array
      description: Status of all properties set in serialize of cd-deploy stage
      default: []
  results:
    - name: skip-peer-review
      description: Whether to skip peer review
      default: 'false'
    - name: skip-artifact-verification
      description: Whether to skip peer review
      default: 'false'
  volumes:
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: rollback-configmap
      configMap:
        name: $(params.configmap[1])
        optional: true
    - name: rollback-secret
      secret:
        secretName: $(params.secret[1])
        optional: true
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: toolchain
        mountPath: /toolchain
      - mountPath: /var/run/
        name: docker-socket
      - name: tmp
        mountPath: /tmp
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: DEPLOYMENT_STATUS
        value: $(params.deployment-status)
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: pipelinectl
          mountPath: /usr/share/cocoa
      env:
        - name: STEP_NAME
          value: prepare
      args:
        - $(params.CDDEPLOYPROPERTIES[*])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
          cleanup_data_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          # loop through all the arguments - CDDEPLOYPROPERTIES
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done
        fi


        #prepare

        # set_env pipeline_namespace "cd"

        # start

        echo -n "false" > "$(results.skip-peer-review.path)"

        echo -n "false" > "$(results.skip-artifact-verification.path)"

        export skip_peer_review="$(results.skip-peer-review.path)"

        export
        skip_artifact_verification="$(results.skip-artifact-verification.path)"

        . "${ONE_PIPELINE_PATH}/internal/cd_start"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-cd-pipeline-config.yaml" v11 "$config_file" cd
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: rollback-if-needed
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      onError: continue
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: rollback-configmap
          mountPath: /config/rollback-configmap
        - name: rollback-secret
          mountPath: /config/rollback-secret
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: rollback
        - name: STAGE_RUN_AFTER
          value: 'null'
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        echo "notRun" > /config/rollback-status

        echo "" > /config/rollback-exit-code

        /opt/cocoa/bin/pipelinectl set_env rollback-status "notRun"

        /opt/cocoa/bin/pipelinectl set_env rollback-exit-code ""


        echo "Deployment status: $(params.deployment-status)"

        echo "Acceptance-tests status: $(params.acceptance-tests-state)"


        if [ "$(params.rollback-enabled)" != "1" ]; then
          echo "Rollback is not enabled, to enable rollback functionality set the \"rollback-enabled\" env property to \"1\"."
          echo "This step will be skipped."
          exit 0
        fi


        rollback_triggered="false"


        if [ "$(params.acceptance-tests-state)" != "[]" ]; then
          if [ "$(params.acceptance-tests-state)" != "Succeeded" ]; then
            rollback_triggered="true"
          fi
        else
          echo "Acceptance tests has not reported any status, the task might be skipped. Checking deployment status next."
        fi


        if [ "$(params.deployment-status)" != "Succeeded" ]; then
          rollback_triggered="true"
        fi


        if [ "$rollback_triggered" != "true" ]; then
          echo "*********    NO ROLLBACK EXECUTED    *********"
          echo "This step will now passthrough."
          exit 0
        fi


        echo "Triggering rollback script."

        export STAGE_SCRIPT_PATH="stage_script_runner_rollback_if_needed"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_run_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0)
        }' | sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_run_stage}"_EXIT_CODE)

        if [ $exit_code -eq 0 ]; then
          echo "success" > /config/rollback-status
          set_env rollback-status "success"
        else
          echo "failure" > /config/rollback-status
          set_env rollback-status "failure"
        fi

        echo "$exit_code" > /config/rollback-exit-code

        set_env rollback-exit-code "$exit_code"

        exit $exit_code
    - name: post-deploy
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      onError: continue
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: configmap
          mountPath: /config/configmap
        - name: secret
          mountPath: /config/secret
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[1])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: post-deploy
        - name: STAGE_RUN_AFTER
          value: 'null'
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        LOGFILE="/tmp/logs/$TASK_NAME/$STEP_NAME"

        mkdir -p "$(dirname "$LOGFILE")" && touch "$LOGFILE"

        chmod 644 "$LOGFILE"

        (

        trap 'printf "%s" "$?" > /tmp/sub.$$' EXIT

        export rollback_exit_code

        rollback_exit_code=$(/opt/cocoa/bin/pipelinectl get_env
        rollback-exit-code "0")

        export STAGE_SCRIPT_PATH="stage_script_runner_post_deploy"


        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?


        if [ $exit_code -eq 0 ]; then
          echo "Post-deploy stage completed successfully."
        else
          echo "Post-deploy stage failed."
        fi

        echo "$exit_code" > /config/post-deploy-exit-code

        set_env post-deploy-exit-code "$exit_code"

        exit $exit_code

        ) 2>&1 | tee "$LOGFILE"

        exit $(cat "/tmp/sub.$$")
    - name: evaluate
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      onError: continue
      env:
        - name: STEP_NAME
          value: evaluate
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        rollback_status=$(get_env rollback-status)
        rollback_exit_code=$(get_env rollback-exit-code)

        post_deploy_exit_code=$(get_env post-deploy-exit-code "0")
        if [ $post_deploy_exit_code != 0 ]; then
          echo "Post-deploy stage has failed with exit code $post_deploy_exit_code. Please check the logs of the post-deploy step for more details."
        fi

        pipeline_execution_status="successful_deployment"
        if [ $rollback_status != "notRun" ]; then
          if [ $rollback_status = "failure" ]; then
            echo "Rollback has failed with exit code $rollback_exit_code. Please check the logs of the rollback-if-needed step for more details."
            pipeline_execution_status="failed_deployment_failed_rollback"
          else
            pipeline_execution_status="failed_deployment_successful_rollback"
          fi
        fi

        set_env pipeline-execution-status "${pipeline_execution_status}"
        echo "Calling cd_finish..."
        . "${ONE_PIPELINE_PATH}/internal/cd_finish"
    - name: finish
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: configmap
          mountPath: /config/configmap
        - name: secret
          mountPath: /config/secret
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[2])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export finish_evaluate_exit_code

        export rollback_exit_code

        export post_deploy_exit_code


        finish_evaluate_exit_code=$(cat $(steps.step-evaluate.exitCode.path))

        rollback_exit_code=$(/opt/cocoa/bin/pipelinectl get_env
        rollback-exit-code "0")

        post_deploy_exit_code=$(/opt/cocoa/bin/pipelinectl get_env
        post-deploy-exit-code "0")


        export STAGE_SCRIPT_PATH="stage_script_runner_finish"


        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?


        if [ $exit_code -gt 0 ]; then
          echo "Custom Finish stage has failed with exit code $exit_code. Please check the logs of the run-stage step for more details."
        fi


        if [ $finish_evaluate_exit_code -gt 0 ]; then
          echo "CD finish evaluation has failed with exit code $finish_evaluate_exit_code. Please check the logs of the evaluate step for more details."
        fi


        if [ $rollback_exit_code -gt 0 ]; then
          echo "Rollback has failed with exit code $rollback_exit_code. Please check the logs of the rollback-if-needed step for more details."
        fi


        if [ $post_deploy_exit_code -gt 0 ]; then
          echo "Post-deploy stage has failed with exit code $post_deploy_exit_code. Please check the logs of the post-deploy step for more details."
        fi


        exit_status=$(($exit_code + $finish_evaluate_exit_code +
        $rollback_exit_code + $post_deploy_exit_code))


        if [ $exit_status -gt 0 ] || [ "$PIPELINE_STATUS" != "Succeeded" -a
        "$PIPELINE_STATUS" != "Completed" ]; then
          echo "Pipeline run failed. Please verify the status of previous stages"
          exit 1
        fi

        exit 0
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cd-rollback-change-request
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: change-request
spec:
  params:
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: next-stage
      default: ''
  results:
    - name: image
      description: Docker image to be used in the specified stage.
    - name: script
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: configmap
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: secret
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: working-dir
      description: one-pipeline working dir, the location of pipeline-config.yaml
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: dind
      description: Launch Docker-in-Docker sidecar
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: change-request-id
      description: The id of the created change request
    - name: stage-skip
      description: Whether the stage needs to skipped if possible
    - name: run-after
      description: >-
        Specify the previous stage that needs to be complete before starting
        next stage
  workspaces:
    - name: artifacts
      mountPath: /workspace/app
  volumes:
    - name: config
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
    env:
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: $(workspaces.artifacts.path)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  steps:
    - name: change-request
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: $(workspaces.artifacts.path)
      onError: continue
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
      env:
        - name: STEP_NAME
          value: change-request
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log
        export CR_ID_PATH="$(results.change-request-id.path)"
        . "${ONE_PIPELINE_PATH}/internal/cd_change_request"
    - name: prepare-next-stage
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - name: environment-properties
          mountPath: /config
      env:
        - name: STAGE
          value: $(params.next-stage)
        - name: STEP_NAME
          value: prepare-next-stage
      script: >
        #!/bin/bash


        change_request_exit_code=$(cat
        $(steps.step-change-request.exitCode.path))

        if [ "$change_request_exit_code" == "51" ]; then
          exit 1
        fi


        if [ -z "$STAGE" ]; then
          exit 0
        fi

        . "${ONE_PIPELINE_PATH}"/internal/stage/parse_config


        echo -n "${STAGE_IMAGE}" > "$(results.image.path)"

        echo -n "${STAGE_SCRIPT_PATH}" > "$(results.script.path)"

        echo -n "${STAGE_DIND}" > "$(results.dind.path)"

        echo -n "${STAGE_ABORT_ON_FAILURE}" > "$(results.abort-on-failure.path)"

        echo -n "${STAGE_IMAGE_PULL_POLICY}" >
        "$(results.image-pull-policy.path)"

        echo -n "${STAGE_CONFIGMAP}" > "$(results.configmap.path)"

        echo -n "${STAGE_SECRET}" > "$(results.secret.path)"

        echo -n "${STAGE_SKIP}" > "$(results.stage-skip.path)"

        echo -n "${STAGE_RUN_AFTER}" > "$(results.run-after.path)"
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cd-rollback-deploy
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start,rollback-change-request
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: rollback-enabled
      description: Trigger a rollback if deployment fails
      default: '0'
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
  results:
    - name: change-request-id
      description: The id of the created change request
      default: Failed
    - name: change-request-state
      description: change request status
      default: Failed
    - name: deployment-status
      description: prod deployment status
      default: Failed
    - name: acceptance-test-state
      description: acceptance test status
      default: Failed
    - name: CDDEPLOYPROPERTIES
      type: string
      default: '[]'
    - name: skip-peer-review
      description: Whether to skip peer review
      default: 'false'
    - name: skip-artifact-verification
      description: Whether to skip peer review
      default: 'false'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_VARIANT
        value: v11
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"


        # Initialize task results

        echo -n "[]" > $(results.CDDEPLOYPROPERTIES.path)

        echo -n "Failed" > "$(results.change-request-state.path)"

        echo -n "Failed" > "$(results.deployment-status.path)"

        echo -n "Failed" > "$(results.acceptance-test-state.path)"

        echo -n "false" > "$(results.skip-peer-review.path)"

        echo -n "false" > "$(results.skip-artifact-verification.path)"

        export skip_peer_review="$(results.skip-peer-review.path)"

        export
        skip_artifact_verification="$(results.skip-artifact-verification.path)"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cd-deploy steps: ${stages[*]}"

        echo "Number of steps in cd-deploy: ${#stages[@]}"


        set_env pipeline_namespace "cd"

        # start

        . "${ONE_PIPELINE_PATH}/internal/cd_start"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-cd-pipeline-config.yaml" v11 "$config_file" cd
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app)
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: dind-image
          value: $(params.dind-image)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: rollback-change-request
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-change-request
      env:
        - name: STAGE
          value: change-request
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_change_request
        - name: STAGE_ABORT_ON_FAILURE
          value: stopAndFail
        - name: STEP_NAME
          value: change-request
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-change-request
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log
        export CR_ID_PATH="$(results.change-request-id.path)"
        . "${ONE_PIPELINE_PATH}/internal/cd_change_request"
    - name: deploy
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_deploy
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: deploy
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_deploy"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: acceptance-test
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_acceptance_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: acceptance-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_acceptance_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: VERIFY_PEER_REVIEW_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: VERIFY_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: CHANGE_REQUEST_STAGE_ABORT_ON_FAILURE
          value: stopAndFail
        - name: DEPLOYMENT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: ACCEPTANCE_TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      script: >
        #!/usr/bin/bash


        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        #source "${ONE_PIPELINE_PATH}"/internal/evidence/summary_v2_helpers

        #generate_task_summary


        stages=$(/usr/local/bin/list_stage_names)

        # capture exit code of each step

        echo "checking the exit status code"

        setup_exit_code=$(cat $(steps.step-setup.exitCode.path))

        change_request_exit_code=$(cat
        $(steps.step-change-request.exitCode.path))

        if [[ "$stages" == *"deploy"* ]]; then
          deployment_exit_code=$(cat $(steps.step-deploy.exitCode.path))
        fi

        if [[ "$stages" == *"acceptance-test"* ]]; then
          acceptance_test_exit_code=$(cat $(steps.step-acceptance-test.exitCode.path))
        fi


        if [ "$change_request_exit_code" == "0" ]; then
           echo -n "Succeeded" > "$(results.change-request-state.path)"
        else
           echo -n "Failed" > "$(results.change-request-state.path)"
        fi


        if [ "$deployment_exit_code" == "0" ]; then
           echo -n "Succeeded" > "$(results.deployment-status.path)"
        else
           echo -n "Failed" > "$(results.deployment-status.path)"
        fi


        if [ "$acceptance_test_exit_code" == "0" ]; then
           echo -n "Succeeded" > "$(results.acceptance-test-state.path)"
        else
           echo -n "Failed" > "$(results.acceptance-test-state.path)"
        fi


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.CDDEPLOYPROPERTIES.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cd-rollback-finish
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: prepare,run-stage
spec:
  params:
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: deployment-status
      description: Deployment status
      default: ''
    - name: acceptance-tests-state
      description: Acceptance tests status
      default: ''
    - name: rollback-enabled
      description: Trigger a rollback if deployment fails
      default: '0'
  workspaces:
    - name: app
      description: Workspace where the application is checked out.
      mountPath: $(params.workspace-mount-path)
  volumes:
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: rollback-configmap
      configMap:
        name: $(params.configmap[1])
        optional: true
    - name: rollback-secret
      secret:
        secretName: $(params.secret[1])
        optional: true
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: toolchain
        mountPath: /toolchain
      - mountPath: /var/run/
        name: docker-socket
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: $(workspaces.app.path)
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: DEPLOYMENT_STATUS
        value: $(params.deployment-status)
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: DOCKER_TLS_CERTDIR
          value: /certs
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: $(workspaces.app.path)
      volumeMounts:
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: pipelinectl
          mountPath: /usr/share/cocoa
      env:
        - name: STEP_NAME
          value: prepare
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: rollback-if-needed
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: $(workspaces.app.path)
      onError: continue
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: rollback-configmap
          mountPath: /config/rollback-configmap
        - name: rollback-secret
          mountPath: /config/rollback-secret
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[1])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: rollback
        - name: STAGE_RUN_AFTER
          value: 'null'
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        echo "notRun" > /config/rollback-status

        echo "" > /config/rollback-exit-code

        /opt/cocoa/bin/pipelinectl set_env rollback-status "notRun"

        /opt/cocoa/bin/pipelinectl set_env rollback-exit-code ""


        echo "Deployment status: $(params.deployment-status)"

        echo "Acceptance-tests status: $(params.acceptance-tests-state)"


        if [ "$(params.rollback-enabled)" != "1" ]; then
          echo "Rollback is not enabled, to enable rollback functionality set the \"rollback-enabled\" env property to \"1\".\n This step will be skipped."
          exit 0
        fi


        rollback_triggered="false"


        if [ "$(params.acceptance-tests-state)" != "[]" ]; then
          if [ "$(params.acceptance-tests-state)" != "Succeeded" ]; then
            rollback_triggered="true"
          fi
        else
          echo "Acceptance tests has not reported any status, the task might be skipped. Checking deployment status next."
        fi


        if [ "$(params.deployment-status)" != "Succeeded" ]; then
          rollback_triggered="true"
        fi


        if [ "$rollback_triggered" != "true" ]; then
          echo "*********    NO ROLLBACK EXECUTED    *********"
          echo "This step will now passthrough."
          exit 0
        fi


        echo "Triggering rollback script."

        export STAGE_SCRIPT_PATH="stage_script_runner_rollback_if_needed"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_run_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0)
        }' | sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_run_stage}"_EXIT_CODE)

        if [ $exit_code -eq 0 ]; then
          echo "success" > /config/rollback-status
          /opt/cocoa/bin/pipelinectl set_env rollback-status "success"
        else
          echo "failure" > /config/rollback-status
          /opt/cocoa/bin/pipelinectl set_env rollback-status "failure"
        fi

        echo "$exit_code" > /config/rollback-exit-code

        /opt/cocoa/bin/pipelinectl set_env rollback-exit-code "$exit_code"

        exit $exit_code
    - name: evaluate
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: $(workspaces.app.path)
      onError: continue
      env:
        - name: STEP_NAME
          value: evaluate
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        rollback_status=$(get_env rollback-status)
        rollback_exit_code=$(get_env rollback-exit-code)

        pipeline_execution_status="successful_deployment"
        if [ $rollback_status != "notRun" ]; then
          if [ $rollback_status = "failure" ]; then
            pipeline_execution_status="failed_deployment_failed_rollback"
          else
            pipeline_execution_status="failed_deployment_successful_rollback"
          fi
        fi

        set_env pipeline-execution-status "${pipeline_execution_status}"

        . "${ONE_PIPELINE_PATH}/internal/cd_finish"
    - name: run-stage
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: $(workspaces.app.path)
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: configmap
          mountPath: /config/configmap
        - name: secret
          mountPath: /config/secret
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_RUN_AFTER
          value: 'null'
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export finish_evaluate_exit_code

        export rollback_exit_code

        finish_evaluate_exit_code=$(cat $(steps.step-evaluate.exitCode.path))

        rollback_exit_code=$(/opt/cocoa/bin/pipelinectl get_env
        rollback-exit-code "0")


        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?


        exit_status=$(($exit_code + $finish_evaluate_exit_code +
        $rollback_exit_code))


        if [ $exit_status -gt 0 ] || [ "$PIPELINE_STATUS" != "Succeeded" -a
        "$PIPELINE_STATUS" != "Completed" ]; then
          echo "Pipeline run failed. Please verify the status of previous stages"
          exit 1
        fi

        exit 0
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: cd-start
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: start
spec:
  params:
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: next-stages
      type: array
      default:
        - ''
  results:
    - name: image
      type: array
      description: Docker image to be used in the specified stage.
    - name: script
      type: array
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: configmap
      type: array
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: secret
      type: array
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: working-dir
      description: one-pipeline working dir, the location of pipeline-config.yaml
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: stage-skip
      type: array
      description: Whether the stage needs to skipped if possible
    - name: run-after
      type: array
      description: >-
        Specify the previous stage that needs to be complete before starting
        next stage
  volumes:
    - name: config
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: COMPLIANCE_BASEIMAGE
        value: $(params.compliance-baseimage)
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      onError: continue
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
      env:
        - name: STEP_NAME
          value: start
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log
        export PATH="$PATH:/opt/cocoa/bin"
        set_env pipeline_namespace "cd"
        . "${ONE_PIPELINE_PATH}/internal/pipeline_start"
        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils
        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: ci-finish
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: evaluate,prepare
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: deploy-dev-state
      description: Status of deployment to dev cluster status
      default: ''
    - name: REPOS
      description: Status of all the scans done in deploy-checks
      default: '[]'
    - name: CODECHECKSTATUS
      type: array
      description: Status of all the scans done in code-checks
      default: []
    - name: BUILDARTIFACTS
      type: array
      description: all artifacts from the code-build
      default: []
    - name: start-exitcode
      default: '0'
  volumes:
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: configmap
        mountPath: /config/$(params.configmap)
      - name: secret
        mountPath: /config/$(params.secret)
      - name: toolchain
        mountPath: /toolchain
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: DEPLOY_DEV_STATUS
        value: $(params.deploy-dev-state)
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 30G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: evaluate
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      onError: continue
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: evaluate
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      args:
        - $(params.CODECHECKSTATUS[*])
        - $(params.BUILDARTIFACTS[*])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
          cleanup_data_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          # loop through all the arguments - CODECHECKSTATUS, ARTIFACTS
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done

          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        set_env STAGE_TEST_STATUS "$(get_env STAGE_UNIT_TEST_STATUS)"


        . "${ONE_PIPELINE_PATH}/internal/task_start"


        # evaluate

        . "${ONE_PIPELINE_PATH}/internal/ci_finish"
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: tmp
          mountPath: /tmp
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
      script: >
        #!/bin/bash


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        echo "Preparing custom finish step"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: run-stage
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: tmp
          mountPath: /tmp
      env:
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        finish_evaluate_exit_code=$(cat $(steps.step-evaluate.exitCode.path))

        export finish_evaluate_exit_code

        export STAGE_SCRIPT_PATH="stage_script_runner_run_stage"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?

        if [ $finish_evaluate_exit_code -ne 0 ]; then
          echo "Failing the finish stage as \"evaluate\" step failed with error code $finish_evaluate_exit_code"
        fi

        if [ "$PIPELINE_STATUS" != "Succeeded" -a "$PIPELINE_STATUS" !=
        "Completed" ]; then
          echo "Pipeline run failed. Please verify the status of previous stages"
          exit 1
        fi

        exit $(($exit_code + $finish_evaluate_exit_code))
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: ci-start
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: start
spec:
  params:
    - name: branch
      description: branch
      default: ''
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: next-stage
      default: ''
    - name: final-stage
      default: ''
  results:
    - name: image
      description: Docker image to be used in the specified stage.
    - name: script
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: configmap
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: secret
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: working-dir
      description: one-pipeline working dir, the location of pipeline-config.yaml
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: dind
      description: Launch Docker-in-Docker sidecar
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: stage-skip
      description: Whether the stage needs to skipped if possible
    - name: final-stage-image
      description: Docker image to be used in the specified stage.
    - name: final-stage-script
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: final-stage-configmap
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: final-stage-secret
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: final-stage-abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: final-stage-dind
      description: Launch Docker-in-Docker sidecar
    - name: final-stage-image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: final-stage-skip
      description: Whether the stage needs to skipped if possible
    - name: run-after
      description: >-
        Specify the previous stage that needs to be complete before starting
        next stage
    - name: REPOS
      type: string
      default: '[]'
    - name: task-exit-code
      description: Initialize to kill the pipeline and and when success reset
  volumes:
    - name: config
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: COMPLIANCE_BASEIMAGE
        value: $(params.compliance-baseimage)
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      onError: continue
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        echo -n "[]" > $(results.REPOS.path)

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        BRANCH=${BRANCH#"refs/tags/"}

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        # Capture the exit code and send the exit code to task result

        if "${ONE_PIPELINE_PATH}/internal/ci_start"; then
          start_exit_code=$?
          echo -n "${start_exit_code}" > $(results.task-exit-code.path)
        else
          start_exit_code=$?
          echo -n "${start_exit_code}" > $(results.task-exit-code.path)
        fi


        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          set_env fatal_error "true"
          exit ${start_exit_code}
        fi


        echo "serialize all the repos and env params here..."

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.REPOS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true

        exit ${exit_code}
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: code-build
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      default: '0'
  results:
    - name: IMAGES
      type: string
      default: ''
    - name: ARTIFACTS
      type: string
      default: '[]'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: docker-socket
        mountPath: /var/run/
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 30G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
        - name: start_exit_code
          value: $(params.start-exitcode)
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "code-build steps: ${stages[*]}"

        echo "Number of steps in code-build: ${#stages[@]}"


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i

          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: unit-test
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: unit-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_unit_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: build-artifact
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_build-artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: build-artifact
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_build_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$STAGE" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: sign-artifact
      image: $(params.image[3])
      imagePullPolicy: $(params.image-pull-policy[3])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[3])
        - name: secret
          mountPath: /config/$(params.secret[3])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[3])
      env:
        - name: STAGE
          value: $(params.stage[3])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_sign_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STEP_NAME
          value: sign-artifact
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[3])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_sign_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: scan-artifact
      image: $(params.image[4])
      imagePullPolicy: $(params.image-pull-policy[4])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[4])
        - name: secret
          mountPath: /config/$(params.secret[4])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_scan_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: scan-artifact
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_scan_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/no-configmap-provided
        - name: secret
          mountPath: /config/no-secret-provided
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: CONTAINERIZE_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: SIGN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: SCAN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: ON_ERROR
          value: ''
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      script: >
        #!/usr/bin/bash

        export PATH="$PATH:/opt/cocoa/bin"


        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: code-checks
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      default: '0'
  results:
    - name: CODECHECKSTATUS
      type: string
      default: '[]'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 30G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
        - name: start_exit_code
          value: $(params.start-exitcode)
      args:
        - $(params.stage[*])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.CODECHECKSTATUS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "code-check steps: ${stages[*]}""

        echo "Number of steps in code-check: ${#stages[@]}""


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: checks-setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: checks-setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_checks_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: detect-secrets
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_detect_secrets
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: detect-secrets
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_detect_secrets"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: compliance-checks
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_compliance_checks
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: compliance-checks
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_compliance_checks"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: peer-review
      image: $(params.image[3])
      imagePullPolicy: $(params.image-pull-policy[3])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[3])
        - name: secret
          mountPath: /config/$(params.secret[3])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[3])
      env:
        - name: STAGE
          value: $(params.stage[3])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_peer_review
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STEP_NAME
          value: peer-review
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[3])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_peer_review"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: static-scan
      image: $(params.image[4])
      imagePullPolicy: $(params.image-pull-policy[4])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[4])
        - name: secret
          mountPath: /config/$(params.secret[4])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_static_scan
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: static-scan
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_static_scan"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/no-configmap-provided
        - name: secret
          mountPath: /config/no-secret-provided
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: DETECT_SECRETS_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: COMPLIANCE_CHECKS_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: PEER_REVIEW_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STATIC_SCAN_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: ON_ERROR
          value: ''
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      script: >
        #!/usr/bin/bash


        export PATH="$PATH:/opt/cocoa/bin"

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.CODECHECKSTATUS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: concert-aggregator
spec:
  params:
    - name: stage
      description: Stage in the config file
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: toolchain
        mountPath: /toolchain
      - name: docker-socket
        mountPath: /var/run/
      - name: dind-certs
        mountPath: /certs/client
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
      - name: tmp
        mountPath: /tmp
      - name: pipeline-scripts
        mountPath: /usr/share/one-pipeline
      - name: commons-library
        mountPath: /usr/share/commons
      - name: pipelinectl
        mountPath: /opt/cocoa
    env:
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: WORKSPACE
        value: /workspace/app
      - name: COMMONS_PATH
        value: /opt/commons
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: STAGE_WORKSPACE
        value: /stage-$(params.stage)
      - name: STAGE
        value: $(params.stage)
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: PIPELINE_VARIANT
        value: v11
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
  steps:
    - name: static-sbom-generate-upload
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STEP_NAME
          value: static-sbom-generate-upload
      script: >
        #!/bin/bash

        echo "::notice::concert-aggregator task is retired. Now App-SBOM uploads
        in CI pipeline. This step is a no-op."

        exit 0
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: core-change-request
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: change-request
spec:
  params:
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: pipeline-name
      description: Name of the pipeline
      default: operational-pipeline
  results:
    - name: change-request-id
      description: ID of the change request
      type: string
      default: notAvailable
    - name: manual-change-request
      description: Determine if a manual change request is being processed
      type: string
      default: 'false'
  volumes:
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: toolchain
        mountPath: /toolchain
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: STAGE
        value: operational-change-request
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        value: $(params.pipeline-name)
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  steps:
    - name: change-request
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      onError: stopAndFail
      env:
        - name: STEP_NAME
          value: change-request
      script: |
        #!/bin/bash

        echo -n "notAvailable" > $(results.change-request-id.path)

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        . "${ONE_PIPELINE_PATH}/internal/ops_change_request_driver"

        echo -n "$CHANGE_REQUEST_ID" > $(results.change-request-id.path)
        echo -n "$MANUAL_CHANGE_REQUEST" > $(results.manual-change-request.path)
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: custom-finish-stage
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: prepare
spec:
  params:
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: next-stage
      default: ''
  results:
    - name: image
      description: Docker image to be used in the specified stage.
    - name: script
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: configmap
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: secret
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: working-dir
      description: one-pipeline working dir, the location of pipeline-config.yaml
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: dind
      description: Launch Docker-in-Docker sidecar
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: IMAGES
      type: string
      default: ''
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: $(params.dind)
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - mountPath: /var/run/
          name: docker-socket
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - mountPath: /var/run/
          name: docker-socket
        - name: tmp
          mountPath: /tmp
        - name: toolchain
          mountPath: /toolchain
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        . "${ONE_PIPELINE_PATH}/internal/task_start"


        echo "Preparing custom finish step"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: finish
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: toolchain
          mountPath: /toolchain
        - mountPath: /var/run/
          name: docker-socket
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STAGE_SCRIPT_PATH
          value: $(params.script)
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: PIPELINE_RUN_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.labels['tekton.dev/pipelineRun']
        - name: PIPELINE_RUN_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
        - name: PIPELINE_RUN_URL
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
        - name: BUILD_NUMBER
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
        - name: PIPELINE_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
        - name: TRIGGER_TYPE
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
        - name: TRIGGER_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
        - name: TRIGGERED_BY
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_finish"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$(params.stage)" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: deploy-checks
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: ARTIFACTS
      type: array
      description: all artifacts from the code-build
      default: []
    - name: REPOS
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      default: '0'
  results:
    - name: IMAGES
      type: string
      default: ''
    - name: ARTIFACTS
      type: string
      default: '[]'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 30G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
        - name: start_exit_code
          value: $(params.start-exitcode)
      args:
        - $(params.ARTIFACTS[*])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi

        export PATH="$PATH:/opt/cocoa/bin"

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)


        echo "code-check steps: ${stages[*]}"

        echo "Number of steps in code-check: ${#stages[@]}"


        # start


        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: deploy
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_deploy
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_deploy"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: dynamic-scan
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_dynamic_scan
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: dynamic-scan
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_dynamic_scan"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: acceptance-test
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_acceptance_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: acceptance-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_acceptance_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: collect-evidence
        - name: DEPLOY_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: DYNAMIC_SCAN_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: ACCEPTANCE_TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: ON_ERROR
          value: ''
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      script: >
        #!/usr/bin/bash


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          pipelinectl serialize --all-artifacts --all-deployments STAGE_ACCEPTANCE_TEST_STATUS one-pipeline-status > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: deploy-release
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: prepare,collect-evidence
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      description: Stage in the config file
      default: run-stage
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: ARTIFACTS
      type: array
      description: all artifacts from the code-build
      default: []
    - name: REPOS
      description: all repos from the ci-start
      default: '[]'
    - name: DEPLOY_ARTIFACTS
      description: all artifacts from the deploy-checks
      default: '[]'
    - name: CODECHECKSTATUS
      description: status of code-checks
      default: '[]'
    - name: start-exitcode
      default: '0'
  results:
    - name: DEPLOYARTIFACTS
      type: string
      default: '[]'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: docker-socket
        mountPath: /var/run/
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: $(params.dind)
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 30G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: toolchain
          mountPath: /toolchain
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
        - name: start_exit_code
          value: $(params.start-exitcode)
      args:
        - $(params.ARTIFACTS[*])
      script: >
        #!/usr/bin/env bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.DEPLOYARTIFACTS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi

        export PATH="$PATH:/opt/cocoa/bin"

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if [ "$(get_env pipeline-error-status "")" == "failed" ]; then
          exit 1
        fi


        # start

        onepl_status=0

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
          onepl_status=$(get_env one-pipeline-status 0)
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          # loop through all the arguments - ARTIFACTS
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done

          if [ "$(params.DEPLOY_ARTIFACTS)" != "[]" ]; then
            echo -n "$(params.DEPLOY_ARTIFACTS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
            deploy_artifacts_status=$(get_env one-pipeline-status 0)
            if [[ deploy_artifacts_status -ne 0 ]]; then
              onepl_status=1
            fi
          fi
          if [ "$(params.CODECHECKSTATUS)" != "[]" ]; then
            echo -n "$(params.CODECHECKSTATUS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
            codecheck_status=$(get_env one-pipeline-status 0)
            if [[ codecheck_status -eq 1 ]]; then
              onepl_status=1
            fi
          fi
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi


        set_env one-pipeline-status ${onepl_status}


        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        . "${ONE_PIPELINE_PATH}/internal/task_start"


        # prepare

        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cc-deployment-scan steps: ${stages[*]}"

        echo "Number of steps in cc-deployment-scan: ${#stages[@]}"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: run-stage
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_RUN_AFTER
          value: $(params.run-after)
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export PATH="$PATH:/opt/cocoa/bin"

        export STAGE_SCRIPT_PATH="stage_script_runner_run_stage"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit ${exit_code}
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      env:
        - name: STAGE
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      script: >
        #!/usr/bin/bash


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          pipelinectl serialize --all-artifacts one-pipeline-status  > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.DEPLOYARTIFACTS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: dev-mode-cd-deploy
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: rollback-enabled
      description: Trigger a rollback if deployment fails
      default: '0'
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
  results:
    - name: deployment-status
      description: prod deployment status
      default: Failed
    - name: acceptance-test-state
      description: acceptance test status
      default: Failed
    - name: CDDEPLOYPROPERTIES
      type: string
      default: '[]'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"


        # Initialize task results

        echo -n "[]" > $(results.CDDEPLOYPROPERTIES.path)

        echo -n "Failed" > "$(results.deployment-status.path)"

        echo -n "Failed" > "$(results.acceptance-test-state.path)"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cd-deploy steps: ${stages[*]}"

        echo "Number of steps in cd-deploy: ${#stages[@]}"


        set_env pipeline_namespace "cd"

        # start

        . "${ONE_PIPELINE_PATH}/internal/cd_start"


        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-cd-pipeline-config.yaml" v11 "$config_file" cd
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app)
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: dind-image
          value: $(params.dind-image)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: deploy
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_deploy
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: deploy
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_deploy"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: acceptance-test
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_acceptance_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: acceptance-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_acceptance_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: DEPLOYMENT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: ACCEPTANCE_TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: finalize
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/usr/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        stages=$(/usr/local/bin/list_stage_names)

        # capture exit code of each step


        setup_exit_code=$(cat $(steps.step-setup.exitCode.path))

        if [[ "$stages" == *"deploy"* ]]; then
          deployment_exit_code=$(cat $(steps.step-deploy.exitCode.path))
        fi

        if [[ "$stages" == *"acceptance-test"* ]]; then
          acceptance_test_exit_code=$(cat $(steps.step-acceptance-test.exitCode.path))
        fi


        if [ "$deployment_exit_code" == "0" ]; then
          echo -n "Succeeded" > "$(results.deployment-status.path)"
        else
          echo -n "Failed" > "$(results.deployment-status.path)"
        fi


        if [ "$acceptance_test_exit_code" == "0" ]; then
          echo -n "Succeeded" > "$(results.acceptance-test-state.path)"
        else
          echo -n "Failed" > "$(results.acceptance-test-state.path)"
        fi


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.CDDEPLOYPROPERTIES.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: dev-mode-cd-finish
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: prepare,evaluate
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
      default: ''
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
      default: ''
    - name: pr-url
      description: PR url
      default: ''
    - name: repository-integration
      description: the repo integration name
      default: ''
    - name: repository
      description: app repository owner and name
      default: ''
    - name: stage
      description: Stage in the config file
      type: array
      default:
        - ''
        - ''
    - name: image
      description: Docker image to be used to run the stage script.
      type: array
    - name: script
      description: Location of the script that is used to run the stage.
      type: array
    - name: dind
      description: Launch Docker-in-Docker sidecar
      type: array
      default:
        - 'false'
        - 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
      type: array
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      type: array
      default:
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: CDDEPLOYPROPERTIES
      type: array
      description: Status of all properties set in serialize of cd-deploy stage
      default: []
    - name: REPOS
      description: repos list created in the start
      default: '[]'
  volumes:
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: configmap
        mountPath: /config/$(params.configmap)
      - name: secret
        mountPath: /config/$(params.secret)
      - name: toolchain
        mountPath: /toolchain
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: tmp
          mountPath: /tmp
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
      args:
        - $(params.CDDEPLOYPROPERTIES[*])
      script: >
        #!/bin/bash

        LOGFILE="/tmp/logs/$TASK_NAME/$STEP_NAME"

        mkdir -p "$(dirname "$LOGFILE")" && touch "$LOGFILE"

        chmod 644 "$LOGFILE"

        (

        trap 'printf "%s" "$?" > /tmp/sub.$$' EXIT

        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()

        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done

        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done

        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"

        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
          cleanup_data_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          # loop through all the arguments - CDDEPLOYPROPERTIES
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done

          # Notice we also need to pull in REPOS if applicable (ensure params.REPOS is defined in the task params)
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi


        . "${ONE_PIPELINE_PATH}/internal/cd_start"

        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-cd-pipeline-config.yaml" v11 "$config_file" cd
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}
          stage_script=$stage_script
          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done

        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare

        ) 2>&1 | tee "$LOGFILE"

        exit $(cat "/tmp/sub.$$")
    - name: evaluate
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      script: |
        #!/bin/bash
        export PATH="$PATH:/opt/cocoa/bin"
        source "${ONE_PIPELINE_PATH}/internal/devmode_cd_finish"
    - name: post-deploy
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: tmp
          mountPath: /tmp
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: post-deploy
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        LOGFILE="/tmp/logs/$TASK_NAME/$STEP_NAME"

        mkdir -p "$(dirname "$LOGFILE")" && touch "$LOGFILE"

        chmod 644 "$LOGFILE"

        (

        trap 'printf "%s" "$?" > /tmp/sub.$$' EXIT

        export STAGE_SCRIPT_PATH="stage_script_runner_post_deploy"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code

        ) 2>&1 | tee "$LOGFILE"

        exit $(cat "/tmp/sub.$$")
    - name: finish
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: tmp
          mountPath: /tmp
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[1])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: finish
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        finish_evaluate_exit_code=$(cat $(steps.step-evaluate.exitCode.path))

        finish_post_deploy_exit_code=$(cat
        $(steps.step-post-deploy.exitCode.path))

        export finish_post_deploy_exit_code

        export finish_evaluate_exit_code


        export STAGE_SCRIPT_PATH="stage_script_runner_finish"


        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?


        export PIPELINE_NAME="dev-mode-cd-pipeline"

        export PIPELINE_STATUS="${PIPELINE_STATUS:-Failed}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then

          PROD_SETUP_EXIT_CODE=$(get_env STAGE_SETUP_EXIT_CODE "1")
          DEPLOYMENT_EXIT_CODE=$(get_env STAGE_DEPLOY_EXIT_CODE "1")
          ACCEPTANCE_TEST_EXIT_CODE=$(get_env STAGE_ACCEPTANCE_TEST_EXIT_CODE "1")

          if [ "$PROD_SETUP_EXIT_CODE" -eq 0 ]; then
            export PROD_SETUP_STATUS="Succeeded"
          else
            export PROD_SETUP_STATUS="Failed"
          fi

          if [ "$DEPLOYMENT_EXIT_CODE" -eq 0 ]; then
            export DEPLOYMENT_STATUS="Succeeded"
          else
            export DEPLOYMENT_STATUS="Failed"
          fi

          if [ "$ACCEPTANCE_TEST_EXIT_CODE" -eq 0 ]; then
            export ACCEPTANCE_TEST_STATUS="Succeeded"
          else
            export ACCEPTANCE_TEST_STATUS="Failed"
          fi

          "${COMMONS_PATH}/communications/post_message" "dev_mode_cd_pipeline_end"
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data to get the slack notification"
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $(($exit_code + $finish_post_deploy_exit_code +
        $finish_evaluate_exit_code))
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: dev-mode-ci-finish
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: prepare
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
      default: ''
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
      default: ''
    - name: pr-url
      description: PR url
      default: ''
    - name: repository-integration
      description: the repo integration name
      default: ''
    - name: repository
      description: app repository owner and name
      default: ''
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: CDDEPLOYPROPERTIES
      type: array
      description: Status of all properties set in serialize of cd-deploy stage
      default: []
    - name: REPOS
      description: repos list created in the start
      default: '[]'
    - name: start-exitcode
      default: '0'
  volumes:
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: configmap
        mountPath: /config/$(params.configmap)
      - name: secret
        mountPath: /config/$(params.secret)
      - name: toolchain
        mountPath: /toolchain
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: tmp
          mountPath: /tmp
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      args:
        - $(params.CDDEPLOYPROPERTIES[*])
      script: >
        #!/bin/bash


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi

        export PATH="$PATH:/opt/cocoa/bin"

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
          cleanup_data_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          # loop through all the arguments - CDDEPLOYPROPERTIES
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        echo "Preparing custom finish step"


        pipeline_name=$(get_env pipeline_name "${PIPELINE_NAME:-""}")

        if [[ "$pipeline_name" == *"dev-mode-cd-pipeline"* ]]; then
          . "${ONE_PIPELINE_PATH}/internal/cd_start"
          default_pipeline_config_file="${ONE_PIPELINE_PATH}/polyglot/default-cd-pipeline-config.yaml"
          v11_pipeline_shape="cd"
        else
          . "${ONE_PIPELINE_PATH}/internal/task_start"
          # define the config_file variable that is not prepared in internal/task_start
          config_file_name="$(get_env one-pipeline-config)"
          config_file="${WORKSPACE}/$(get_env ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"
          default_pipeline_config_file="${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml"
          v11_pipeline_shape="ci"
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cc-deployment-scan steps: ${stages[*]}"

        echo "Number of steps in cc-deployment-scan: ${#stages[@]}"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "$default_pipeline_config_file" v11 "$config_file" "$v11_pipeline_shape"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: run-stage
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: tmp
          mountPath: /tmp
      env:
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_run_stage"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        export PIPELINE_NAME="dev-mode-pipeline"

        export PIPELINE_STATUS="${PIPELINE_STATUS:-Failed}"

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          START_EXITCODE=$(get_env STAGE_SETUP_EXIT_CODE "1")
          UNIT_TEST_EXIT_CODE=$(get_env STAGE_UNIT_TEST_EXIT_CODE "1")
          BUILD_ARTIFACT_EXIT_CODE=$(get_env STAGE_BUILD_ARTIFACT_EXIT_CODE "1")
          DEPLOY_EXIT_CODE=$(get_env STAGE_DEPLOY_EXIT_CODE "1")
          ACCEPTANCE_TEST_EXIT_CODE=$(get_env STAGE_ACCEPTANCE_TEST_EXIT_CODE "1")
          RELEASE_EXIT_CODE=$(get_env STAGE_RUN_STAGE_EXIT_CODE "1")

          if [ "$START_EXITCODE" -eq 0 ]; then
            export CODE_CI_START_STATUS="Succeeded"
          else
            export CODE_CI_START_STATUS="Failed"
          fi

          if [ "$UNIT_TEST_EXIT_CODE" -eq 0 ]; then
            export UNIT_TESTS_STATUS="Succeeded"
          else
            export UNIT_TESTS_STATUS="Failed"
          fi

          if [ "$BUILD_ARTIFACT_EXIT_CODE" -eq 0 ]; then
            export CONTAINERIZE_STATUS="Succeeded"
          else
            export CONTAINERIZE_STATUS="Failed"
          fi

          if [ "$DEPLOY_EXIT_CODE" -eq 0 ]; then
            export DEPLOY_DEV_STATUS="Succeeded"
          else
            export DEPLOY_DEV_STATUS="Failed"
          fi

          if [ "$ACCEPTANCE_TEST_EXIT_CODE" -eq 0 ]; then
            export DEPLOY_ACCEPTANCE_TESTS_STATUS="Succeeded"
          else
            export DEPLOY_ACCEPTANCE_TESTS_STATUS="Failed"
          fi

          if [ "$RELEASE_EXIT_CODE" -eq 0 ]; then
            export DEPLOY_RELEASE_STATUS="Succeeded"
          else
            export DEPLOY_RELEASE_STATUS="Failed"
          fi

          "${COMMONS_PATH}/communications/post_message" "dev_mode_pipeline_end"
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data to get the slack notification"
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: devmode-code-build
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      default: '0'
  results:
    - name: IMAGES
      type: string
      default: ''
    - name: ARTIFACTS
      type: string
      default: '[]'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: docker-socket
        mountPath: /var/run/
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 30G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
        - name: start_exit_code
          value: $(params.start-exitcode)
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi

        export PATH="$PATH:/opt/cocoa/bin"

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "dev-mode code-build steps: ${stages[*]}"

        echo "Number of steps in dev-mode code-build: ${#stages[@]}"


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: unit-test
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: unit-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_unit_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: build-artifact
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_build_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: build-artifact
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_build_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$STAGE" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/no-configmap-provided
        - name: secret
          mountPath: /config/no-secret-provided
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: CONTAINERIZE_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      script: >
        #!/usr/bin/bash


        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: devmode-deploy-checks
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: ARTIFACTS
      type: array
      description: all artifacts from the code-build
      default: []
    - name: REPOS
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      default: '0'
  results:
    - name: IMAGES
      type: string
      default: ''
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 30G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
        - name: start_exit_code
          value: $(params.start-exitcode)
      args:
        - $(params.ARTIFACTS[*])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi

        export PATH="$PATH:/opt/cocoa/bin"

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done

          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        echo "dev-mode deploy-check steps: ${stages[*]}"

        echo "Number of steps in dev-mode deploy-check: ${#stages[@]}"


        # start


        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: deploy
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_deploy
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_deploy"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: acceptance-test
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_acceptance_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: acceptance-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_acceptance_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: collect-evidence
        - name: DEPLOY_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: ACCEPTANCE_TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      script: >
        #!/usr/bin/bash


        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        fi
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: devmode-deploy-release
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: prepare,collect-evidence
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: branch
      description: branch
      default: master
    - name: commit-id
      description: commit id
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: pr-url
      description: PR url
    - name: repository-integration
      description: the repo integration name
    - name: repository
      description: app repository owner and name
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: ARTIFACTS
      type: array
      description: all artifacts from the code-build
      default: []
    - name: REPOS
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      default: '0'
  results:
    - name: IMAGES
      type: string
      default: ''
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: docker-socket
        mountPath: /var/run/
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: $(params.dind)
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 30G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: toolchain
          mountPath: /toolchain
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      env:
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
        - name: start_exit_code
          value: $(params.start-exitcode)
      args:
        - $(params.ARTIFACTS[*])
      script: >
        #!/usr/bin/env bash


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if [ "$(get_env pipeline-error-status "")" == "failed" ]; then
          exit 1
        fi


        # start


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          # loop through all the arguments - ARTIFACTS
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done

          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cc-deployment-scan steps: ${stages[*]}"

        echo "Number of steps in cc-deployment-scan: ${#stages[@]}"


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: run-stage
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_RUN_AFTER
          value: $(params.run-after)
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_run_stage"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$(params.stage)" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STAGE
          value: collect-evidence
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: STEP_NAME
          value: collect-evidence
        - name: ON_ERROR
          value: ''
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      script: >
        #!/usr/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        fi
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: inventory-promotion
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: promotion,prepare
spec:
  params:
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: priority
      description: priority of the change request
    - name: assignee
      description: >-
        functional id or an email who will be assigned to the change request in
        service now
  results:
    - name: IMAGES
      type: string
      default: ''
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: STAGE
        value: $(params.stage)
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: $(params.dind)
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: promotion
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: toolchain
          mountPath: /toolchain
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: promotion
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
      script: |
        #!/usr/bin/env bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi
        if [ "$(get_env pipeline-error-status "")" == "failed" ]; then
          exit 1
        fi

        export PATH="$PATH:/opt/cocoa/bin"

        if [ -n "${PRIORITY}" ]; then
          echo -n "${PRIORITY}" >> /config/priority
          set_env priority "${PRIORITY}"
        fi

        if [ -n "${ASSIGNEE}" ]; then
          echo -n "${ASSIGNEE}" >> /config/assignee
          set_env assignee "${ASSIGNEE}"
        fi

        . "${ONE_PIPELINE_PATH}/internal/promotion"
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - mountPath: /var/run/
          name: docker-socket
        - name: tmp
          mountPath: /tmp
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
      script: >
        #!/bin/bash


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        export PATH="$PATH:/opt/cocoa/bin"

        echo "Preparing custom finish step"

        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cc-deployment-scan steps: ${stages[*]}"

        echo "Number of steps in cc-deployment-scan: ${#stages[@]}"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-cd-pipeline-config.yaml" v11 "$config_file" cd
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: finish
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: toolchain
          mountPath: /toolchain
        - mountPath: /var/run/
          name: docker-socket
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STAGE_SCRIPT_PATH
          value: $(params.script)
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: STEP_NAME
          value: finish
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_finish"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$(params.stage)" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: operational-execute
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: start
spec:
  params:
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: change-request-id
      description: The ID of the change request to close
      default: notAvailable
  results:
    - name: operational-execute-exit-code
      description: Exit code of execute step
      type: string
      default: '1'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
      - name: toolchain
        mountPath: /toolchain
      - name: tmp
        mountPath: /tmp
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: $(params.dind)
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: dind-certs
          mountPath: /certs/client
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
      script: >
        #!/bin/bash


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        export NEXT_STAGE="execute"


        . "${ONE_PIPELINE_PATH}/internal/simple_pipeline_start"

        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare


        set_env ONE_PIPELINE_REPO "$(get_env pipeline-config-repo "")"

        set_env ONE_PIPELINE_CONFIG_COMMIT "$(get_env GIT_COMMIT "")"

        set_env pipeline-namespace "simple-executor"

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        # fetch the SCRIPT with counter and inderect reference

        SCRIPT="$(params.script)"

        # Replace the - with _for the standard naming convention

        stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}"
        | tr '-' '_')"

        echo "stage_script runner file ${stage_script_runner}"

        stage_script="/tmp/stage-script-${STAGE}"

        echo "stage_script file ${stage_script}"

        touch $stage_script

        if [ ! -f ${stage_script_runner} ]; then
          echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
          echo \#\!/bin/bash > "${stage_script_runner}"
          echo >> "${stage_script_runner}"
          echo "${stage_script}" >> "${stage_script_runner}"
        fi

        cat ${stage_script_runner}

        chmod +x ${stage_script_runner}


        stage_script=$stage_script

        script="script"


        generate_user_script "${stage_script}" "${script}" "${STAGE}"
        "${TASK_NAME}"

        chmod +x ${stage_script}
    - name: execute
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      onError: $(params.abort-on-failure)
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STAGE_SCRIPT_PATH
          value: $(params.script)
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: PIPELINE_RUN_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.labels['tekton.dev/pipelineRun']
        - name: PIPELINE_RUN_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
        - name: PIPELINE_RUN_URL
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
        - name: BUILD_NUMBER
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
        - name: TRIGGER_TYPE
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
        - name: TRIGGER_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
        - name: TRIGGERED_BY
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
        - name: STEP_NAME
          value: execute
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage)
      script: >
        #!/bin/bash

        echo -n "notAvailable" > $(results.operational-execute-exit-code.path)


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        export STAGE_SCRIPT_PATH="stage_script_runner_$(echo "${STAGE}" | tr '-'
        '_')"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_run_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0)
        }' | sed 's/-/_/g')"

        run_stage_exit_code=$(get_env STAGE_"${current_run_stage}"_EXIT_CODE)


        if [ -n "$(params.change-request-id)" ]; then
          export CHANGE_REQUEST_ID="$(params.change-request-id)"
          set_env CHANGE_REQUEST_ID "$CHANGE_REQUEST_ID"
        fi


        . "${ONE_PIPELINE_PATH}/internal/ops_attach_execution_log"


        echo -n "$run_stage_exit_code" >
        $(results.operational-execute-exit-code.path)


        exit $run_stage_exit_code
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: operational-finish
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: finish
spec:
  params:
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: pipeline-name
      description: The name of the running pipeline
      default: operational-pipeline
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: manual-change-request
      description: Determine if a manual change request is being processed
      default: 'false'
    - name: change-request-id
      description: The ID of the change request to close
      default: notAvailable
    - name: operational-execute-exit-code
      description: Exit code of execute step
      type: array
      default: []
  volumes:
    - name: docker-socket
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: toolchain
        mountPath: /toolchain
      - name: workspace
        mountPath: /workspace/app
      - name: tmp
        mountPath: /tmp
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
  steps:
    - name: finish
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: finish
      args:
        - $(params.operational-execute-exit-code[*])
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export PATH="$PATH:/opt/cocoa/bin"

        set_env pipeline-status "$(params.pipeline-status)"

        stage_exit_code="0"
        for arg in "$@"; do
          if [  "$arg" == "notAvailable" ]; then
            stage_exit_code="notAvailable"
            break
          fi
          if [  "$arg" != "0" ]; then
            stage_exit_code="1"
          fi
        done

        export OPERATIONAL_EXECUTE_EXIT_CODE="$stage_exit_code"
        set_env OPERATIONAL_EXECUTE_EXIT_CODE "$stage_exit_code"

        if [ -n "$(params.change-request-id)" ]; then
          export CHANGE_REQUEST_ID="$(params.change-request-id)"
          set_env CHANGE_REQUEST_ID "$CHANGE_REQUEST_ID"
        fi

        if [ -n "$(params.manual-change-request)" ]; then
          export MANUAL_CHANGE_REQUEST="$(params.manual-change-request)"
          set_env MANUAL_CHANGE_REQUEST "$MANUAL_CHANGE_REQUEST"
        fi

        . "${ONE_PIPELINE_PATH}/internal/ops_finish"
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: pr-code-checks
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/finish-step: collect-evidence
spec:
  params:
    - name: head-branch
      value: $(params.head-branch)
    - name: head-sha
      value: $(params.head-sha)
    - name: head-repo
      value: $(params.head-repo)
    - name: base-branch
      value: $(params.base-branch)
    - name: base-repo
      value: $(params.base-repo)
    - name: base-repo-name
      value: $(params.base-repo-name)
    - name: base-repo-owner
      value: $(params.base-repo-owner)
    - name: commit-timestamp
      value: $(params.commit-timestamp)
    - name: pr-url
      value: $(params.pr-url)
    - name: pr-html-url
      value: $(params.pr-html-url)
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: stage
      type: array
      description: Stage in the config file
      default:
        - ''
        - ''
        - ''
        - ''
    - name: image
      type: array
      description: Docker image to be used to run the stage script.
    - name: script
      type: array
      description: Location of the script that is used to run the stage.
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
      default:
        - 'false'
        - 'false'
        - 'false'
        - 'true'
    - name: configmap
      type: array
      description: Name of a `ConfigMap` that's going to be mounted.
      default:
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
        - not-provided-configmap
    - name: secret
      type: array
      description: Name of a `Secret` that's going to be mounted.
      default:
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
        - not-provided-secret
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
      default:
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
        - IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      description: all repos from the pr-start
      default: '[]'
    - name: start-exitcode
      default: '0'
  results:
    - name: PRCODECHECKSTATUS
      type: string
      default: '[]'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap[0])
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret[0])
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: toolchain
        mountPath: /toolchain
      - name: tmp
        mountPath: /tmp
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: dind-certs
          mountPath: /certs/client
      env:
        - name: STEP_NAME
          value: start
        - name: HEAD_REPO
          value: $(params.head-repo)
        - name: HEAD_BRANCH
          value: $(params.head-branch)
        - name: HEAD_SHA
          value: $(params.head-sha)
        - name: BASE_REPO
          value: $(params.base-repo)
        - name: BASE_BRANCH
          value: $(params.base-branch)
        - name: BASE_REPO_OWNER
          value: $(params.base-repo-owner)
        - name: BASE_REPO_NAME
          value: $(params.base-repo-name)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: PR_HTML_URL
          value: $(params.pr-html-url)
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap[0])
        - name: CUSTOM_SECRET
          value: $(params.secret[0])
        - name: start_exit_code
          value: $(params.start-exitcode)
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        #!/bin/bash


        # Initialize task results

        echo -n "[]" > $(results.PRCODECHECKSTATUS.path)

        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "pr-code-check steps: ${stages[*]}""

        echo "Number of steps in pr-code-check: ${#stages[@]}""

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          set_env fatal_error "true"
          exit ${start_exit_code}
        fi


        echo -n "${HEAD_REPO}" >> /config/head-repo

        echo -n "${HEAD_BRANCH}" >> /config/head-branch

        echo -n "${HEAD_SHA}" >> /config/head-sha

        echo -n "${BASE_REPO}" >> /config/base-repo

        echo -n "${BASE_BRANCH}" >> /config/base-branch

        echo -n "${BASE_REPO_OWNER}" >> /config/base-repo-owner

        echo -n "${BASE_REPO_NAME}" >> /config/base-repo-name


        echo -n "${HEAD_BRANCH}" >> /config/branch

        echo -n "${BASE_BRANCH}" >> /config/target-branch

        echo -n "${HEAD_SHA}" >> /config/commit-sha

        echo -n "${BASE_REPO}" >> /config/repository

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${PR_HTML_URL}" >> /config/pr-html-url

        set_env head-repo "${HEAD_REPO}"

        set_env head-branch "${HEAD_BRANCH}"

        set_env head-sha "${HEAD_SHA}"

        set_env base-repo "${BASE_REPO}"

        set_env base-branch "${BASE_BRANCH}"

        set_env base-repo-owner "${BASE_REPO_OWNER}"

        set_env base-repo-name "${BASE_REPO_NAME}"

        set_env branch "${HEAD_BRANCH}"

        set_env target-branch "${BASE_BRANCH}"

        set_env commit-sha "${HEAD_SHA}"

        set_env repository "${BASE_REPO}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env pr-html-url "${PR_HTML_URL}"


        . "${ONE_PIPELINE_PATH}/internal/task_start"


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        status_checks_prefix="$(get_env branch-protection-status-check-prefix
        "tekton")"

        if [ -e /tekton/steps/step-unit-test ]; then
          set-commit-status \
            --repository "$(load_repo app-repo url)" \
            --commit-sha "$(load_repo app-repo commit)" \
            --state "pending" \
            --description "Running unit tests..." \
            --context "${status_checks_prefix}/${TASK_NAME}/code-unit-tests" \
            --task-name "$(get_env UNIT_TESTS_TASK_NAME)" \
            --step-name "$(get_env UNIT_TESTS_STEP_NAME)"
        fi


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: checks-setup
      image: $(params.image[0])
      imagePullPolicy: $(params.image-pull-policy[0])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[0])
        - name: secret
          mountPath: /config/$(params.secret[0])
        - name: docker-socket
          mountPath: /var/run/
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: checks-setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_checks_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: detect-secrets
      image: $(params.image[1])
      imagePullPolicy: $(params.image-pull-policy[1])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[1])
        - name: secret
          mountPath: /config/$(params.secret[1])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_detect_secrets
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: detect-secrets
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_detect_secrets"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: unit-test
      image: $(params.image[2])
      imagePullPolicy: $(params.image-pull-policy[2])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[2])
        - name: secret
          mountPath: /config/$(params.secret[2])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: unit-test
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_unit_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: compliance-checks
      image: $(params.image[3])
      imagePullPolicy: $(params.image-pull-policy[3])
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[3])
        - name: secret
          mountPath: /config/$(params.secret[3])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[3])
      env:
        - name: STAGE
          value: $(params.stage[3])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_compliance_checks
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STEP_NAME
          value: compliance-checks
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[3])
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_compliance_checks"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: configmap
          mountPath: /config/$(params.configmap[3])
        - name: secret
          mountPath: /config/$(params.secret[3])
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: working-dir
          mountPath: /stage-$(params.stage[3])
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: DETECT_SECRETS_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: COMPLIANCE_CHECKS_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: ON_ERROR
          value: ''
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[3])
      script: >
        #!/usr/bin/bash


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        stages=$(/usr/local/bin/list_stage_names)


        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if [ "$(get_env STAGE_UNIT_TEST_STATUS "")" != "" ]; then
          unit_test_description="Unit tests finished running."
        else
          unit_test_description="Pipeline failed before checking this check"
        fi


        status_checks_prefix="$(get_env branch-protection-status-check-prefix
        "tekton")"


        setup_exit_code=$(cat $(steps.step-checks-setup.exitCode.path))

        if [[ "$stages" == *"detect-secrets"* ]]; then
          detect_secrets_exit_code=$(cat $(steps.step-detect-secrets.exitCode.path))
        fi

        if [[ "$stages" == *"unit-test"* ]]; then
          test_exit_code=$(cat $(steps.step-unit-test.exitCode.path))
          set-commit-status \
            --repository "$(load_repo app-repo url)" \
            --commit-sha "$(load_repo app-repo commit)" \
            --state "$(get_env STAGE_UNIT_TEST_STATUS "failure")" \
            --description "${unit_test_description}" \
            --context "${status_checks_prefix}/${TASK_NAME}/code-unit-tests" \
            --task-name "$(get_env UNIT_TESTS_TASK_NAME)" \
            --step-name "$(get_env UNIT_TESTS_STEP_NAME)"
        fi

        if [[ "$stages" == *"compliance-checks"* ]]; then
          compliance_checks_exit_code=$(cat $(steps.step-compliance-checks.exitCode.path))
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          pipelinectl serialize STAGE_UNIT_TEST_STATUS UNIT_TESTS_TASK_NAME UNIT_TESTS_STEP_NAME > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.PRCODECHECKSTATUS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: pr-finish
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: evaluate,prepare
spec:
  params:
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the name of the PR's repo target
    - name: base-repo-name
      description: name of the base repo
    - name: base-repo-owner
      description: owner of the base repo
    - name: pr-url
      description: PR url
    - name: pr-html-url
      description: HTML url of the PR
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: pipeline-status
      description: Agggregate status of pipeline tasks
      default: ''
    - name: pr-title
      description: PR title
    - name: default-branch
      description: default target branch
      default: ''
    - name: PRCODECHECKSTATUS
      type: array
      default: []
      description: Status of unit test status with step names fetched from code check stage
    - name: REPOS
      description: all repos from the pr-start
      default: '[]'
    - name: start-exitcode
      default: '0'
  volumes:
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: configmap
        mountPath: /config/$(params.configmap)
      - name: secret
        mountPath: /config/$(params.secret)
      - name: toolchain
        mountPath: /toolchain
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PR_TITLE
        value: $(params.pr-title)
      - name: DEFAULT_BRANCH
        value: $(params.default-branch)
      - name: PR_HTML_URL
        value: $(params.pr-html-url)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: 'true'
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: workspace
          mountPath: /workspace/app
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: evaluate
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      onError: continue
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: evaluate
        - name: HEAD_REPO
          value: $(params.head-repo)
        - name: HEAD_BRANCH
          value: $(params.head-branch)
        - name: HEAD_SHA
          value: $(params.head-sha)
        - name: BASE_REPO
          value: $(params.base-repo)
        - name: BASE_BRANCH
          value: $(params.base-branch)
        - name: BASE_REPO_OWNER
          value: $(params.base-repo-owner)
        - name: BASE_REPO_NAME
          value: $(params.base-repo-name)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: start_exit_code
          value: $(params.start-exitcode)
      args:
        - $(params.PRCODECHECKSTATUS[*])
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        echo -n "${HEAD_REPO}" >> /config/head-repo

        echo -n "${HEAD_BRANCH}" >> /config/head-branch

        echo -n "${HEAD_SHA}" >> /config/head-sha

        echo -n "${BASE_REPO}" >> /config/base-repo

        echo -n "${BASE_BRANCH}" >> /config/base-branch

        echo -n "${BASE_REPO_OWNER}" >> /config/base-repo-owner

        echo -n "${BASE_REPO_NAME}" >> /config/base-repo-name


        echo -n "${HEAD_BRANCH}" >> /config/branch

        echo -n "${BASE_BRANCH}" >> /config/target-branch

        echo -n "${HEAD_SHA}" >> /config/commit-sha

        echo -n "${BASE_REPO}" >> /config/repository

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${PR_HTML_URL}" >> /config/pr-html-url

        set_env head-repo "${HEAD_REPO}"

        set_env head-branch "${HEAD_BRANCH}"

        set_env head-sha "${HEAD_SHA}"

        set_env base-repo "${BASE_REPO}"

        set_env base-branch "${BASE_BRANCH}"

        set_env base-repo-owner "${BASE_REPO_OWNER}"

        set_env base-repo-name "${BASE_REPO_NAME}"

        set_env branch "${HEAD_BRANCH}"

        set_env target-branch "${BASE_BRANCH}"

        set_env commit-sha "${HEAD_SHA}"

        set_env repository "${BASE_REPO}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env pr-html-url "${PR_HTML_URL}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
          cleanup_data_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done

          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        export PATH="$PATH:/opt/cocoa/bin"

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" = "true" ]; then
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi

        set_env STAGE_TEST_STATUS "$(get_env STAGE_UNIT_TEST_STATUS "skipped")"


        . "${ONE_PIPELINE_PATH}/internal/task_start"

        . "${ONE_PIPELINE_PATH}"/internal/pr_finish
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: tmp
          mountPath: /tmp
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
      script: >
        #!/bin/bash


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        echo "Preparing custom finish step"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: run-stage
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: working-dir
          mountPath: /stage-finish
        - name: tmp
          mountPath: /tmp
      env:
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_WORKSPACE
          value: /stage-finish
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        finish_evaluate_exit_code=$(cat $(steps.step-evaluate.exitCode.path))

        export finish_evaluate_exit_code

        export STAGE_SCRIPT_PATH="stage_script_runner_run_stage"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run||exit_code=$?

        if [ $finish_evaluate_exit_code -ne 0 ]; then
          echo "Failing the finish stage as \"evaluate\" step failed with error code $finish_evaluate_exit_code"
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true

        if [ "$PIPELINE_STATUS" != "Succeeded" -a "$PIPELINE_STATUS" !=
        "Completed" ]; then
          echo "Pipeline run failed. Please verify the status of previous stages"
          exit 1
        fi

        exit $(($exit_code + $finish_evaluate_exit_code))
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: pr-start
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: start
spec:
  params:
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the name of the PR's repo target
    - name: base-repo-name
      description: name of the base repo
    - name: base-repo-owner
      description: owner of the base repo
    - name: pr-url
      description: PR url
    - name: pr-html-url
      description: HTML url of the PR
    - name: pr-title
      description: PR title
    - name: default-branch
      description: default target branch
      default: ''
    - name: action
      description: status of PR
    - name: base_ref
      description: target branch of PR
    - name: commit-timestamp
      description: the timestamp of when the commit pushed
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: next-stage
      default: ''
    - name: final-stage
      default: ''
  results:
    - name: image
      description: Docker image to be used in the specified stage.
    - name: script
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: configmap
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: secret
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: working-dir
      description: one-pipeline working dir, the location of pipeline-config.yaml
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: dind
      description: Launch Docker-in-Docker sidecar
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: stage-skip
      description: Whether the stage needs to skipped if possible
    - name: final-stage-image
      description: Docker image to be used in the specified stage.
    - name: final-stage-script
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: final-stage-configmap
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: final-stage-secret
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: final-stage-abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: final-stage-dind
      description: Launch Docker-in-Docker sidecar
    - name: final-stage-image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: final-stage-skip
      description: Whether the stage needs to skipped if possible
    - name: run-after
      description: >-
        Specify the previous stage that needs to be complete before starting
        next stage
    - name: REPOS
      type: string
      default: '[]'
    - name: pr-url
      description: PR URL that may have been computed in case of MergeQueue
    - name: pr-html-url
      description: PR HTML URL that may have been computed in case of MergeQueue
    - name: task-exit-code
      description: Initialize to kill the pipeline and and when success reset
  volumes:
    - name: config
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
    env:
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: COMPLIANCE_BASEIMAGE
        value: $(params.compliance-baseimage)
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
      env:
        - name: STEP_NAME
          value: start
        - name: HEAD_REPO
          value: $(params.head-repo)
        - name: HEAD_BRANCH
          value: $(params.head-branch)
        - name: HEAD_SHA
          value: $(params.head-sha)
        - name: BASE_REPO
          value: $(params.base-repo)
        - name: BASE_BRANCH
          value: $(params.base-branch)
        - name: BASE_REPO_OWNER
          value: $(params.base-repo-owner)
        - name: BASE_REPO_NAME
          value: $(params.base-repo-name)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: PR_HTML_URL
          value: $(params.pr-html-url)
        - name: PR_TITLE
          value: $(params.pr-title)
        - name: DEFAULT_BRANCH
          value: $(params.default-branch)
        - name: ACTION
          value: $(params.action)
        - name: BASE_REF
          value: $(params.base_ref)
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        # initialine task results

        echo -n "[]" > $(results.REPOS.path)

        # handle both PR and Merge request event branch format

        # PR: master

        # Merge queue: refs/heads/master

        HEAD_BRANCH=${HEAD_BRANCH#"refs/heads/"}

        BASE_BRANCH=${BASE_BRANCH#"refs/heads/"}


        # Handle Merge queue PRs

        # if PR_URL is empty, then assume this is a Merge queue PR

        if [ -z "$PR_URL" ]; then
          echo "Merge queue Pull Request"
          echo ""

          SKIP_MERGE_PR=$(get_env skip-merge-pr-to-base "false")
          if [[ $SKIP_MERGE_PR != "false" ]]; then
            echo "skip-merge-pr-to-base has been set at pipeline or trigger level with value set to $SKIP_MERGE_PR."
            echo ""
          fi
          if [[ $SKIP_MERGE_PR == "false" ]]; then
            echo "WARNING! skip-merge-pr-to-base property is not set, or is set to false (default)."
            echo "Merging PR into base branch is not necessary in the context of Merge queue,"
            echo "as this merge operation has automatically been performed when PR was added to Merge queue."
            echo ""
            echo "Enable this option by ensuring skip-merge-pr-to-base is added to the trigger properties, with value set to true."
            echo ""
            echo ""
            EARLY_ABORT=true
          fi

          PR_UPDATES=$(get_env opt-in-pr-updates "1")
          if [[ $PR_UPDATES != "1" ]]; then
            echo "opt-in-pr-updates has been set at pipeline or trigger level with value set to $PR_UPDATES."
            echo ""
          fi
          if [[ $PR_UPDATES == "1" ]]; then
            echo "WARNING! opt-in-pr-updates property is not set, or is set to 1 (default)."
            echo "Commenting in PR is not possible in the context of Merge queue PRs."
            echo ""
            echo "Disable this option by ensuring opt-in-pr-updates is added to the trigger properties, with value set to 0."
            echo ""
            echo ""
            EARLY_ABORT=true
          fi

          if [ -n "${EARLY_ABORT}" ]; then
            echo "Please fix the above trigger configuration problem(s) and add PR to Merge queue again."
            echo "Exiting 1."
            exit 1
          fi

          # need to use the base repo and base branch to compute a pr url and pr html url for merge request like
          # pr-url: https://github.ibm.com/api/v3/repos/cd-jumpstart/adoption-issue-3111/pulls/37
          # pr-html-url: https://github.ibm.com/cd-jumpstart/adoption-issue-3111/pull/37
          # with such information HEAD_REPO, HEAD_BRANCH, BASE_REPO and BASE_BRANCH like:
          # BASE_BRANCH=refs/heads/master
          # BASE_REPO=https://github.ibm.com/cd-jumpstart/adoption-issue-3111
          # BASE_REPO_NAME=adoption-issue-3111
          # BASE_REPO_OWNER=cd-jumpstart
          # HEAD_BRANCH=refs/heads/gh-readonly-queue/master/pr-37-74616a36b123df7bcff4df1952efc6ca982ea53c
          # HEAD_REPO=https://github.ibm.com/cd-jumpstart/adoption-issue-3111
          # this is required to fix https://github.ibm.com/one-pipeline/adoption-issues/issues/3565

          PR_NUMBER="$(echo ${HEAD_BRANCH#*/$BASE_BRANCH/} | awk -F- '{print $2}')"

          export PR_URL="${BASE_REPO%/$BASE_REPO_OWNER/$BASE_REPO_NAME}/api/v3/repos/${BASE_REPO_OWNER}/${BASE_REPO_NAME}/pulls/${PR_NUMBER}"
          export PR_HTML_URL="${BASE_REPO}/pull/${PR_NUMBER}"

        fi


        export PATH="$PATH:/opt/cocoa/bin"

        echo -n "${HEAD_REPO}" >> /config/head-repo

        echo -n "${HEAD_BRANCH}" >> /config/head-branch

        echo -n "${HEAD_SHA}" >> /config/head-sha

        echo -n "${BASE_REPO}" >> /config/base-repo

        echo -n "${BASE_BRANCH}" >> /config/base-branch

        echo -n "${BASE_REPO_OWNER}" >> /config/base-repo-owner

        echo -n "${BASE_REPO_NAME}" >> /config/base-repo-name


        echo -n "${HEAD_BRANCH}" >> /config/branch

        echo -n "${BASE_BRANCH}" >> /config/target-branch

        echo -n "${HEAD_SHA}" >> /config/commit-sha

        echo -n "${BASE_REPO}" >> /config/repository

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${PR_HTML_URL}" >> /config/pr-html-url

        echo -n "${PR_TITLE}" >> /config/pr-title

        echo -n "${DEFAULT_BRANCH}" >> /config/default-branch

        echo -n "${ACTION}" >> /config/action

        echo -n "${BASE_REF}" >> /config/base_ref

        set_env head-repo "${HEAD_REPO}"

        set_env head-branch "${HEAD_BRANCH}"

        set_env head-sha "${HEAD_SHA}"

        set_env base-repo "${BASE_REPO}"

        set_env base-branch "${BASE_BRANCH}"

        set_env base-repo-owner "${BASE_REPO_OWNER}"

        set_env base-repo-name "${BASE_REPO_NAME}"

        set_env branch "${HEAD_BRANCH}"

        set_env target-branch "${BASE_BRANCH}"

        set_env commit-sha "${HEAD_SHA}"

        set_env repository "${BASE_REPO}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env pr-html-url "${PR_HTML_URL}"

        set_env pr-title "${PR_TITLE}"

        set_env default-branch "${DEFAULT_BRANCH}"

        set_env action "${ACTION}"

        set_env base_ref "${BASE_REF}"


        # Capture the exit code and send the exit code to task result

        if "${ONE_PIPELINE_PATH}/internal/pr_start"; then
          start_exit_code=$?
          echo -n "${start_exit_code}" > $(results.task-exit-code.path)
        else
          start_exit_code=$?
          echo -n "${start_exit_code}" > $(results.task-exit-code.path)
        fi


        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          set_env fatal_error "true"
          exit ${start_exit_code}
        fi


        if [ -z "${BASE_REF}" ]; then
          set_env base_ref "<none>"
        else
          set_env base_ref "${BASE_REF}"
        fi

        if [ -z "${PR_TITLE}" ]; then
          set_env pr-title "<none>"
        else
          set_env pr-title "${PR_TITLE}"
        fi

        if [ -z "${ACTION}" ]; then
          set_env action "<none>"
        else
          set_env action "${ACTION}"
        fi

        if [ -z "${DEFAULT_BRANCH}" ]; then
          set_env default-branch "<none>"
        else
          set_env default-branch "${DEFAULT_BRANCH}"
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.REPOS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true


        echo -n "${PR_URL}" > $(results.pr-url.path)

        echo -n "${PR_HTML_URL}" > $(results.pr-html-url.path)


        exit ${start_exit_code}
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: inventory-validation
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: validate,prepare
spec:
  params:
    - name: pr-url
      description: PR url
    - name: head-sha
      description: head-sha of the PR to update statuses
    - name: target-environment
      description: target-environment of the promotion
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: pipeline-debug
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: final-stage
      description: Name of the finish stage in pipeline-config.yaml (e.g. finish)
      default: finish
    - name: stage
      description: Current stage name used for STAGE env var
      default: finish
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
  results:
    - name: image
      description: Docker image to be used in the specified stage.
    - name: script
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: configmap
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: secret
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: working-dir
      description: one-pipeline working dir, the location of pipeline-config.yaml
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: dind
      description: Launch Docker-in-Docker sidecar
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: IMAGES
      type: string
      default: ''
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: COMPLIANCE_BASEIMAGE
        value: $(params.compliance-baseimage)
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: $(params.dind)
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: toolchain
          mountPath: /toolchain
        - mountPath: /var/run/
          name: docker-socket
      env:
        - name: STEP_NAME
          value: prepare
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export PATH="$PATH:/opt/cocoa/bin"

        set_env pipeline_namespace "promotion_validation"

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        . "${ONE_PIPELINE_PATH}"/internal/pipeline/setup_promotion_validation


        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        echo "Preparing custom finish step"

        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "inventory-validation steps: ${stages[*]}"

        echo "Number of steps in inventory-validation: ${#stages[@]}"


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _ for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: validate
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
      env:
        - name: STAGE
          value: promotion-validate
        - name: STEP_NAME
          value: validate
        - name: PR_URL
          value: $(params.pr-url)
        - name: HEAD_SHA
          value: $(params.head-sha)
        - name: TARGET_ENVIRONMENT
          value: $(params.target-environment)
      script: |
        #!/bin/bash
        . "${ONE_PIPELINE_PATH}"/tools/capture_log
        export PROMOTION_PR
        PROMOTION_PR="${PR_URL}"
        export HEAD_SHA
        HEAD_SHA="${HEAD_SHA}"
        echo "Running validation script"
        . "${ONE_PIPELINE_PATH}/internal/promotion_validate"
    - name: finish
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: toolchain
          mountPath: /toolchain
        - mountPath: /var/run/
          name: docker-socket
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.final-stage)
      env:
        - name: STAGE
          value: $(params.stage)
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: STEP_NAME
          value: finish
        - name: FINAL_STAGE
          value: $(params.final-stage)
        - name: STAGE_WORKSPACE
          value: /stage-$(params.final-stage)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_finish"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$(params.final-stage)" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: run-stage-with-fork
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: prepare
spec:
  params:
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: next-stages
      type: array
      default:
        - ''
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
  results:
    - name: image
      type: array
      description: Docker image to be used in the specified stage.
    - name: script
      type: array
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: configmap
      type: array
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: secret
      type: array
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: working-dir
      description: one-pipeline working dir, the location of pipeline-config.yaml
    - name: abort-on-failure
      type: array
      description: Whether abort the pipeline in case of a failure or not
    - name: dind
      type: array
      description: Launch Docker-in-Docker sidecar
    - name: image-pull-policy
      type: array
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: stage-skip
      type: array
      description: Whether the stage needs to skipped if possible
    - name: run-after
      type: array
      description: >-
        Specify the previous stage that needs to be complete before starting
        next stage
    - name: IMAGES
      type: string
      default: ''
  workspaces:
    - name: app
      description: Workspace where the application is checked out.
      mountPath: $(params.workspace-mount-path)
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: $(workspaces.app.path)
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: $(params.dind)
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: $(workspaces.app.volume)
          mountPath: $(workspaces.app.path)
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
      script: |
        #!/usr/bin/env bash

        if [ "$(get_env pipeline-error-status "")" == "failed" ]; then
          exit 1
        fi
        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: run-stage
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: $(workspaces.app.path)
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STAGE_SCRIPT_PATH
          value: $(params.script)
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: PIPELINE_RUN_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.labels['tekton.dev/pipelineRun']
        - name: BUILD_NUMBER
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
        - name: TRIGGER_TYPE
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
        - name: TRIGGER_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
        - name: TRIGGERED_BY
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_RUN_AFTER
          value: $(params.run-after)
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_run_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0)
        }' | sed 's/-/_/g')"

        run_stage_exit_code=$(get_env STAGE_"${current_run_stage}"_EXIT_CODE)


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$(params.stage)" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi

        exit $run_stage_exit_code
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: run-stage
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: prepare
spec:
  params:
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
    - name: next-stage
      default: ''
    - name: run-after
      description: run the current stage after the mentioned stage
      default: 'null'
  results:
    - name: image
      description: Docker image to be used in the specified stage.
    - name: script
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: configmap
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: secret
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: working-dir
      description: one-pipeline working dir, the location of pipeline-config.yaml
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: dind
      description: Launch Docker-in-Docker sidecar
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: stage-skip
      description: Whether the stage needs to skipped if possible
    - name: run-after
      description: >-
        Specify the previous stage that needs to be complete before starting
        next stage
    - name: IMAGES
      type: string
      default: ''
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: $(params.dind)
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: prepare
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: dind-certs
          mountPath: /certs/client
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
      script: |
        #!/usr/bin/env bash

        if [ "$(get_env pipeline-error-status "")" == "failed" ]; then
          exit 1
        fi
        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: run-stage
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: toolchain
          mountPath: /toolchain
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STAGE_SCRIPT_PATH
          value: $(params.script)
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: TOOLCHAIN_DATA_PATH
          value: /toolchain/toolchain.json
        - name: PIPELINE_RUN_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.labels['tekton.dev/pipelineRun']
        - name: BUILD_NUMBER
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
        - name: TRIGGER_TYPE
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
        - name: TRIGGER_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
        - name: TRIGGERED_BY
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_RUN_AFTER
          value: $(params.run-after)
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_run_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0)
        }' | sed 's/-/_/g')"

        run_stage_exit_code=$(get_env STAGE_"${current_run_stage}"_EXIT_CODE)


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$(params.stage)" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi

        exit $run_stage_exit_code
---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: simple-execute
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: validate-pipeline,start
    devops.cloud.ibm.com/finish-step: validate-pipeline
spec:
  params:
    - name: stage
      description: Stage in the config file
    - name: image
      description: Docker image to be used to run the stage script.
    - name: script
      description: Location of the script that is used to run the stage.
    - name: dind
      description: Launch Docker-in-Docker sidecar
      default: 'false'
    - name: configmap
      description: Name of a `ConfigMap` that's going to be mounted.
      default: not-provided-configmap
    - name: secret
      description: Name of a `Secret` that's going to be mounted.
      default: not-provided-secret
    - name: abort-on-failure
      description: Whether abort the pipeline in case of a failure or not
    - name: image-pull-policy
      description: Set the imagePullPolicy for the Docker image in the runner task
      default: IfNotPresent
    - name: workspace-mount-path
      default: /workspace/app
    - name: compliance-baseimage
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: head-branch
      description: source branch of the PR
    - name: head-sha
      description: commit id
    - name: head-repo
      description: the name of the PR's repo source
    - name: base-branch
      description: target branch of the PR
    - name: base-repo
      description: the name of the PR's repo target
    - name: base-sha
      description: base commit
    - name: pr-url
      description: PR url
    - name: branch
      description: branch
      default: ''
    - name: commit-sha
      description: commit id
    - name: pipeline-debug
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      description: Enable metrics collection
      default: '1'
  volumes:
    - name: config
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: configmap
      configMap:
        name: $(params.configmap)
        optional: true
    - name: secret
      secret:
        secretName: $(params.secret)
        optional: true
    - name: toolchain
      configMap:
        name: toolchain
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
  stepTemplate:
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - mountPath: /var/run/
        name: docker-socket
      - name: workspace
        mountPath: /workspace/app
      - name: toolchain
        mountPath: /toolchain
      - name: tmp
        mountPath: /tmp
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
  sidecars:
    - name: dind
      image: $(params.dind-image)
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      env:
        - name: ENABLE_DIND
          value: $(params.dind)
      volumeMounts:
        - mountPath: /var/lib/docker
          name: dind-storage
        - mountPath: /var/run/
          name: docker-socket
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      script: |
        if [[ "$ENABLE_DIND" == "true" ]]; then
          if [[ $(df -PT /var/lib/docker | awk 'NR==2 {print $2}') == virtiofs ]]; then
            apk add e2fsprogs
            truncate -s 50G /tmp/disk.img
            mkfs.ext4 /tmp/disk.img
            mount /tmp/disk.img /var/lib/docker
          fi
          # Set the MTU to a value that is containable in the ibmcloud calico mtu value
          # References:
          # - https://liejuntao001.medium.com/fix-docker-in-docker-network-issue-in-kubernetes-cc18c229d9e5
          # - https://cloud.ibm.com/docs/containers?topic=containers-kernel#calico-mtu
          #
          exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
        fi
      startupProbe:
        timeoutSeconds: 7
        failureThreshold: 60
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
      readinessProbe:
        initialDelaySeconds: 5
        timeoutSeconds: 7
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: dind-certs
          mountPath: /certs/client
      env:
        - name: CUSTOM_CONFIGMAP
          value: $(params.configmap)
        - name: CUSTOM_SECRET
          value: $(params.secret)
        - name: STEP_NAME
          value: prepare
        - name: HEAD_REPO
          value: $(params.head-repo)
        - name: HEAD_BRANCH
          value: $(params.head-branch)
        - name: HEAD_SHA
          value: $(params.head-sha)
        - name: BASE_REPO
          value: $(params.base-repo)
        - name: BASE_BRANCH
          value: $(params.base-branch)
        - name: BASE_SHA
          value: $(params.base-sha)
        - name: PR_URL
          value: $(params.pr-url)
        - name: COMMIT_SHA
          value: $(params.commit-sha)
        - name: BRANCH
          value: $(params.branch)
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        export NEXT_STAGE="execute"


        . "${ONE_PIPELINE_PATH}/internal/simple_pipeline_start"

        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare


        set_env ONE_PIPELINE_REPO "$(get_env pipeline-config-repo "")"

        set_env ONE_PIPELINE_CONFIG_COMMIT "$(get_env GIT_COMMIT "")"

        set_env pipeline-namespace "simple-executor"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "cc-deployment-scan steps: ${stages[*]}"

        echo "Number of steps in cc-deployment-scan: ${#stages[@]}"


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done
    - name: execute
      image: $(params.image)
      imagePullPolicy: $(params.image-pull-policy)
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: configmap
          mountPath: /config/$(params.configmap)
        - name: secret
          mountPath: /config/$(params.secret)
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STAGE_SCRIPT_PATH
          value: $(params.script)
        - name: HEAD_REPO
          value: $(params.head-repo)
        - name: HEAD_BRANCH
          value: $(params.head-branch)
        - name: HEAD_SHA
          value: $(params.head-sha)
        - name: BASE_REPO
          value: $(params.base-repo)
        - name: BASE_BRANCH
          value: $(params.base-branch)
        - name: BASE_SHA
          value: $(params.base-sha)
        - name: PR_URL
          value: $(params.pr-url)
        - name: COMMIT_SHA
          value: $(params.commit-sha)
        - name: BRANCH
          value: $(params.branch)
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure)
        - name: PIPELINE_RUN_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.labels['tekton.dev/pipelineRun']
        - name: PIPELINE_RUN_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
        - name: PIPELINE_RUN_URL
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
        - name: BUILD_NUMBER
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
        - name: TRIGGER_TYPE
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
        - name: TRIGGER_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
        - name: TRIGGERED_BY
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
        - name: STEP_NAME
          value: run-stage
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage)
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_$(echo "${STAGE}" | tr '-'
        '_')"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        current_run_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0)
        }' | sed 's/-/_/g')"

        run_stage_exit_code=$(get_env STAGE_"${current_run_stage}"_EXIT_CODE)


        exit $run_stage_exit_code
    - name: validate-pipeline
      image: $(params.compliance-baseimage)
      imagePullPolicy: IfNotPresent
      workingDir: /workspace/app
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage)
      env:
        - name: STAGE
          value: validate-pipeline
        - name: STEP_NAME
          value: finalize
        - name: ON_ERROR
          value: ''
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage)
      script: |
        #!/usr/bin/bash
        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi
        if [ -n "$ON_ERROR" ]; then
          # Split the map string into key-value pairs
          IFS=',' read -r -a pairs <<< "$ON_ERROR"
          for pair in "${pairs[@]}"; do
            # Split each key-value pair and populate the associative array
            IFS='=' read -r STEP ONERROR <<< "$pair"
            if [[ "$stages" == *"$STEP"* ]]; then
              case "${STEP}" in
                "execute") exit_code=$(cat $(steps.step-execute.exitCode.path))
                  ;;
                *) exit_code=0
                  ;;
              esac
            else
              exit_code=0
            fi
            if [ $exit_code -ne 0 ]; then
              if [ $ONERROR == "stopAndFail" ]; then
                echo "step $STEP failed. please verify the status of the previous steps"
                exit 1
              fi
            fi
          done
          exit 0
        fi
---
apiVersion: v1
kind: Secret
data:
  .dockerconfigjson: eyJhdXRocyI6e319
metadata:
  name: artifactory-pull-secret
type: kubernetes.io/dockerconfigjson
---
apiVersion: v1
kind: Secret
data:
  .dockerconfigjson: eyJhdXRocyI6e319
metadata:
  name: onepipeline-pull-secret
type: kubernetes.io/dockerconfigjson
---
apiVersion: v1
kind: Secret
data:
  .dockerconfigjson: eyJhdXRocyI6e319
metadata:
  name: one-pipeline-pull-secret
type: kubernetes.io/dockerconfigjson
---
apiVersion: v1
kind: Secret
data:
  .dockerconfigjson: eyJhdXRocyI6e319
metadata:
  name: pipeline-pull-secret
type: kubernetes.io/dockerconfigjson
---
apiVersion: v1
kind: Secret
data:
  .dockerconfigjson: eyJhdXRocyI6e319
metadata:
  name: icr-pull-secret
type: kubernetes.io/dockerconfigjson
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: docker-build-sa
secrets:
  - name: artifactory-pull-secret
  - name: onepipeline-pull-secret
  - name: one-pipeline-pull-secret
  - name: pipeline-pull-secret
  - name: icr-pull-secret
---
kind: PipelineRun
apiVersion: tekton.dev/v1
metadata:
  name: ci-pipelinerun
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: 3eb9042c-7a65-43f8-9708-2fb19985b54f
  resourceVersion: '1298957245'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:44Z'
  labels:
    app: workeragent
    tekton.dev/pipeline: ci-pipeline
  annotations:
    devops.cloud.ibm.com/build-number: '180'
    devops.cloud.ibm.com/chains-agentname: us-south-501-9x-worker
    devops.cloud.ibm.com/chains-agentversion: 0.22.15
    devops.cloud.ibm.com/chains-chainsversion: v0.26.3
    devops.cloud.ibm.com/chains-clustercrn: >-
      crn:v1:bluemix:public:containers-kubernetes:us-south:a6677d241bbad8189145cbae0c73208f:cvrqrmdd0c5f227fcsog
    devops.cloud.ibm.com/chains-clustername: otc-tektonpw-9x-dal03-1
    devops.cloud.ibm.com/chains-clusterregion: us-south
    devops.cloud.ibm.com/chains-externalsecretsversion: external-secrets-2.9.0
    devops.cloud.ibm.com/chains-k8splatform: linux/amd64
    devops.cloud.ibm.com/chains-k8sversion: '1.33'
    devops.cloud.ibm.com/chains-kataversion: 3.31.0-245
    devops.cloud.ibm.com/chains-nodename: 192.168.46.115
    devops.cloud.ibm.com/chains-tektonversion: v1.6.0
    devops.cloud.ibm.com/chains-timestampstart: '20260826111244'
    devops.cloud.ibm.com/compliance-pipeline-definitions: v11
    devops.cloud.ibm.com/listener: ci-listener
    devops.cloud.ibm.com/owned-by: Arya.Mohanan@ibm.com
    devops.cloud.ibm.com/pipeline-config-version: '2'
    devops.cloud.ibm.com/pipeline-id: 579d9c4d-163d-4171-be94-9535ff3f68c4
    devops.cloud.ibm.com/pipeline-run-url: >-
      https://cloud.ibm.com/devops/pipelines/tekton/579d9c4d-163d-4171-be94-9535ff3f68c4/runs/c5bc3783-9c47-4a5f-bd97-f75e541644d6?env_id=ibm:yp:us-south
    devops.cloud.ibm.com/preprocessor: enabled
    devops.cloud.ibm.com/pwp: 27a20337-47e2-4050-8da5-c0e68ca28084
    devops.cloud.ibm.com/pwp-sub: 0a5baad3-502d-45fe-b6e1-4d1de0f1e0cd
    devops.cloud.ibm.com/tekton-pipeline: c5bc3783-9c47-4a5f-bd97-f75e541644d6
    devops.cloud.ibm.com/trigger-id: 95d9a2f8-9ee8-4777-9efd-d41b1c966108
    devops.cloud.ibm.com/trigger-name: listener_manual_node_x
    devops.cloud.ibm.com/trigger-type: manual
    devops.cloud.ibm.com/triggered-by: Katharina.Irrgang@ibm.com
    devops.cloud.ibm.com/worker-instance: 0507c9b9-1670-4378-afc2-b1a56a84eece
  finalizers:
    - chains.tekton.dev/pipelinerun
spec:
  pipelineRef:
    name: ci-pipeline
  params:
    - name: app-name
      value: ''
    - name: registry-region
      value: ''
    - name: branch
      value: ci-sps
    - name: repository-integration
      value: ''
    - name: slack-notifications
      value: ''
    - name: pipeline-debug
      value: ''
    - name: collect-metrics
      value: '1'
    - name: commit-id
      value: ''
    - name: commit-timestamp
      value: ''
    - name: cos-bucket-name
      value: cos-secure-pipelines-service-instana
    - name: cos-endpoint
      value: s3.us-south.cloud-object-storage.appdomain.cloud
    - name: doi-toolchain-id
      value: ''
    - name: doi-environment
      value: ''
    - name: pipeline-config
      value: .sps/pipeline-config-collector-logging.yaml
    - name: pipeline-config-repo
      value: ''
    - name: pipeline-config-branch
      value: main
    - name: repository
      value: https://github.com/instana/nodejs
    - name: compliance-baseimage
      value: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: dind-image
      value: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: detect-secrets-image
      value: >-
        icr.io/continuous-delivery/toolchains/devsecops/detect-secrets:0.13.1.ibm.64.dss-redhat-ubi@sha256:e855dcf18944f1d92e567a8d6979caa089592915250c225c8642ff89a54a1c72
  timeouts:
    pipeline: 24h0m0s
  taskRunTemplate:
    podTemplate:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 1
              preference:
                matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - 192.168.46.115
      runtimeClassName: kata
      imagePullSecrets:
        - name: artifactory-pull-secret
        - name: onepipeline-pull-secret
        - name: one-pipeline-pull-secret
        - name: pipeline-pull-secret
        - name: icr-pull-secret
    serviceAccountName: docker-build-sa
  taskRunSpecs:
    - pipelineTaskName: ci-start
      podTemplate:
        affinity:
          nodeAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:
                  matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - 192.168.46.115
        runtimeClassName: kata
        imagePullSecrets:
          - name: artifactory-pull-secret
          - name: onepipeline-pull-secret
          - name: one-pipeline-pull-secret
          - name: pipeline-pull-secret
          - name: icr-pull-secret
    - pipelineTaskName: code-checks
      podTemplate:
        affinity:
          nodeAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:
                  matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - 192.168.46.115
        runtimeClassName: kata
        imagePullSecrets:
          - name: artifactory-pull-secret
          - name: onepipeline-pull-secret
          - name: one-pipeline-pull-secret
          - name: pipeline-pull-secret
          - name: icr-pull-secret
    - pipelineTaskName: code-build
      podTemplate:
        affinity:
          nodeAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:
                  matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - 192.168.46.115
        runtimeClassName: kata-large
        imagePullSecrets:
          - name: artifactory-pull-secret
          - name: onepipeline-pull-secret
          - name: one-pipeline-pull-secret
          - name: pipeline-pull-secret
          - name: icr-pull-secret
    - pipelineTaskName: pr-messaging-console
      podTemplate:
        affinity:
          nodeAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:
                  matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - 192.168.46.115
        runtimeClassName: kata-large
        imagePullSecrets:
          - name: artifactory-pull-secret
          - name: onepipeline-pull-secret
          - name: one-pipeline-pull-secret
          - name: pipeline-pull-secret
          - name: icr-pull-secret
    - pipelineTaskName: pr-messaging-express-winston
      podTemplate:
        affinity:
          nodeAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:
                  matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - 192.168.46.115
        runtimeClassName: kata-large
        imagePullSecrets:
          - name: artifactory-pull-secret
          - name: onepipeline-pull-secret
          - name: one-pipeline-pull-secret
          - name: pipeline-pull-secret
          - name: icr-pull-secret
    - pipelineTaskName: pr-messaging-pino
      podTemplate:
        affinity:
          nodeAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:
                  matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - 192.168.46.115
        runtimeClassName: kata-large
        imagePullSecrets:
          - name: artifactory-pull-secret
          - name: onepipeline-pull-secret
          - name: one-pipeline-pull-secret
          - name: pipeline-pull-secret
          - name: icr-pull-secret
    - pipelineTaskName: pr-messaging-bunyan
      podTemplate:
        affinity:
          nodeAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:
                  matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - 192.168.46.115
        runtimeClassName: kata-large
        imagePullSecrets:
          - name: artifactory-pull-secret
          - name: onepipeline-pull-secret
          - name: one-pipeline-pull-secret
          - name: pipeline-pull-secret
          - name: icr-pull-secret
    - pipelineTaskName: pr-messaging-log4js
      podTemplate:
        affinity:
          nodeAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:
                  matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - 192.168.46.115
        runtimeClassName: kata-large
        imagePullSecrets:
          - name: artifactory-pull-secret
          - name: onepipeline-pull-secret
          - name: one-pipeline-pull-secret
          - name: pipeline-pull-secret
          - name: icr-pull-secret
    - pipelineTaskName: pr-messaging-winston
      podTemplate:
        affinity:
          nodeAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:
                  matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - 192.168.46.115
        runtimeClassName: kata-large
        imagePullSecrets:
          - name: artifactory-pull-secret
          - name: onepipeline-pull-secret
          - name: one-pipeline-pull-secret
          - name: pipeline-pull-secret
          - name: icr-pull-secret
    - pipelineTaskName: code-ci-finish
      podTemplate:
        affinity:
          nodeAffinity:
            preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 1
                preference:
                  matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - 192.168.46.115
        runtimeClassName: kata
        imagePullSecrets:
          - name: artifactory-pull-secret
          - name: onepipeline-pull-secret
          - name: one-pipeline-pull-secret
          - name: pipeline-pull-secret
          - name: icr-pull-secret
status:
  conditions:
    - type: Succeeded
      status: Unknown
      lastTransitionTime: '2026-08-26T11:12:46Z'
      reason: Running
      message: 'Tasks Completed: 0 (Failed: 0, Cancelled 0), Incomplete: 10, Skipped: 0'
  startTime: '2026-08-26T11:12:44Z'
  childReferences:
    - apiVersion: tekton.dev/v1
      kind: TaskRun
      name: ci-pipelinerun-ci-start
      pipelineTaskName: ci-start
---
kind: Pipeline
apiVersion: tekton.dev/v1
metadata:
  name: ci-pipeline
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: 76e3e2ed-34e3-4316-ab58-c3eabba41026
  resourceVersion: '1298957100'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:43Z'
  annotations:
    devops.cloud.ibm.com/compliance-pipeline-definitions: v11
    devops.cloud.ibm.com/pipeline-config-version: '2'
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:43Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/compliance-pipeline-definitions: {}
            f:devops.cloud.ibm.com/pipeline-config-version: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:finally: {}
          f:params: {}
          f:tasks: {}
spec:
  tasks:
    - name: ci-start
      taskRef:
        name: ci-start
        kind: Task
      params:
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: final-stage
          value: finish
    - name: code-checks
      taskRef:
        name: code-checks
        kind: Task
      runAfter:
        - ci-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: code-build
      taskRef:
        name: code-build
        kind: Task
      runAfter:
        - ci-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: pr-messaging-console
      displayName: console
      taskRef:
        name: pr-messaging-console
        kind: Task
      runAfter:
        - ci-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: pr-messaging-express-winston
      displayName: express-winston
      taskRef:
        name: pr-messaging-express-winston
        kind: Task
      runAfter:
        - ci-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: pr-messaging-pino
      displayName: pino
      taskRef:
        name: pr-messaging-pino
        kind: Task
      runAfter:
        - ci-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: pr-messaging-bunyan
      displayName: bunyan
      taskRef:
        name: pr-messaging-bunyan
        kind: Task
      runAfter:
        - ci-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: pr-messaging-log4js
      displayName: log4js
      taskRef:
        name: pr-messaging-log4js
        kind: Task
      runAfter:
        - ci-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
    - name: pr-messaging-winston
      displayName: winston
      taskRef:
        name: pr-messaging-winston
        kind: Task
      runAfter:
        - ci-start
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
  params:
    - name: branch
      type: string
      description: the branch for the git repo
      default: ''
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: pr url
      default: ''
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: compliance-baseimage
      type: string
      description: Pipeline baseimage to run most of the built-in pipeline code
    - name: dind-image
      type: string
      description: Base image to run sidecars
    - name: detect-secrets-image
      type: string
      description: Base image to detect Secrets
    - name: pipeline-debug
      type: string
      description: toggles debug mode for the pipeline
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
  finally:
    - name: code-ci-finish
      taskRef:
        name: ci-finish
        kind: Task
      params:
        - name: compliance-baseimage
          value: $(params.compliance-baseimage)
        - name: pipeline-debug
          value: $(params.pipeline-debug)
        - name: collect-metrics
          value: $(params.collect-metrics)
        - name: dind-image
          value: $(params.dind-image)
        - name: detect-secrets-image
          value: $(params.detect-secrets-image)
        - name: pipeline-status
          value: $(tasks.status)
        - name: branch
          value: $(params.branch)
        - name: repository-integration
          value: $(params.repository-integration)
        - name: commit-id
          value: $(params.commit-id)
        - name: commit-timestamp
          value: $(params.commit-timestamp)
        - name: pr-url
          value: $(params.pr-url)
        - name: repository
          value: $(params.repository)
        - name: CODECHECKSTATUS
          value:
            - $(tasks.code-checks.results.CODECHECKSTATUS)
        - name: BUILDARTIFACTS
          value:
            - $(tasks.code-build.results.ARTIFACTS)
            - $(tasks.pr-messaging-console.results.ARTIFACTS)
            - $(tasks.pr-messaging-express-winston.results.ARTIFACTS)
            - $(tasks.pr-messaging-pino.results.ARTIFACTS)
            - $(tasks.pr-messaging-bunyan.results.ARTIFACTS)
            - $(tasks.pr-messaging-log4js.results.ARTIFACTS)
            - $(tasks.pr-messaging-winston.results.ARTIFACTS)
        - name: REPOS
          value: $(tasks.ci-start.results.REPOS)
        - name: start-exitcode
          value: $(tasks.ci-start.results.task-exit-code)
---
kind: TaskRun
apiVersion: tekton.dev/v1
metadata:
  name: ci-pipelinerun-ci-start
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: 992f1ae5-61d4-4927-ad22-87a74c79f993
  resourceVersion: '1298957543'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:45Z'
  labels:
    app: workeragent
    app.kubernetes.io/managed-by: tekton-pipelines
    tekton.dev/memberOf: tasks
    tekton.dev/pipeline: ci-pipeline
    tekton.dev/pipelineRun: ci-pipelinerun
    tekton.dev/pipelineRunUID: 3eb9042c-7a65-43f8-9708-2fb19985b54f
    tekton.dev/pipelineTask: ci-start
    tekton.dev/task: ci-start
  annotations:
    ci-pipelinerun-ci-start-pod: Started
    devops.cloud.ibm.com/build-number: '180'
    devops.cloud.ibm.com/chains-agentname: us-south-501-9x-worker
    devops.cloud.ibm.com/chains-agentversion: 0.22.15
    devops.cloud.ibm.com/chains-chainsversion: v0.26.3
    devops.cloud.ibm.com/chains-clustercrn: >-
      crn:v1:bluemix:public:containers-kubernetes:us-south:a6677d241bbad8189145cbae0c73208f:cvrqrmdd0c5f227fcsog
    devops.cloud.ibm.com/chains-clustername: otc-tektonpw-9x-dal03-1
    devops.cloud.ibm.com/chains-clusterregion: us-south
    devops.cloud.ibm.com/chains-externalsecretsversion: external-secrets-2.9.0
    devops.cloud.ibm.com/chains-k8splatform: linux/amd64
    devops.cloud.ibm.com/chains-k8sversion: '1.33'
    devops.cloud.ibm.com/chains-kataversion: 3.31.0-245
    devops.cloud.ibm.com/chains-nodename: 192.168.46.115
    devops.cloud.ibm.com/chains-tektonversion: v1.6.0
    devops.cloud.ibm.com/chains-timestampstart: '20260826111244'
    devops.cloud.ibm.com/compliance-pipeline-definitions: v11
    devops.cloud.ibm.com/listener: ci-listener
    devops.cloud.ibm.com/operatorName: private-worker-agent-6b44c8c589-csmwg
    devops.cloud.ibm.com/owned-by: Arya.Mohanan@ibm.com
    devops.cloud.ibm.com/pipeline-config-version: '2'
    devops.cloud.ibm.com/pipeline-id: 579d9c4d-163d-4171-be94-9535ff3f68c4
    devops.cloud.ibm.com/pipeline-run-url: >-
      https://cloud.ibm.com/devops/pipelines/tekton/579d9c4d-163d-4171-be94-9535ff3f68c4/runs/c5bc3783-9c47-4a5f-bd97-f75e541644d6?env_id=ibm:yp:us-south
    devops.cloud.ibm.com/platform-controlled-steps: start
    devops.cloud.ibm.com/preprocessor: enabled
    devops.cloud.ibm.com/pwp: 27a20337-47e2-4050-8da5-c0e68ca28084
    devops.cloud.ibm.com/pwp-sub: 0a5baad3-502d-45fe-b6e1-4d1de0f1e0cd
    devops.cloud.ibm.com/tekton-logs: >-
      {"ci-pipelinerun-ci-start-pod/start":"08e05168-2079-4111-9697-506fe7a47198","ci-pipelinerun-ci-start-pod/tekton-log-results":"8be2ef32-51cb-44f4-9cf5-0f9024496cb9"}
    devops.cloud.ibm.com/tekton-pipeline: c5bc3783-9c47-4a5f-bd97-f75e541644d6
    devops.cloud.ibm.com/trigger-id: 95d9a2f8-9ee8-4777-9efd-d41b1c966108
    devops.cloud.ibm.com/trigger-name: listener_manual_node_x
    devops.cloud.ibm.com/trigger-type: manual
    devops.cloud.ibm.com/triggered-by: Katharina.Irrgang@ibm.com
    devops.cloud.ibm.com/worker-instance: 0507c9b9-1670-4378-afc2-b1a56a84eece
    pipeline.tekton.dev/release: fcba522
  ownerReferences:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      name: ci-pipelinerun
      uid: 3eb9042c-7a65-43f8-9708-2fb19985b54f
      controller: true
      blockOwnerDeletion: true
  finalizers:
    - chains.tekton.dev/taskrun
spec:
  params:
    - name: branch
      value: ci-sps
    - name: repository-integration
      value: ''
    - name: commit-id
      value: ''
    - name: commit-timestamp
      value: ''
    - name: pr-url
      value: ''
    - name: repository
      value: https://github.com/instana/nodejs
    - name: compliance-baseimage
      value: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage:4.22.5_commons-1.92.1@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
    - name: pipeline-debug
      value: ''
    - name: collect-metrics
      value: '1'
    - name: final-stage
      value: finish
  serviceAccountName: docker-build-sa
  taskRef:
    name: ci-start
    kind: Task
  timeout: 24h0m0s
  podTemplate:
    affinity:
      nodeAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1
            preference:
              matchExpressions:
                - key: kubernetes.io/hostname
                  operator: In
                  values:
                    - 192.168.46.115
    runtimeClassName: kata
    imagePullSecrets:
      - name: artifactory-pull-secret
      - name: onepipeline-pull-secret
      - name: one-pipeline-pull-secret
      - name: pipeline-pull-secret
      - name: icr-pull-secret
status:
  conditions:
    - type: Succeeded
      status: Unknown
      lastTransitionTime: '2026-08-26T11:12:52Z'
      reason: Running
      message: Not all Steps in the Task have finished executing
  podName: ci-pipelinerun-ci-start-pod
  startTime: '2026-08-26T11:12:46Z'
  steps:
    - running:
        startedAt: '2026-08-26T11:12:51Z'
      name: start
      container: step-start
      imageID: >-
        icr.io/continuous-delivery/toolchains/devsecops/baseimage@sha256:79663675dc66a5baaee5a94da4bba024a115f973701d63bfc05ee6b2b3560d26
      provenance: {}
  sidecars:
    - running:
        startedAt: '2026-08-26T11:12:52Z'
      name: tekton-log-results
      container: sidecar-tekton-log-results
      imageID: >-
        icr.io/continuous-delivery/pipeline/tekton/sidecarlogresults@sha256:934e82f377c37aa98d42a7f37692a631f1b29a1dd1cc01e1b29b8f402b56b552
---
kind: Task
apiVersion: tekton.dev/v1
metadata:
  name: pr-messaging-express-winston
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: 6f07f9cb-2f52-423a-b76a-51865d269039
  resourceVersion: '1298957034'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:41Z'
  annotations:
    devops.cloud.ibm.com/finish-step: collect-evidence
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:41Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/finish-step: {}
            f:devops.cloud.ibm.com/platform-controlled-steps: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:params: {}
          f:results: {}
          f:sidecars: {}
          f:stepTemplate:
            .: {}
            f:computeResources: {}
            f:env: {}
            f:volumeMounts: {}
          f:steps: {}
          f:volumes: {}
spec:
  params:
    - name: branch
      type: string
      description: branch
      default: master
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: PR url
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: stage
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: script
      type: array
      default:
        - script
        - script
        - script
        - script
        - script
    - name: abort-on-failure
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: workspace-mount-path
      type: string
      default: /workspace/app
    - name: compliance-baseimage
      type: string
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      type: string
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      type: string
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
    - name: run-after
      type: string
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      type: string
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      type: string
      default: '0'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "code-build steps: ${stages[*]}"

        echo "Number of steps in code-build: ${#stages[@]}"


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i

          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
      onError: stopAndFail
    - name: unit-test
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: unit-test
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_unit_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: build-artifact
      displayName: express-winston
      image: mirror.gcr.io/library/node:24
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_build-artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: build-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_build_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$STAGE" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: scan-artifact
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_scan_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: scan-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_scan_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: continue
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: CONTAINERIZE_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: SIGN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: SCAN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: ON_ERROR
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      imagePullPolicy: IfNotPresent
      script: >
        #!/usr/bin/bash

        export PATH="$PATH:/opt/cocoa/bin"


        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi
  volumes:
    - name: config
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
  stepTemplate:
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
    computeResources: {}
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
  sidecars:
    - name: dind
      image: $(params.dind-image)
      computeResources: {}
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      readinessProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        initialDelaySeconds: 5
        timeoutSeconds: 7
      startupProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        timeoutSeconds: 7
        failureThreshold: 60
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
  results:
    - name: IMAGES
      type: string
    - name: ARTIFACTS
      type: string
---
kind: Task
apiVersion: tekton.dev/v1
metadata:
  name: pr-messaging-bunyan
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: a29375c0-5927-4ab6-a898-0d71ba686b02
  resourceVersion: '1298957060'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:42Z'
  annotations:
    devops.cloud.ibm.com/finish-step: collect-evidence
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:42Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/finish-step: {}
            f:devops.cloud.ibm.com/platform-controlled-steps: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:params: {}
          f:results: {}
          f:sidecars: {}
          f:stepTemplate:
            .: {}
            f:computeResources: {}
            f:env: {}
            f:volumeMounts: {}
          f:steps: {}
          f:volumes: {}
spec:
  params:
    - name: branch
      type: string
      description: branch
      default: master
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: PR url
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: stage
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: script
      type: array
      default:
        - script
        - script
        - script
        - script
        - script
    - name: abort-on-failure
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: workspace-mount-path
      type: string
      default: /workspace/app
    - name: compliance-baseimage
      type: string
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      type: string
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      type: string
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
    - name: run-after
      type: string
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      type: string
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      type: string
      default: '0'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "code-build steps: ${stages[*]}"

        echo "Number of steps in code-build: ${#stages[@]}"


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i

          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
      onError: stopAndFail
    - name: unit-test
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: unit-test
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_unit_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: build-artifact
      displayName: bunyan
      image: mirror.gcr.io/library/node:24
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_build-artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: build-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_build_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$STAGE" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: scan-artifact
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_scan_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: scan-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_scan_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: continue
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: CONTAINERIZE_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: SIGN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: SCAN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: ON_ERROR
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      imagePullPolicy: IfNotPresent
      script: >
        #!/usr/bin/bash

        export PATH="$PATH:/opt/cocoa/bin"


        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi
  volumes:
    - name: config
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
  stepTemplate:
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
    computeResources: {}
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
  sidecars:
    - name: dind
      image: $(params.dind-image)
      computeResources: {}
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      readinessProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        initialDelaySeconds: 5
        timeoutSeconds: 7
      startupProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        timeoutSeconds: 7
        failureThreshold: 60
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
  results:
    - name: IMAGES
      type: string
    - name: ARTIFACTS
      type: string
---
kind: Task
apiVersion: tekton.dev/v1
metadata:
  name: ci-finish
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: f159203e-c178-45e6-af60-fcd44e42fab8
  resourceVersion: '1298957113'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:43Z'
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: evaluate,prepare
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:43Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/platform-controlled-steps: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:params: {}
          f:sidecars: {}
          f:stepTemplate:
            .: {}
            f:computeResources: {}
            f:env: {}
            f:volumeMounts: {}
          f:steps: {}
          f:volumes: {}
spec:
  params:
    - name: branch
      type: string
      description: branch
      default: master
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: PR url
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: stage
      type: string
      default: run-stage
    - name: script
      type: string
      default: script
    - name: abort-on-failure
      type: string
      default: run-stage
    - name: workspace-mount-path
      type: string
      default: /workspace/app
    - name: compliance-baseimage
      type: string
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      type: string
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      type: string
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
    - name: pipeline-status
      type: string
      description: Agggregate status of pipeline tasks
      default: ''
    - name: deploy-dev-state
      type: string
      description: Status of deployment to dev cluster status
      default: ''
    - name: REPOS
      type: string
      description: Status of all the scans done in deploy-checks
      default: '[]'
    - name: CODECHECKSTATUS
      type: array
      description: Status of all the scans done in code-checks
      default: []
    - name: BUILDARTIFACTS
      type: array
      description: all artifacts from the code-build
      default: []
    - name: start-exitcode
      type: string
      default: '0'
  steps:
    - name: evaluate
      image: $(params.compliance-baseimage)
      args:
        - $(params.CODECHECKSTATUS[*])
        - $(params.BUILDARTIFACTS[*])
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: evaluate
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      computeResources: {}
      volumeMounts:
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
          cleanup_data_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          # loop through all the arguments - CODECHECKSTATUS, ARTIFACTS
          for arg in "$@"; do
            if [  "$arg" != "[]" ]; then
              echo -n "$arg" | base64 -d > ./args.json.gz
              gzip -d ./args.json.gz
              pipelinectl deserialize ./args.json
              rm ./args.json
            fi
          done

          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        set_env STAGE_TEST_STATUS "$(get_env STAGE_UNIT_TEST_STATUS)"


        . "${ONE_PIPELINE_PATH}/internal/task_start"


        # evaluate

        . "${ONE_PIPELINE_PATH}/internal/ci_finish"
      onError: continue
    - name: prepare
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: prepare
      computeResources: {}
      volumeMounts:
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: tmp
          mountPath: /tmp
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        export PATH="$PATH:/opt/cocoa/bin"

        echo "Preparing custom finish step"


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
  volumes:
    - name: dind-certs
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: config
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: working-dir
      emptyDir: {}
    - name: workspace
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
  stepTemplate:
    env:
      - name: STAGE
        value: $(params.stage)
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: PIPELINE_STATUS
        value: $(params.pipeline-status)
      - name: DEPLOY_DEV_STATUS
        value: $(params.deploy-dev-state)
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
    computeResources: {}
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: config
        mountPath: /config
      - name: environment-properties
        mountPath: /config/environment-properties
      - name: secure-properties
        mountPath: /config/secure-properties
      - name: toolchain
        mountPath: /toolchain
      - name: workspace
        mountPath: /workspace/app
  sidecars:
    - name: dind
      image: $(params.dind-image)
      computeResources: {}
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      readinessProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        initialDelaySeconds: 5
        timeoutSeconds: 7
      startupProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        timeoutSeconds: 7
        failureThreshold: 60
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
---
kind: Task
apiVersion: tekton.dev/v1
metadata:
  name: code-build
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: 1560a70a-ac8a-4703-8ac4-a3ed3a982417
  resourceVersion: '1298957144'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:43Z'
  annotations:
    devops.cloud.ibm.com/finish-step: collect-evidence
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:43Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/finish-step: {}
            f:devops.cloud.ibm.com/platform-controlled-steps: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:params: {}
          f:results: {}
          f:sidecars: {}
          f:stepTemplate:
            .: {}
            f:computeResources: {}
            f:env: {}
            f:volumeMounts: {}
          f:steps: {}
          f:volumes: {}
spec:
  params:
    - name: branch
      type: string
      description: branch
      default: master
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: PR url
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: stage
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: script
      type: array
      default:
        - script
        - script
        - script
        - script
        - script
    - name: abort-on-failure
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: workspace-mount-path
      type: string
      default: /workspace/app
    - name: compliance-baseimage
      type: string
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      type: string
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      type: string
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
    - name: run-after
      type: string
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      type: string
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      type: string
      default: '0'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "code-build steps: ${stages[*]}"

        echo "Number of steps in code-build: ${#stages[@]}"


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i

          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
      onError: stopAndFail
    - name: build-artifact
      displayName: npm-install
      image: mirror.gcr.io/library/node:24
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_build-artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: build-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_build_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$STAGE" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: CONTAINERIZE_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: SIGN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: SCAN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: ON_ERROR
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      imagePullPolicy: IfNotPresent
      script: >
        #!/usr/bin/bash

        export PATH="$PATH:/opt/cocoa/bin"


        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi
  volumes:
    - name: config
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
  stepTemplate:
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
    computeResources: {}
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
  sidecars:
    - name: dind
      image: $(params.dind-image)
      computeResources: {}
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      readinessProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        initialDelaySeconds: 5
        timeoutSeconds: 7
      startupProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        timeoutSeconds: 7
        failureThreshold: 60
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
  results:
    - name: IMAGES
      type: string
    - name: ARTIFACTS
      type: string
---
kind: Task
apiVersion: tekton.dev/v1
metadata:
  name: code-checks
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: bbd2ee2b-384b-49b5-8bc6-590192747f28
  resourceVersion: '1298957154'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:44Z'
  annotations:
    devops.cloud.ibm.com/finish-step: collect-evidence
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:44Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/finish-step: {}
            f:devops.cloud.ibm.com/platform-controlled-steps: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:params: {}
          f:results: {}
          f:sidecars: {}
          f:stepTemplate:
            .: {}
            f:computeResources: {}
            f:env: {}
            f:volumeMounts: {}
          f:steps: {}
          f:volumes: {}
spec:
  params:
    - name: branch
      type: string
      description: branch
      default: master
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: PR url
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: stage
      type: array
      default:
        - checks-setup
        - detect-secrets
        - compliance-checks
        - peer-review
        - static-scan
    - name: script
      type: array
      default:
        - script
        - script
        - script
        - script
        - script
    - name: abort-on-failure
      type: array
      default:
        - checks-setup
        - detect-secrets
        - compliance-checks
        - peer-review
        - static-scan
    - name: workspace-mount-path
      type: string
      default: /workspace/app
    - name: compliance-baseimage
      type: string
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      type: string
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      type: string
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
    - name: run-after
      type: string
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      type: string
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      type: string
      default: '0'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      args:
        - $(params.stage[*])
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.CODECHECKSTATUS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "code-check steps: ${stages[*]}""

        echo "Number of steps in code-check: ${#stages[@]}""


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        for i in "${stages[@]}"

        do
          STAGE=$i
          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: checks-setup
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: checks-setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_checks_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: detect-secrets
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_detect_secrets
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: detect-secrets
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_detect_secrets"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: compliance-checks
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_compliance_checks
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: compliance-checks
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_compliance_checks"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: continue
    - name: static-scan
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_static_scan
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: static-scan
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_static_scan"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: continue
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: DETECT_SECRETS_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: COMPLIANCE_CHECKS_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: PEER_REVIEW_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: STATIC_SCAN_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: ON_ERROR
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
      imagePullPolicy: IfNotPresent
      script: >
        #!/usr/bin/bash


        export PATH="$PATH:/opt/cocoa/bin"

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.CODECHECKSTATUS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true
  volumes:
    - name: config
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
  stepTemplate:
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
    computeResources: {}
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
  sidecars:
    - name: dind
      image: $(params.dind-image)
      computeResources: {}
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      readinessProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        initialDelaySeconds: 5
        timeoutSeconds: 7
      startupProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        timeoutSeconds: 7
        failureThreshold: 60
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
  results:
    - name: CODECHECKSTATUS
      type: string
---
kind: Task
apiVersion: tekton.dev/v1
metadata:
  name: pr-messaging-console
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: 7bee4004-5865-483e-8108-db1b7d4f777b
  resourceVersion: '1298957030'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:41Z'
  annotations:
    devops.cloud.ibm.com/finish-step: collect-evidence
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:41Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/finish-step: {}
            f:devops.cloud.ibm.com/platform-controlled-steps: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:params: {}
          f:results: {}
          f:sidecars: {}
          f:stepTemplate:
            .: {}
            f:computeResources: {}
            f:env: {}
            f:volumeMounts: {}
          f:steps: {}
          f:volumes: {}
spec:
  params:
    - name: branch
      type: string
      description: branch
      default: master
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: PR url
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: stage
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: script
      type: array
      default:
        - script
        - script
        - script
        - script
        - script
    - name: abort-on-failure
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: workspace-mount-path
      type: string
      default: /workspace/app
    - name: compliance-baseimage
      type: string
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      type: string
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      type: string
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
    - name: run-after
      type: string
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      type: string
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      type: string
      default: '0'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "code-build steps: ${stages[*]}"

        echo "Number of steps in code-build: ${#stages[@]}"


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i

          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
      onError: stopAndFail
    - name: unit-test
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: unit-test
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_unit_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: build-artifact
      displayName: console
      image: mirror.gcr.io/library/node:24
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_build-artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: build-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_build_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$STAGE" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: scan-artifact
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_scan_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: scan-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_scan_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: continue
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: CONTAINERIZE_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: SIGN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: SCAN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: ON_ERROR
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      imagePullPolicy: IfNotPresent
      script: >
        #!/usr/bin/bash

        export PATH="$PATH:/opt/cocoa/bin"


        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi
  volumes:
    - name: config
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
  stepTemplate:
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
    computeResources: {}
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
  sidecars:
    - name: dind
      image: $(params.dind-image)
      computeResources: {}
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      readinessProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        initialDelaySeconds: 5
        timeoutSeconds: 7
      startupProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        timeoutSeconds: 7
        failureThreshold: 60
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
  results:
    - name: IMAGES
      type: string
    - name: ARTIFACTS
      type: string
---
kind: Task
apiVersion: tekton.dev/v1
metadata:
  name: pr-messaging-pino
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: 2920cab8-2944-41fb-9f7e-492502130cc8
  resourceVersion: '1298957043'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:41Z'
  annotations:
    devops.cloud.ibm.com/finish-step: collect-evidence
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:41Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/finish-step: {}
            f:devops.cloud.ibm.com/platform-controlled-steps: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:params: {}
          f:results: {}
          f:sidecars: {}
          f:stepTemplate:
            .: {}
            f:computeResources: {}
            f:env: {}
            f:volumeMounts: {}
          f:steps: {}
          f:volumes: {}
spec:
  params:
    - name: branch
      type: string
      description: branch
      default: master
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: PR url
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: stage
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: script
      type: array
      default:
        - script
        - script
        - script
        - script
        - script
    - name: abort-on-failure
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: workspace-mount-path
      type: string
      default: /workspace/app
    - name: compliance-baseimage
      type: string
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      type: string
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      type: string
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
    - name: run-after
      type: string
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      type: string
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      type: string
      default: '0'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "code-build steps: ${stages[*]}"

        echo "Number of steps in code-build: ${#stages[@]}"


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i

          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
      onError: stopAndFail
    - name: unit-test
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: unit-test
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_unit_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: build-artifact
      displayName: pino
      image: mirror.gcr.io/library/node:24
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_build-artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: build-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_build_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$STAGE" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: scan-artifact
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_scan_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: scan-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_scan_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: continue
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: CONTAINERIZE_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: SIGN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: SCAN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: ON_ERROR
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      imagePullPolicy: IfNotPresent
      script: >
        #!/usr/bin/bash

        export PATH="$PATH:/opt/cocoa/bin"


        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi
  volumes:
    - name: config
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
  stepTemplate:
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
    computeResources: {}
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
  sidecars:
    - name: dind
      image: $(params.dind-image)
      computeResources: {}
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      readinessProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        initialDelaySeconds: 5
        timeoutSeconds: 7
      startupProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        timeoutSeconds: 7
        failureThreshold: 60
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
  results:
    - name: IMAGES
      type: string
    - name: ARTIFACTS
      type: string
---
kind: Task
apiVersion: tekton.dev/v1
metadata:
  name: pr-messaging-log4js
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: e917047d-560d-4775-8b46-01957b73ee97
  resourceVersion: '1298957084'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:42Z'
  annotations:
    devops.cloud.ibm.com/finish-step: collect-evidence
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:42Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/finish-step: {}
            f:devops.cloud.ibm.com/platform-controlled-steps: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:params: {}
          f:results: {}
          f:sidecars: {}
          f:stepTemplate:
            .: {}
            f:computeResources: {}
            f:env: {}
            f:volumeMounts: {}
          f:steps: {}
          f:volumes: {}
spec:
  params:
    - name: branch
      type: string
      description: branch
      default: master
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: PR url
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: stage
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: script
      type: array
      default:
        - script
        - script
        - script
        - script
        - script
    - name: abort-on-failure
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: workspace-mount-path
      type: string
      default: /workspace/app
    - name: compliance-baseimage
      type: string
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      type: string
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      type: string
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
    - name: run-after
      type: string
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      type: string
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      type: string
      default: '0'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "code-build steps: ${stages[*]}"

        echo "Number of steps in code-build: ${#stages[@]}"


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i

          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
      onError: stopAndFail
    - name: unit-test
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: unit-test
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_unit_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: build-artifact
      displayName: log4js
      image: mirror.gcr.io/library/node:24
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_build-artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: build-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_build_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$STAGE" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: scan-artifact
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_scan_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: scan-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_scan_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: continue
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: CONTAINERIZE_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: SIGN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: SCAN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: ON_ERROR
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      imagePullPolicy: IfNotPresent
      script: >
        #!/usr/bin/bash

        export PATH="$PATH:/opt/cocoa/bin"


        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi
  volumes:
    - name: config
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
  stepTemplate:
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
    computeResources: {}
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
  sidecars:
    - name: dind
      image: $(params.dind-image)
      computeResources: {}
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      readinessProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        initialDelaySeconds: 5
        timeoutSeconds: 7
      startupProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        timeoutSeconds: 7
        failureThreshold: 60
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
  results:
    - name: IMAGES
      type: string
    - name: ARTIFACTS
      type: string
---
kind: Task
apiVersion: tekton.dev/v1
metadata:
  name: pr-messaging-winston
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: 2c1a33bb-0ebe-4fc8-8f02-e0895d51f7d6
  resourceVersion: '1298957096'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:42Z'
  annotations:
    devops.cloud.ibm.com/finish-step: collect-evidence
    devops.cloud.ibm.com/platform-controlled-steps: collect-evidence,start
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:42Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/finish-step: {}
            f:devops.cloud.ibm.com/platform-controlled-steps: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:params: {}
          f:results: {}
          f:sidecars: {}
          f:stepTemplate:
            .: {}
            f:computeResources: {}
            f:env: {}
            f:volumeMounts: {}
          f:steps: {}
          f:volumes: {}
spec:
  params:
    - name: branch
      type: string
      description: branch
      default: master
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: PR url
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: stage
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: script
      type: array
      default:
        - script
        - script
        - script
        - script
        - script
    - name: abort-on-failure
      type: array
      default:
        - setup
        - unit-test
        - build-artifact
        - sign-artifact
        - scan-artifact
    - name: workspace-mount-path
      type: string
      default: /workspace/app
    - name: compliance-baseimage
      type: string
      description: base image to run most of the built-in pipeline code
    - name: dind-image
      type: string
      description: Base image to run sidecars
      default: icr.io/continuous-delivery/base-images/multiarch-dind:v1.26.0
    - name: pipeline-debug
      type: string
      description: Use debug mode
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
    - name: run-after
      type: string
      description: run the current stage after the mentioned stage
      default: 'null'
    - name: REPOS
      type: string
      description: all repos from the ci-start
      default: '[]'
    - name: start-exitcode
      type: string
      default: '0'
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
        - name: start_exit_code
          value: $(params.start-exitcode)
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: pipeline-scripts
          mountPath: /usr/share/one-pipeline
        - name: commons-library
          mountPath: /usr/share/commons
        - name: pipelinectl
          mountPath: /usr/share/cocoa
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        if [ "$PIPELINE_DEBUG" == 1 ]; then
          pwd
          set -x
        fi


        # Initialize task results

        echo -n "[]" > $(results.ARTIFACTS.path)

        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          exit ${start_exit_code}
        fi


        # Initialize an empty array

        stages=()

        steps=()

        STEP_NAMES=()


        # Loop through directories starting with step- and capture the step
        names

        for f in /tekton/steps/step-*; do
          target=$(readlink "$f")
          num=$(basename "$(dirname "$target")")
          steps+=("$num ${f##*/}")
        done


        sorted_steps=($(printf "%s\n" "${steps[@]}" | sort -n))

        for item in "${sorted_steps[@]}"; do
          if [[ "$item" == *step-* ]]; then
            STEP_NAMES+=("$item")
          fi
        done


        PLATFORM_CONTROLLED_STEPS="${PLATFORM_CONTROLLED_STEPS:-}"

        IFS=',' read -r -a controlled_steps_array <<<
        "$PLATFORM_CONTROLLED_STEPS"


        # Iterate over each step name

        for step in "${STEP_NAMES[@]}"; do
          stage="${step#step-}"  # Remove "step-" prefix
          if [[ " ${controlled_steps_array[*]} " =~ " $stage " ]]; then
            continue  # Skip the platform controlled step
          fi
          stages+=("$stage")     # Append to array
        done


        echo "code-build steps: ${stages[*]}"

        echo "Number of steps in code-build: ${#stages[@]}"


        # start

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        if [ -z "$BRANCH" ]; then
          BRANCH="master"
        fi

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils

        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/environment-properties" -type f)


        while read -r file; do
          ln -snf "$file" "/config/$(basename "$file")"
        done < <(find "/config/secure-properties" -type f)

        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to retrieve pipeline data"
          download_from_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          if [ "$(params.REPOS)" != "[]" ]; then
            echo -n "$(params.REPOS)" | base64 -d > ./args.json.gz
            gzip -d ./args.json.gz
            pipelinectl deserialize ./args.json
            rm ./args.json
          fi
        fi

        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        if "${ONE_PIPELINE_PATH}/internal/task_start"; then
          ci_start_exit_code=0
        else
          ci_start_exit_code=1
        fi


        # Ensure a pipeline-config file exists - if not polyglot to the rescue

        config_file_name="$(get_env one-pipeline-config
        ".pipeline-config.yaml")"

        config_file="${WORKSPACE}/$(get_env
        ONE_PIPELINE_CONFIG_DIRECTORY_NAME)/${config_file_name}"

        # internal flag to force usage of polyglot/inferred devsecops for
        pipeline configuration

        # workaround until
        https://github.ibm.com/one-pipeline/adoption-issues/issues/3299 is
        completed

        if [ "$(get_env force-polyglot false)" == "true" ]; then
          rm -f "$config_file"
        fi

        if [ ! -f "$config_file" ]; then
          # ensure yq version4 is the default yq
          ln -s -f /usr/local/bin/yq4 /usr/local/bin/yq
          source "${ONE_PIPELINE_PATH}/internal/pipeline/polyglot"
          highlight "Unable to locate pipeline configuration at path $config_file_name. Injecting default pipeline configuration based on code introspection.\nDocumentation available at https://cloud.ibm.com/docs/devsecops?topic=devsecops-devsecops-inferred-pipeline-configuration"
          # translate default pipeline-config yaml to v11 in the expected location
          chmod +x "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh"
          "${ONE_PIPELINE_PATH}/polyglot/polyglot/translate-pipeline-config.sh" "${ONE_PIPELINE_PATH}/polyglot/default-pipeline-config.yaml" v11 "$config_file"
          # bootstrap polyglot for the known repos
          mkdir -p "${WORKSPACE}/.1pl/repos"
          while read -r key; do
            bootstrap-polyglot "$key"
          done < <(list_repos)
          ln -s -f /usr/local/bin/yq3 /usr/local/bin/yq
        fi


        # prepare

        source "${ONE_PIPELINE_PATH}"/internal/stage/parse_config_helpers

        #initialize j for SCRIPT counter

        for i in "${stages[@]}"

        do
          STAGE=$i

          # Replace the - with _for the standard naming convention
          stage_script_runner="${WORKSPACE}/stage_script_runner_$(echo "${STAGE}" | tr '-' '_')"
          echo "stage_script runner file ${stage_script_runner}"
          stage_script="/tmp/stage-script-${STAGE}"
          echo "stage_script file ${stage_script}"
          touch $stage_script
          if [ ! -f ${stage_script_runner} ]; then
            echo "Creating stage script runner ${STAGE} and runner file name ${stage_script_runner}"
            echo \#\!/bin/bash > "${stage_script_runner}"
            echo >> "${stage_script_runner}"
            echo "${stage_script}" >> "${stage_script_runner}"
          fi
          cat ${stage_script_runner}
          chmod +x ${stage_script_runner}

          stage_script=$stage_script

          generate_user_script "${stage_script}" script "${STAGE}" "${TASK_NAME}"
          chmod +x ${stage_script}
        done


        . "${ONE_PIPELINE_PATH}"/internal/stage/prepare
    - name: setup
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[0])
        - name: STAGE_SCRIPT_PATH
          value: $(params.script[0])
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: STEP_NAME
          value: setup
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[0])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[0])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_setup"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        exit $exit_code
      onError: stopAndFail
    - name: unit-test
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[1])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_test
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: STEP_NAME
          value: unit-test
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[1])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[1])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_unit_test"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: build-artifact
      displayName: winston
      image: mirror.gcr.io/library/node:24
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[2])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_build-artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: STEP_NAME
          value: build-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[2])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[2])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_build_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run


        ENABLE_SLSA_ATTESTATION=$(get_env "slsa-attestation" 0)

        if [ "${ENABLE_SLSA_ATTESTATION}" -ne 0 ]; then
          if [ "$STAGE" = "build-artifact" ]; then
            echo -n "${image_list}" > $(results.IMAGES.path)
          fi
        fi


        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: stopAndFail
    - name: scan-artifact
      image: icr.io/continuous-delivery/pipeline/pipeline-base-ubi:3.83
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: $(params.stage[4])
        - name: STAGE_SCRIPT_PATH
          value: stage_script_runner_scan_artifact
        - name: STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: STEP_NAME
          value: scan-artifact
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-$(params.stage[4])
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-$(params.stage[4])
        - name: docker-socket
          mountPath: /var/run/
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/sh

        . "${ONE_PIPELINE_PATH}"/tools/capture_log

        export STAGE_SCRIPT_PATH="stage_script_runner_scan_artifact"

        . "${ONE_PIPELINE_PATH}"/internal/stage/run

        current_stage="$(printf '%s\n' "$STAGE" | awk '{ print toupper($0) }' |
        sed 's/-/_/g')"

        exit_code=$(get_env STAGE_"${current_stage}"_EXIT_CODE 0)

        rm -rf $STAGE_WORKSPACE/*

        if [ $exit_code -eq 51 ]; then
          set_env fatal_error "true"
        fi

        exit $exit_code
      onError: continue
    - name: collect-evidence
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STAGE
          value: collect-evidence
        - name: SETUP_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[0])
        - name: TEST_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[1])
        - name: CONTAINERIZE_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[2])
        - name: SIGN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[3])
        - name: SCAN_ARTIFACT_STAGE_ABORT_ON_FAILURE
          value: $(params.abort-on-failure[4])
        - name: ON_ERROR
        - name: STEP_NAME
          value: collect-evidence
        - name: STAGE_RUN_AFTER
        - name: STAGE_WORKSPACE
          value: /stage-collect-evidence
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: pipeline-scripts
          mountPath: /opt/one-pipeline
        - name: commons-library
          mountPath: /opt/commons
        - name: pipelinectl
          mountPath: /opt/cocoa
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
        - name: dind-certs
          mountPath: /certs/client
        - name: tmp
          mountPath: /tmp
        - name: working-dir
          mountPath: /stage-collect-evidence
      imagePullPolicy: IfNotPresent
      script: >
        #!/usr/bin/bash

        export PATH="$PATH:/opt/cocoa/bin"


        FATAL_ERROR="$(get_env fatal_error "false")"

        if [ "$FATAL_ERROR" == "true" ]; then
          set_env fatal_error "true"
          echo "Fatal error occured. Killing the pipeline"
          exit 51
        fi


        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.ARTIFACTS.path)
          rm ./args.json.gz
        fi
  volumes:
    - name: config
      emptyDir: {}
    - name: pipeline-scripts
      emptyDir: {}
    - name: commons-library
      emptyDir: {}
    - name: pipelinectl
      emptyDir: {}
    - name: dind-certs
      emptyDir: {}
    - name: tmp
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
    - name: dind-storage
      emptyDir: {}
    - name: docker-socket
      emptyDir: {}
  stepTemplate:
    env:
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: WORKSPACE
        value: /workspace/app
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: TASK_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/taskRunUID']
      - name: TOOLCHAIN_DATA_PATH
        value: /toolchain/toolchain.json
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: PIPELINE_VERSION
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/compliance-pipeline-definitions']
      - name: PIPELINE_VARIANT
        value: v11
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
    computeResources: {}
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
  sidecars:
    - name: dind
      image: $(params.dind-image)
      computeResources: {}
      volumeMounts:
        - name: dind-storage
          mountPath: /var/lib/docker
        - name: docker-socket
          mountPath: /var/run/
        - name: dind-certs
          mountPath: /certs/client
        - name: workspace
          mountPath: /workspace/app
        - name: tmp
          mountPath: /tmp
      readinessProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        initialDelaySeconds: 5
        timeoutSeconds: 7
      startupProbe:
        exec:
          command:
            - docker
            - stats
            - '--no-stream'
        timeoutSeconds: 7
        failureThreshold: 60
      imagePullPolicy: IfNotPresent
      securityContext:
        privileged: true
      script: |
        mkdir -p /etc/docker
        touch /etc/docker/daemon.json
        echo "{\"log-level\": \"debug\"}" > /etc/docker/daemon.json
        exec /usr/local/bin/dockerd-entrypoint.sh --mtu=1400
  results:
    - name: IMAGES
      type: string
    - name: ARTIFACTS
      type: string
---
kind: Task
apiVersion: tekton.dev/v1
metadata:
  name: ci-start
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: 59e5289b-bc9b-4c7f-9643-a552898d08e8
  resourceVersion: '1298957120'
  generation: 1
  creationTimestamp: '2026-08-26T11:12:43Z'
  annotations:
    devops.cloud.ibm.com/platform-controlled-steps: start
    devops.cloud.ibm.com/preprocessor: enabled
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: tekton.dev/v1
      time: '2026-08-26T11:12:43Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:devops.cloud.ibm.com/platform-controlled-steps: {}
            f:devops.cloud.ibm.com/preprocessor: {}
        f:spec:
          .: {}
          f:params: {}
          f:results: {}
          f:stepTemplate:
            .: {}
            f:computeResources: {}
            f:env: {}
            f:volumeMounts: {}
          f:steps: {}
          f:volumes: {}
spec:
  params:
    - name: branch
      type: string
      description: branch
      default: ''
    - name: commit-id
      type: string
      description: commit id
    - name: commit-timestamp
      type: string
      description: the timestamp of when the commit pushed
    - name: pr-url
      type: string
      description: PR url
    - name: repository-integration
      type: string
      description: the repo integration name
    - name: repository
      type: string
      description: app repository owner and name
    - name: compliance-baseimage
      type: string
      description: base image to run most of the built-in pipeline code
    - name: pipeline-debug
      type: string
      description: Pipeline debug mode
      default: '0'
    - name: collect-metrics
      type: string
      description: Enable metrics collection
      default: '1'
    - name: next-stage
      type: string
      default: ''
    - name: final-stage
      type: string
      default: ''
  steps:
    - name: start
      image: $(params.compliance-baseimage)
      workingDir: /workspace/app
      env:
        - name: STEP_NAME
          value: start
        - name: REPOSITORY_INTEGRATION
          value: $(params.repository-integration)
        - name: COMMIT_ID
          value: $(params.commit-id)
        - name: COMMIT_TIMESTAMP
          value: $(params.commit-timestamp)
        - name: PR_URL
          value: $(params.pr-url)
        - name: REPOSITORY
          value: $(params.repository)
        - name: BRANCH
          value: $(params.branch)
      computeResources: {}
      volumeMounts:
        - name: config
          mountPath: /config
        - name: environment-properties
          mountPath: /config/environment-properties
        - name: secure-properties
          mountPath: /config/secure-properties
        - name: toolchain
          mountPath: /toolchain
      imagePullPolicy: IfNotPresent
      script: >
        #!/bin/bash

        . "${ONE_PIPELINE_PATH}"/tools/capture_log


        echo -n "[]" > $(results.REPOS.path)

        export PATH="$PATH:/opt/cocoa/bin"

        BRANCH=${BRANCH#"refs/heads/"}

        BRANCH=${BRANCH#"refs/tags/"}

        echo -n "$BRANCH" >> /config/branch

        echo -n "${REPOSITORY_INTEGRATION}" >> /config/repository-integration

        echo -n "${COMMIT_ID}" >> /config/commit-sha

        echo -n "${COMMIT_TIMESTAMP}" >> /config/commit-timestamp

        echo -n "${PR_URL}" >> /config/pr-url

        echo -n "${REPOSITORY}" >> /config/repository

        set_env branch "$BRANCH"

        set_env repository-integration "${REPOSITORY_INTEGRATION}"

        set_env commit-sha "${COMMIT_ID}"

        set_env commit-timestamp "${COMMIT_TIMESTAMP}"

        set_env pr-url "${PR_URL}"

        set_env repository "${REPOSITORY}"


        # Capture the exit code and send the exit code to task result

        if "${ONE_PIPELINE_PATH}/internal/ci_start"; then
          start_exit_code=$?
          echo -n "${start_exit_code}" > $(results.task-exit-code.path)
        else
          start_exit_code=$?
          echo -n "${start_exit_code}" > $(results.task-exit-code.path)
        fi


        if [ "${start_exit_code}" == "51" ]; then
          echo "Irrecoverable failure occured. Killing the pipeline"
          set_env fatal_error "true"
          exit ${start_exit_code}
        fi


        echo "serialize all the repos and env params here..."

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_results_utils


        init_data_cos_env

        if validate_cos_config $DATA_COS_API_KEY $DATA_COS_ENDPOINT
        $DATA_COS_BUCKET_NAME; then
          debug "Found pipeline data COS. Using COS to store pipeline data"
          upload_to_cos
        else
          debug "Not found pipeline data COS. Please configure COS to store pipeline data"
          exclude_file_path=$(mktemp)
          cat "${ONE_PIPELINE_PATH}/internal/tools/serialize_exclude_patterns.md" > "$exclude_file_path"
          custom_exclude="$(get_env "serialize_exclude_keys" "")"
          if [ -f "$custom_exclude" ]; then
            cat "$custom_exclude" >> "$exclude_file_path"
          fi
          # shellcheck disable=SC2046
          env_key_list=$(list_env | grep -v -f "$exclude_file_path")
          # shellcheck disable=SC2046
          pipelinectl serialize --all-repos --all-artifacts $env_key_list > ./args.json
          gzip ./args.json
          cat ./args.json.gz | base64 > $(results.REPOS.path)
          rm ./args.json.gz
        fi

        source "${ONE_PIPELINE_PATH}"/internal/tools/pipeline_utils

        summarize_metric || true

        exit ${exit_code}
      onError: continue
  volumes:
    - name: config
      emptyDir: {}
    - name: environment-properties
      configMap:
        name: environment-properties
    - name: secure-properties
      secret:
        secretName: secure-properties
    - name: toolchain
      configMap:
        name: toolchain
    - name: event-parameters
      configMap:
        name: event-parameters
        items:
          - key: eventBody
            path: payload.json
          - key: eventHeader
            path: header.json
        optional: true
    - name: workspace
      emptyDir: {}
    - name: working-dir
      emptyDir: {}
  stepTemplate:
    env:
      - name: ONE_PIPELINE_PATH
        value: /opt/one-pipeline
      - name: COMMONS_PATH
        value: /opt/commons
      - name: WORKSPACE
        value: /workspace/app
      - name: PIPELINE_RUN_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineRun']
      - name: PIPELINE_RUN_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/tekton-pipeline']
      - name: PIPELINE_RUN_URL
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-run-url']
      - name: BUILD_NUMBER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/build-number']
      - name: PIPELINE_ID
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/pipeline-id']
      - name: TRIGGER_TYPE
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-type']
      - name: TRIGGER_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/trigger-name']
      - name: TRIGGERED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/triggered-by']
      - name: PLATFORM_CONTROLLED_STEPS
        valueFrom:
          fieldRef:
            fieldPath: >-
              metadata.annotations['devops.cloud.ibm.com/platform-controlled-steps']
      - name: TRIGGER_PAYLOAD_PATH
        value: /trigger-payload/payload.json
      - name: PIPELINE_DEBUG
        value: $(params.pipeline-debug)
      - name: COLLECT_METRIC
        value: $(params.collect-metrics)
      - name: PIPELINE_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipeline']
      - name: COMPLIANCE_BASEIMAGE
        value: $(params.compliance-baseimage)
      - name: TASK_NAME
        valueFrom:
          fieldRef:
            fieldPath: metadata.labels['tekton.dev/pipelineTask']
      - name: OWNED_BY
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/owned-by']
      - name: LISTENER
        valueFrom:
          fieldRef:
            fieldPath: metadata.annotations['devops.cloud.ibm.com/listener']
      - name: PIPELINE_VARIANT
        value: v11
    computeResources: {}
    volumeMounts:
      - name: event-parameters
        mountPath: /trigger-payload
      - name: workspace
        mountPath: /workspace/app
  results:
    - name: image
      type: string
      description: Docker image to be used in the specified stage.
    - name: script
      type: string
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: configmap
      type: string
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: secret
      type: string
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: working-dir
      type: string
      description: one-pipeline working dir, the location of pipeline-config.yaml
    - name: abort-on-failure
      type: string
      description: Whether abort the pipeline in case of a failure or not
    - name: dind
      type: string
      description: Launch Docker-in-Docker sidecar
    - name: image-pull-policy
      type: string
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: stage-skip
      type: string
      description: Whether the stage needs to skipped if possible
    - name: final-stage-image
      type: string
      description: Docker image to be used in the specified stage.
    - name: final-stage-script
      type: string
      description: >-
        Location of the script that's going to be used to run the specified
        stage.
    - name: final-stage-configmap
      type: string
      description: Name of the `ConfigMap` that's going to be mounted in `run-stage`
    - name: final-stage-secret
      type: string
      description: Name of the `Secret` that's going to be mounted in `run-stage`
    - name: final-stage-abort-on-failure
      type: string
      description: Whether abort the pipeline in case of a failure or not
    - name: final-stage-dind
      type: string
      description: Launch Docker-in-Docker sidecar
    - name: final-stage-image-pull-policy
      type: string
      description: Set the imagePullPolicy for the Docker image in the runner task
    - name: final-stage-skip
      type: string
      description: Whether the stage needs to skipped if possible
    - name: run-after
      type: string
      description: >-
        Specify the previous stage that needs to be complete before starting
        next stage
    - name: REPOS
      type: string
    - name: task-exit-code
      type: string
      description: Initialize to kill the pipeline and and when success reset
---
kind: AgentLog
apiVersion: devops.cloud.ibm.com/v1alpha1
metadata:
  name: steplog
  namespace: pw-c5bc3783-9c47-4a5f-bd97-f75e541644d6
  uid: 5e4bc328-7a1b-4501-8e95-c1e6121d8800
  resourceVersion: '1298957182'
  generation: 7
  creationTimestamp: '2026-08-26T11:12:33Z'
  managedFields:
    - manager: manager
      operation: Update
      apiVersion: devops.cloud.ibm.com/v1alpha1
      time: '2026-08-26T11:12:44Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:spec:
          .: {}
          f:steplogs: {}
spec:
  steplogs:
    - type: INFO
      logs:
        - timestamp: '2026-08-26T11:12:33Z'
          log: '::group::INFO'
        - timestamp: '2026-08-26T11:12:33Z'
          log: '::info::Running on cluster: otc-tektonpw-9x-dal03-1'
        - timestamp: '2026-08-26T11:12:33Z'
          log: '::info::Current Agent Version: 0.22.15'
        - timestamp: '2026-08-26T11:12:33Z'
          log: '::endgroup::'
    - type: GIT
      logs:
        - timestamp: '2026-08-26T11:12:33Z'
          log: '::group::GIT'
        - timestamp: '2026-08-26T11:12:33Z'
          log: '::info::Finding repos to clone, 1 definition(s)'
        - timestamp: '2026-08-26T11:12:33Z'
          log: '::info::Processing 1 git repos'
        - timestamp: '2026-08-26T11:12:33Z'
          log: >-
            ::info::Cloning
            https//github.ibm.com/one-pipeline/compliance-pipelines.git : v11
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::info::Repo successfully cloned...'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::info::Kustomization file detected: false'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::endgroup::'
    - type: KUSTOMIZE
      logs:
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::group::KUSTOMIZE'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::info::Finding repos to clone, 1 definition(s)'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::info::No Kustomization detected in repo 0'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::endgroup::'
    - type: EXTERNAL_SECRETS
      logs:
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::group::EXTERNAL_SECRETS'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::info::Processing external secrets'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::endgroup::'
    - type: TRIGGERS
      logs:
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::group::TRIGGERS'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::info::Processing triggers'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::info::Reading event-parameters'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::info::Reading environment-properties'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::info::Reading secure-properties-env'
        - timestamp: '2026-08-26T11:12:34Z'
          log: '::endgroup::'
    - type: PREPROCESSOR
      logs:
        - timestamp: '2026-08-26T11:12:35Z'
          log: '::group::PREPROCESSOR'
        - timestamp: '2026-08-26T11:12:35Z'
          log: >-
            ::info::Executing pipeline "ci-pipeline" as specified in the trigger
            payload.
        - timestamp: '2026-08-26T11:12:35Z'
          log: >-
            ::debug::Extra pipeline definitions have been removed and will not
            be deployed.
        - timestamp: '2026-08-26T11:12:35Z'
          log: >-
            ::debug::Pipeline configuration file configured through environment
            property "pipeline-config" :
            ".sps/pipeline-config-collector-logging.yaml"
        - timestamp: '2026-08-26T11:12:35Z'
          log: >-
            ::debug::Pipeline configuration repository configured through
            environment property "repository" :
            "https://github.com/instana/nodejs"
        - timestamp: '2026-08-26T11:12:35Z'
          log: >-
            ::debug::Pipeline configuration branch configured through
            environment property "pipeline-config-branch" : "main"
        - timestamp: '2026-08-26T11:12:35Z'
          log: '::info::Compliance Pipeline Definition: v11'
        - timestamp: '2026-08-26T11:12:35Z'
          log: '::info::Pipeline Config Repo: https://github.com/instana/nodejs'
        - timestamp: '2026-08-26T11:12:35Z'
          log: '::info::Pipeline Config Branch: main'
        - timestamp: '2026-08-26T11:12:35Z'
          log: >-
            ::info::Pipeline Config File:
            .sps/pipeline-config-collector-logging.yaml
        - timestamp: '2026-08-26T11:12:35Z'
          log: '::info::InferredDevSecOps Enabled: false'
        - timestamp: '2026-08-26T11:12:35Z'
          log: >-
            ::info::Cloning pipeline configuration file
            ".sps/pipeline-config-collector-logging.yaml" from
            "https://github.com/instana/nodejs" : "main"
        - timestamp: '2026-08-26T11:12:38Z'
          log: >-
            ::info::Successfully cloned the pipeline config repository
            'https://github.com/instana/nodejs' and branch 'main'
        - timestamp: '2026-08-26T11:12:38Z'
          log: '::info::Pipeline Configuration Version in Config File: 2'
        - timestamp: '2026-08-26T11:12:38Z'
          log: '::info::Preprocessor Enabled: true'
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Initiating parsing of the pipeline config file into the
            Pipeline Configuration V2 structure
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Successfully parsed the pipeline config file into the
            Pipeline Configuration V2 structure
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::Pipeline configuration file may include specifications for:'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::• tasks:'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - ci-start'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - code-checks'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - code-build'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - deploy-checks'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - deploy-release'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::• finally:'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - code-ci-finish'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::• steps within ''deploy-checks'':'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - deploy'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - dynamic-scan'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - acceptance-test'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::• steps within ''deploy-release'':'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - run-stage'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::• steps within ''code-ci-finish'':'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - run-stage'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::• steps within ''code-checks'':'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - checks-setup'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - detect-secrets'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - compliance-checks'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - peer-review'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - static-scan'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::• steps within ''code-build'':'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - setup'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - unit-test'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - build-artifact'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - sign-artifact'
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::  - scan-artifact'
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Task 'deploy-checks' has been removed from the pipeline
            because the specified condition 'false' evaluated to false
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Task 'deploy-checks' has been removed from the pipeline, and
            as a result, its dependencies '(deploy-release)' will also be
            removed.
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Task 'deploy-release' has been removed from the pipeline
            because the specified condition 'false' evaluated to false
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Task 'deploy-release' has been removed from the pipeline,
            and as a result, its dependencies '()' will also be removed.
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::setting the runtimeClassName to "large" for "code-build"'
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Discovered task 'pr-messaging-console' in the pipeline
            configuration file for fan-out execution using 'code-build'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::setting the displayName of "pr-messaging-console" to
            "console"
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::setting the runtimeClassName to "large" for
            "pr-messaging-console"
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Task 'code-build' has been replicated to
            'pr-messaging-console' so that it can be referenced from a fan out
            instance 'pr-messaging-console'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Fanned-out task 'pr-messaging-console' has been added to the
            pipeline
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Discovered task 'pr-messaging-express-winston' in the
            pipeline configuration file for fan-out execution using
            'code-build'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::setting the displayName of "pr-messaging-express-winston" to
            "express-winston"
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::setting the runtimeClassName to "large" for
            "pr-messaging-express-winston"
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Task 'code-build' has been replicated to
            'pr-messaging-express-winston' so that it can be referenced from a
            fan out instance 'pr-messaging-express-winston'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Fanned-out task 'pr-messaging-express-winston' has been
            added to the pipeline
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Discovered task 'pr-messaging-pino' in the pipeline
            configuration file for fan-out execution using 'code-build'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::setting the displayName of "pr-messaging-pino" to "pino"'
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::setting the runtimeClassName to "large" for
            "pr-messaging-pino"
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Task 'code-build' has been replicated to 'pr-messaging-pino'
            so that it can be referenced from a fan out instance
            'pr-messaging-pino'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Fanned-out task 'pr-messaging-pino' has been added to the
            pipeline
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Discovered task 'pr-messaging-bunyan' in the pipeline
            configuration file for fan-out execution using 'code-build'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::setting the displayName of "pr-messaging-bunyan" to "bunyan"'
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::setting the runtimeClassName to "large" for
            "pr-messaging-bunyan"
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Task 'code-build' has been replicated to
            'pr-messaging-bunyan' so that it can be referenced from a fan out
            instance 'pr-messaging-bunyan'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Fanned-out task 'pr-messaging-bunyan' has been added to the
            pipeline
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Discovered task 'pr-messaging-log4js' in the pipeline
            configuration file for fan-out execution using 'code-build'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: '::info::setting the displayName of "pr-messaging-log4js" to "log4js"'
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::setting the runtimeClassName to "large" for
            "pr-messaging-log4js"
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Task 'code-build' has been replicated to
            'pr-messaging-log4js' so that it can be referenced from a fan out
            instance 'pr-messaging-log4js'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Fanned-out task 'pr-messaging-log4js' has been added to the
            pipeline
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Discovered task 'pr-messaging-winston' in the pipeline
            configuration file for fan-out execution using 'code-build'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::setting the displayName of "pr-messaging-winston" to
            "winston"
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::setting the runtimeClassName to "large" for
            "pr-messaging-winston"
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Task 'code-build' has been replicated to
            'pr-messaging-winston' so that it can be referenced from a fan out
            instance 'pr-messaging-winston'.
        - timestamp: '2026-08-26T11:12:39Z'
          log: >-
            ::info::Fanned-out task 'pr-messaging-winston' has been added to the
            pipeline
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'peer-review' has been removed from the task
            'code-checks' because the specified condition 'false' evaluated to
            false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Task 'code-checks' has been updated to include all
            specifications from task 'dind'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'checks-setup' in task 'code-checks' has been updated
            to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'checks-setup' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'detect-secrets' in task 'code-checks' has been updated
            to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'detect-secrets' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'compliance-checks' in task 'code-checks' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'compliance-checks' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'static-scan' in task 'code-checks' has been updated to
            include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'static-scan' has been updated to include 'dind', based
            on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' has been removed from the task 'code-build'
            because the specified condition 'false' evaluated to false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'sign-artifact' has been removed from the task
            'code-build' because the specified condition 'false' evaluated to
            false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' has been removed from the task
            'code-build' because the specified condition 'false' evaluated to
            false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Task 'code-build' has been updated to include all
            specifications from task 'dind'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' in task 'code-build' has been updated to
            include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' has been updated to include 'dind', based on
            the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' in task 'code-build' has been updated
            to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'sign-artifact' has been removed from the task
            'pr-messaging-console' because the specified condition 'false'
            evaluated to false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Task 'pr-messaging-console' has been updated to include all
            specifications from task 'dind'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' in task 'pr-messaging-console' has been updated
            to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' has been updated to include 'dind', based on
            the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' in task 'pr-messaging-console' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' has been updated to include 'dind', based
            on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' in task 'pr-messaging-console' has
            been updated to include all specifications from step
            'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' in task 'pr-messaging-console' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'sign-artifact' has been removed from the task
            'pr-messaging-express-winston' because the specified condition
            'false' evaluated to false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Task 'pr-messaging-express-winston' has been updated to
            include all specifications from task 'dind'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' in task 'pr-messaging-express-winston' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' has been updated to include 'dind', based on
            the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' in task 'pr-messaging-express-winston' has
            been updated to include all specifications from step
            'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' has been updated to include 'dind', based
            on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' in task 'pr-messaging-express-winston'
            has been updated to include all specifications from step
            'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' in task 'pr-messaging-express-winston'
            has been updated to include all specifications from step
            'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'sign-artifact' has been removed from the task
            'pr-messaging-pino' because the specified condition 'false'
            evaluated to false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Task 'pr-messaging-pino' has been updated to include all
            specifications from task 'dind'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' in task 'pr-messaging-pino' has been updated to
            include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' has been updated to include 'dind', based on
            the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' in task 'pr-messaging-pino' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' has been updated to include 'dind', based
            on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' in task 'pr-messaging-pino' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' in task 'pr-messaging-pino' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'sign-artifact' has been removed from the task
            'pr-messaging-bunyan' because the specified condition 'false'
            evaluated to false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Task 'pr-messaging-bunyan' has been updated to include all
            specifications from task 'dind'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' in task 'pr-messaging-bunyan' has been updated
            to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' has been updated to include 'dind', based on
            the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' in task 'pr-messaging-bunyan' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' has been updated to include 'dind', based
            on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' in task 'pr-messaging-bunyan' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' in task 'pr-messaging-bunyan' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'sign-artifact' has been removed from the task
            'pr-messaging-log4js' because the specified condition 'false'
            evaluated to false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Task 'pr-messaging-log4js' has been updated to include all
            specifications from task 'dind'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' in task 'pr-messaging-log4js' has been updated
            to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' has been updated to include 'dind', based on
            the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' in task 'pr-messaging-log4js' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' has been updated to include 'dind', based
            on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' in task 'pr-messaging-log4js' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' in task 'pr-messaging-log4js' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'sign-artifact' has been removed from the task
            'pr-messaging-winston' because the specified condition 'false'
            evaluated to false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Task 'pr-messaging-winston' has been updated to include all
            specifications from task 'dind'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' in task 'pr-messaging-winston' has been updated
            to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'setup' has been updated to include 'dind', based on
            the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' in task 'pr-messaging-winston' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'unit-test' has been updated to include 'dind', based
            on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' in task 'pr-messaging-winston' has
            been updated to include all specifications from step
            'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'build-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' in task 'pr-messaging-winston' has been
            updated to include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'scan-artifact' has been updated to include 'dind',
            based on the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'run-stage' has been removed from the task 'ci-finish'
            because the specified condition 'false' evaluated to false
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Task 'ci-finish' has been updated to include all
            specifications from task 'dind'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'evaluate' in task 'ci-finish' has been updated to
            include all specifications from step 'docker-socket'.
        - timestamp: '2026-08-26T11:12:40Z'
          log: >-
            ::info::Step 'evaluate' has been updated to include 'dind', based on
            the default settings from the compliance pipelines
        - timestamp: '2026-08-26T11:12:41Z'
          log: '::endgroup::PREPROCESSOR'
    - type: CREATE
      logs:
        - timestamp: '2026-08-26T11:12:41Z'
          log: '::group::CREATE'
        - timestamp: '2026-08-26T11:12:41Z'
          log: '::info::Using passed in ns'
        - timestamp: '2026-08-26T11:12:41Z'
          log: '::info::Creating Task pr-messaging-console'
        - timestamp: '2026-08-26T11:12:41Z'
          log: '::info::Creating Task pr-messaging-express-winston'
        - timestamp: '2026-08-26T11:12:42Z'
          log: '::info::Creating Task pr-messaging-pino'
        - timestamp: '2026-08-26T11:12:42Z'
          log: '::info::Creating Task pr-messaging-bunyan'
        - timestamp: '2026-08-26T11:12:42Z'
          log: '::info::Creating Task pr-messaging-log4js'
        - timestamp: '2026-08-26T11:12:43Z'
          log: '::info::Creating Task pr-messaging-winston'
        - timestamp: '2026-08-26T11:12:43Z'
          log: '::info::Creating Pipeline ci-pipeline'
        - timestamp: '2026-08-26T11:12:43Z'
          log: '::info::Creating Task ci-finish'
        - timestamp: '2026-08-26T11:12:43Z'
          log: '::info::Creating Task ci-start'
        - timestamp: '2026-08-26T11:12:44Z'
          log: '::info::Creating Task code-build'
        - timestamp: '2026-08-26T11:12:44Z'
          log: '::info::Creating Task code-checks'
        - timestamp: '2026-08-26T11:12:44Z'
          log: '::info::Creating Secret artifactory-pull-secret'
        - timestamp: '2026-08-26T11:12:44Z'
          log: '::info::Creating Secret onepipeline-pull-secret'
        - timestamp: '2026-08-26T11:12:44Z'
          log: '::info::Creating Secret one-pipeline-pull-secret'
        - timestamp: '2026-08-26T11:12:44Z'
          log: '::info::Creating Secret pipeline-pull-secret'
        - timestamp: '2026-08-26T11:12:44Z'
          log: '::info::Creating Secret icr-pull-secret'
        - timestamp: '2026-08-26T11:12:44Z'
          log: '::info::Creating ServiceAccount docker-build-sa'
        - timestamp: '2026-08-26T11:12:44Z'
          log: '::info::Creating PipelineRun ci-pipelinerun'
        - timestamp: '2026-08-26T11:12:44Z'
          log: '::endgroup::'
  version: '2'
status: {}

version: '2'
tasks:
# code-checks is skipped intentionally — this checks runs on dedicated security-check pipe only.
# code-checks includes five steps: setup, detect-secrets, compliance-checks, peer-review, static-scan
  code-checks:
    when: false
  code-build:
    runtimeClassName: large
    steps:
      - name: unit-test
        displayName: npm-install
        image: mirror.gcr.io/library/node:24
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail
          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "")")}"
          curl -fsSL https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
          export NVM_DIR="$HOME/.nvm"
          # shellcheck source=/dev/null
          . "$NVM_DIR/nvm.sh"
          nvm install "$node_version" --no-progress
          nvm use "$node_version"
          echo "Node.js: $(node --version)"

          cd "$WORKSPACE/$(load_repo app-repo path)"
          npm install --loglevel warn --foreground-scripts
          node bin/create-version-test-folders.js
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-pr-finish:
    when: 'false'
  code-ci-finish:
    when: 'false'
  deploy-checks:
    when: false
  deploy-release:
    when: false
  code-build-collector-async-await:
    from: code-build
    displayName: await
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: await
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          REPO_PATH="$WORKSPACE/$(load_repo app-repo path)"
          cd "$REPO_PATH"
          echo "REPO_PATH: $REPO_PATH"

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          echo "Node version: $node_version (major: $NODE_MAJOR)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          RUN_ESM="$(get_env RUN_ESM "")"

          # ------------------------------------------------------------
          # Build egress-proxy image (allows only registry.npmjs.org:443)
          # ------------------------------------------------------------

          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # ------------------------------------------------------------
          # Create network + start Squid proxy sidecar
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route
          # their own traffic through Squid.
          # ------------------------------------------------------------

          docker network create npm-net

          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local

          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          echo "Node.js inside container:"
          docker exec test-runner node --version

          docker exec test-runner \
            npm install --loglevel warn

          # generate versioned test folders — must run before TEST_FILES discovery
          docker exec test-runner \
            node bin/create-version-test-folders.js

          # ------------------------------------------------------------
          # Collect test files (after versioned folders are generated)
          # ------------------------------------------------------------

          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find test/integration/currencies/async/await \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '" || true)
          echo "TEST_FILES: $TEST_FILES"

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for await — skipping.'
            docker rm -f test-runner || true
            docker network rm test-net || true
            exit 0
          fi

          # ------------------------------------------------------------
          # Run tests (Isolated on test-net --internal, proxy vars cleared)
          # ------------------------------------------------------------

          LAST_EXIT=0

          retry=1
          while [ "$retry" -le 2 ]; do
            LAST_EXIT=0

            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci \
                --npm_command="test:ci:collector" \
                --report_dir="collector-async-await" \
              || LAST_EXIT=$?

            if [ "$LAST_EXIT" -eq 0 ]; then
              break
            fi

            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # ------------------------------------------------------------
          # Cleanup
          # ------------------------------------------------------------

          docker rm -f test-runner || true
          docker network rm test-net || true

          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-async-await.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-async-await → test-results/$GIT_COMMIT/collector-async-await.txt" \
              || echo "WARNING: Failed to upload test list for collector-async-await (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-async-await/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-async-await/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-async-await → test-results/$GIT_COMMIT/coverage/collector-async-await/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-async-await (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi

          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-async-bluebird:
    from: code-build
    displayName: bluebird
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: bluebird
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          REPO_PATH="$WORKSPACE/$(load_repo app-repo path)"
          cd "$REPO_PATH"
          echo "REPO_PATH: $REPO_PATH"

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          echo "Node version: $node_version (major: $NODE_MAJOR)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          RUN_ESM="$(get_env RUN_ESM "")"

          # ------------------------------------------------------------
          # Build egress-proxy image (allows only registry.npmjs.org:443)
          # ------------------------------------------------------------

          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # ------------------------------------------------------------
          # Create networks + start Squid proxy sidecar
          # ------------------------------------------------------------

          docker network create npm-net
          docker network create --internal test-net

          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local

          # ------------------------------------------------------------
          # Start test-runner — outbound npm traffic routed through proxy
          # ------------------------------------------------------------

          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          echo "Node.js inside container:"
          docker exec test-runner node --version

          # ------------------------------------------------------------
          # Dependency installation (proxy enforces registry.npmjs.org only)
          # ------------------------------------------------------------

          docker exec test-runner \
            npm install --loglevel warn

          # generate versioned test folders — must run before TEST_FILES discovery
          docker exec test-runner \
            node bin/create-version-test-folders.js

          # ------------------------------------------------------------
          # Transition test-runner to isolated test-net
          # (the _v*/ npm installs run at test-time via --prefer-offline cache)
          # ------------------------------------------------------------

          docker network connect test-net test-runner
          docker network disconnect npm-net test-runner
          docker rm -f npm-proxy || true
          docker network rm npm-net || true

          # ------------------------------------------------------------
          # Collect test files (after versioned folders are generated)
          # ------------------------------------------------------------

          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find test/integration/currencies/async/bluebird \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '" || true)
          echo "TEST_FILES: $TEST_FILES"

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for bluebird — skipping.'
            docker rm -f test-runner || true
            docker network rm test-net || true
            exit 0
          fi

          # ------------------------------------------------------------
          # Run tests (Isolated on test-net --internal, proxy vars cleared)
          # ------------------------------------------------------------

          LAST_EXIT=0

          retry=1
          while [ "$retry" -le 2 ]; do
            LAST_EXIT=0

            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci \
                --npm_command="test:ci:collector" \
                --report_dir="collector-async-bluebird" \
              || LAST_EXIT=$?

            if [ "$LAST_EXIT" -eq 0 ]; then
              break
            fi

            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # ------------------------------------------------------------
          # Cleanup
          # ------------------------------------------------------------

          docker rm -f test-runner || true
          docker network rm test-net || true

          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-async-bluebird.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-async-bluebird → test-results/$GIT_COMMIT/collector-async-bluebird.txt" \
              || echo "WARNING: Failed to upload test list for collector-async-bluebird (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-async-bluebird/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-async-bluebird/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-async-bluebird → test-results/$GIT_COMMIT/coverage/collector-async-bluebird/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-async-bluebird (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi

          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-async-native-promise:
    from: code-build
    displayName: native_promise
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: native_promise
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          REPO_PATH="$WORKSPACE/$(load_repo app-repo path)"
          cd "$REPO_PATH"
          echo "REPO_PATH: $REPO_PATH"

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          echo "Node version: $node_version (major: $NODE_MAJOR)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          RUN_ESM="$(get_env RUN_ESM "")"

          # ------------------------------------------------------------
          # Build egress-proxy image (allows only registry.npmjs.org:443)
          # ------------------------------------------------------------

          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # ------------------------------------------------------------
          # Create networks + start Squid proxy sidecar
          # ------------------------------------------------------------

          docker network create npm-net
          docker network create --internal test-net

          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local

          # ------------------------------------------------------------
          # Start test-runner — outbound npm traffic routed through proxy
          # ------------------------------------------------------------

          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          echo "Node.js inside container:"
          docker exec test-runner node --version

          # ------------------------------------------------------------
          # Dependency installation (proxy enforces registry.npmjs.org only)
          # ------------------------------------------------------------

          docker exec test-runner \
            npm install --loglevel warn

          # generate versioned test folders — must run before TEST_FILES discovery
          docker exec test-runner \
            node bin/create-version-test-folders.js

          # ------------------------------------------------------------
          # Transition test-runner to isolated test-net
          # (the _v*/ npm installs run at test-time via --prefer-offline cache)
          # ------------------------------------------------------------

          docker network connect test-net test-runner
          docker network disconnect npm-net test-runner
          docker rm -f npm-proxy || true
          docker network rm npm-net || true

          # ------------------------------------------------------------
          # Collect test files (after versioned folders are generated)
          # ------------------------------------------------------------

          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find test/integration/currencies/async/native_promise \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '" || true)
          echo "TEST_FILES: $TEST_FILES"

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for native_promise — skipping.'
            docker rm -f test-runner || true
            docker network rm test-net || true
            exit 0
          fi

          # ------------------------------------------------------------
          # Run tests (Isolated on test-net --internal, proxy vars cleared)
          # ------------------------------------------------------------

          LAST_EXIT=0

          retry=1
          while [ "$retry" -le 2 ]; do
            LAST_EXIT=0

            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci \
                --npm_command="test:ci:collector" \
                --report_dir="collector-async-native-promise" \
              || LAST_EXIT=$?

            if [ "$LAST_EXIT" -eq 0 ]; then
              break
            fi

            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # ------------------------------------------------------------
          # Cleanup
          # ------------------------------------------------------------

          docker rm -f test-runner || true
          docker network rm test-net || true

          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-async-native-promise.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-async-native-promise → test-results/$GIT_COMMIT/collector-async-native-promise.txt" \
              || echo "WARNING: Failed to upload test list for collector-async-native-promise (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-async-native-promise/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-async-native-promise/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-async-native-promise → test-results/$GIT_COMMIT/coverage/collector-async-native-promise/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-async-native-promise (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi

          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'

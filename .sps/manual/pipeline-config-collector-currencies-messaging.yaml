version: '2'
tasks:
# code-checks is skipped intentionally — this checks runs on dedicated security-check pipe only.
# code-checks includes five steps: setup, detect-secrets, compliance-checks, peer-review, static-scan
  code-checks:
    when: false
  code-build:
    runtimeClassName: large
    steps:
      - name: unit-test
        displayName: npm-install
        image: mirror.gcr.io/library/node:24
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail
          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "")")}"
          curl -fsSL https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
          export NVM_DIR="$HOME/.nvm"
          # shellcheck source=/dev/null
          . "$NVM_DIR/nvm.sh"
          nvm install "$node_version" --no-progress
          nvm use "$node_version"
          echo "Node.js: $(node --version)"

          cd "$WORKSPACE/$(load_repo app-repo path)"
          npm install --loglevel warn --foreground-scripts
          node bin/create-version-test-folders.js
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-pr-finish:
    when: 'false'
  code-ci-finish:
    when: 'false'
  deploy-checks:
    when: false
  deploy-release:
    when: false
  code-build-collector-messaging-kafka-javascript:
    from: code-build
    displayName: '@confluentinc/kafka-javascript'
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: '@confluentinc/kafka-javascript'
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/@confluentinc/kafka-javascript \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for @confluentinc/kafka-javascript — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-kafka-javascript" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-kafka-javascript.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-kafka-javascript → test-results/$GIT_COMMIT/collector-messaging-kafka-javascript.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-kafka-javascript (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-kafka-javascript/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-kafka-javascript/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-kafka-javascript → test-results/$GIT_COMMIT/coverage/collector-messaging-kafka-javascript/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-kafka-javascript (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-amqplib:
    from: code-build
    displayName: amqplib
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: amqplib
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/amqplib \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for amqplib — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-amqplib" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-amqplib.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-amqplib → test-results/$GIT_COMMIT/collector-messaging-amqplib.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-amqplib (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-amqplib/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-amqplib/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-amqplib → test-results/$GIT_COMMIT/coverage/collector-messaging-amqplib/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-amqplib (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-bull:
    from: code-build
    displayName: bull
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: bull
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/bull \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for bull — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-bull" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-bull.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-bull → test-results/$GIT_COMMIT/collector-messaging-bull.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-bull (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-bull/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-bull/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-bull → test-results/$GIT_COMMIT/coverage/collector-messaging-bull/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-bull (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-kafka-node:
    from: code-build
    displayName: kafka-node
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: kafka-node
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/kafka-node \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for kafka-node — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-kafka-node" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-kafka-node.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-kafka-node → test-results/$GIT_COMMIT/collector-messaging-kafka-node.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-kafka-node (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-kafka-node/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-kafka-node/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-kafka-node → test-results/$GIT_COMMIT/coverage/collector-messaging-kafka-node/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-kafka-node (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-kafkajs:
    from: code-build
    displayName: kafkajs
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: kafkajs
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/kafkajs \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for kafkajs — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-kafkajs" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-kafkajs.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-kafkajs → test-results/$GIT_COMMIT/collector-messaging-kafkajs.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-kafkajs (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-kafkajs/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-kafkajs/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-kafkajs → test-results/$GIT_COMMIT/coverage/collector-messaging-kafkajs/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-kafkajs (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-nats:
    from: code-build
    displayName: nats
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: nats
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/nats \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for nats — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-nats" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-nats.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-nats → test-results/$GIT_COMMIT/collector-messaging-nats.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-nats (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-nats/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-nats/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-nats → test-results/$GIT_COMMIT/coverage/collector-messaging-nats/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-nats (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-node-nats-streaming:
    from: code-build
    displayName: node-nats-streaming
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: node-nats-streaming
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/node-nats-streaming \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for node-nats-streaming — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-node-nats-streaming" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-node-nats-streaming.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-node-nats-streaming → test-results/$GIT_COMMIT/collector-messaging-node-nats-streaming.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-node-nats-streaming (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-node-nats-streaming/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-node-nats-streaming/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-node-nats-streaming → test-results/$GIT_COMMIT/coverage/collector-messaging-node-nats-streaming/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-node-nats-streaming (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-node-rdkafka-1:
    from: code-build
    displayName: node-rdkafka-1
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: node-rdkafka-1
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/node-rdkafka/_v3.6.1/withDeliveryCbAndStandardProducer \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for node-rdkafka-1 — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-node-rdkafka-1" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-node-rdkafka-1.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-node-rdkafka-1 → test-results/$GIT_COMMIT/collector-messaging-node-rdkafka-1.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-node-rdkafka-1 (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-node-rdkafka-1/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-node-rdkafka-1/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-node-rdkafka-1 → test-results/$GIT_COMMIT/coverage/collector-messaging-node-rdkafka-1/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-node-rdkafka-1 (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-node-rdkafka-2:
    from: code-build
    displayName: node-rdkafka-2
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: node-rdkafka-2
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/node-rdkafka/_v3.6.1/withDeliveryCbAndStreamProducer \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for node-rdkafka-2 — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-node-rdkafka-2" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-node-rdkafka-2.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-node-rdkafka-2 → test-results/$GIT_COMMIT/collector-messaging-node-rdkafka-2.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-node-rdkafka-2 (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-node-rdkafka-2/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-node-rdkafka-2/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-node-rdkafka-2 → test-results/$GIT_COMMIT/coverage/collector-messaging-node-rdkafka-2/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-node-rdkafka-2 (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-node-rdkafka-3:
    from: code-build
    displayName: node-rdkafka-3
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: node-rdkafka-3
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/node-rdkafka/_v3.6.1/withoutDeliveryCbAndStandardProducer \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for node-rdkafka-3 — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-node-rdkafka-3" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-node-rdkafka-3.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-node-rdkafka-3 → test-results/$GIT_COMMIT/collector-messaging-node-rdkafka-3.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-node-rdkafka-3 (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-node-rdkafka-3/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-node-rdkafka-3/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-node-rdkafka-3 → test-results/$GIT_COMMIT/coverage/collector-messaging-node-rdkafka-3/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-node-rdkafka-3 (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-node-rdkafka-4:
    from: code-build
    displayName: node-rdkafka-4
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: node-rdkafka-4
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/node-rdkafka/_v3.6.1/withoutDeliveryCbAndStreamProducer \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for node-rdkafka-4 — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-node-rdkafka-4" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-node-rdkafka-4.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-node-rdkafka-4 → test-results/$GIT_COMMIT/collector-messaging-node-rdkafka-4.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-node-rdkafka-4 (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-node-rdkafka-4/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-node-rdkafka-4/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-node-rdkafka-4 → test-results/$GIT_COMMIT/coverage/collector-messaging-node-rdkafka-4/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-node-rdkafka-4 (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-node-rdkafka-5:
    from: code-build
    displayName: node-rdkafka-5
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: node-rdkafka-5
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/node-rdkafka/_v3.6.1/other \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for node-rdkafka-5 — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-node-rdkafka-5" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-node-rdkafka-5.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-node-rdkafka-5 → test-results/$GIT_COMMIT/collector-messaging-node-rdkafka-5.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-node-rdkafka-5 (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-node-rdkafka-5/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-node-rdkafka-5/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-node-rdkafka-5 → test-results/$GIT_COMMIT/coverage/collector-messaging-node-rdkafka-5/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-node-rdkafka-5 (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'
  code-build-collector-messaging-socket-io:
    from: code-build
    displayName: socket.io
    runtimeClassName: large
    include:
      - dind
    steps:
      - name: unit-test
        displayName: socket.io
        image: mirror.gcr.io/library/debian:bookworm-slim
        include:
          - docker-socket
        script: |-
          #!/usr/bin/env bash
          set -eo pipefail

          node_version="${node_version:-$(get_env node-version "$(get_env NODE_VERSION "$(cat .nvmrc | tr -d '[:space:]')")")}"
          NODE_MAJOR="${node_version%%.*}"
          RUN_ESM="$(get_env RUN_ESM "")"

          cd "$WORKSPACE/$(load_repo app-repo path)"

          # install docker client
          CODENAME=$(. /etc/os-release; echo "$VERSION_CODENAME")
          ARCH=$(dpkg --print-architecture)
          apt-get update -qq && apt-get install -y -qq ca-certificates curl gnupg netcat-openbsd socat
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
          chmod a+r /etc/apt/keyrings/docker.gpg
          echo "deb [arch=${ARCH} signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian ${CODENAME} stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
          apt-get update -qq && apt-get install -y -qq docker-ce-cli
          timeout 30 bash -c 'until [ -S /var/run/docker.sock ]; do sleep 1; done'

          REPO_PATH="$PWD"

          # build Squid egress-proxy image
          docker build -t npm-proxy:local "$REPO_PATH/ci/squid"

          # start proxy + test-runner on a shared network
          # The proxy enforces the allowlist for the entire run.
          # Test execution clears proxy env vars so tests don't route through Squid.
          docker network create npm-net
          docker run -d \
            --name npm-proxy \
            --network npm-net \
            npm-proxy:local
          docker run -d \
            --name test-runner \
            --network npm-net \
            -v "$REPO_PATH:$REPO_PATH" \
            -w "$REPO_PATH" \
            -e CI=true \
            -e RUN_ESM="$RUN_ESM" \
            -e HTTPS_PROXY="http://npm-proxy:3128" \
            -e HTTP_PROXY="http://npm-proxy:3128" \
            -e NO_PROXY="127.0.0.1,localhost" \
            "mirror.gcr.io/library/node:${NODE_MAJOR}" \
            sleep infinity

          docker exec test-runner npm install --loglevel warn
          docker exec test-runner node bin/create-version-test-folders.js

          # collect test files
          TEST_FILES=$(docker exec test-runner \
            bash -c "cd packages/collector && find \
              test/integration/currencies/messaging/socket.io \
              -name '*.test.js' \
              -not -path '*/node_modules/*' \
              | sort | tr '\n' ' '")

          if [ -z "$TEST_FILES" ]; then
            echo 'WARNING: No test files found for socket.io — skipping.'
            docker rm -f test-runner npm-proxy || true
            docker network rm npm-net || true
            exit 0
          fi

          retry=1
          while [ $retry -le 2 ]; do
            LAST_EXIT=0
            docker exec \
              -e CI=true \
              -e HTTP_PROXY="" \
              -e HTTPS_PROXY="" \
              -e http_proxy="" \
              -e https_proxy="" \
              -e RUN_ESM="$RUN_ESM" \
              -e TEST_FILES="$TEST_FILES" \
              test-runner \
              npm run coverage-ci --npm_command="test:ci:collector" --report_dir="collector-messaging-socket-io" || LAST_EXIT=$?
            if [ $LAST_EXIT -eq 0 ]; then
              break
            fi
            echo "Attempt $retry failed with exit code $LAST_EXIT — retrying..."
            retry=$((retry + 1))
          done

          # cleanup
          docker rm -f test-runner npm-proxy || true
          docker network rm npm-net || true


          # upload executed test files + lcov coverage report to COS
          COS_API_KEY="$(get_secret ibm-object-storage-api-key)"
          GIT_COMMIT="$(get_env commit-id "")"
          if [ -n "$COS_API_KEY" ] && [ -n "$GIT_COMMIT" ]; then
            IAM_TOKEN=$(curl -sf -X POST "https://iam.cloud.ibm.com/identity/token" \
              -H "Content-Type: application/x-www-form-urlencoded" \
              -d "grant_type=urn:ibm:params:oauth:grant-type:apikey&apikey=$COS_API_KEY" \
              | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
            curl -sf -X PUT \
              "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/collector-messaging-socket-io.txt" \
              -H "Authorization: Bearer $IAM_TOKEN" \
              -H "Content-Type: text/plain" \
              --data-binary "$(echo "$TEST_FILES" | tr ' ' '\n' | sort)" \
              && echo "Uploaded test list for collector-messaging-socket-io → test-results/$GIT_COMMIT/collector-messaging-socket-io.txt" \
              || echo "WARNING: Failed to upload test list for collector-messaging-socket-io (non-fatal)"
            NODE_MAJOR="${node_version%%.*}"
            if [ "$NODE_MAJOR" != "24" ]; then
              echo "Skip: lcov upload — not the development node major version ($NODE_MAJOR != 24)"
            else
              LCOV_FILE="coverage/collector-messaging-socket-io/lcov.info"
              if [ -f "$LCOV_FILE" ]; then
                curl -sf -X PUT \
                  "https://s3.eu-de.cloud-object-storage.appdomain.cloud/itp-nodejs-tracer-sps/test-results/$GIT_COMMIT/coverage/collector-messaging-socket-io/lcov.info" \
                  -H "Authorization: Bearer $IAM_TOKEN" \
                  -H "Content-Type: text/plain" \
                  --data-binary @"$LCOV_FILE" \
                  && echo "Uploaded lcov for collector-messaging-socket-io → test-results/$GIT_COMMIT/coverage/collector-messaging-socket-io/lcov.info" \
                  || echo "WARNING: Failed to upload lcov for collector-messaging-socket-io (non-fatal)"
              else
                echo "WARNING: lcov not found at $LCOV_FILE — skipping lcov upload"
              fi
            fi
          else
            echo "WARNING: COS credentials or git commit unavailable — skipping uploads"
          fi


          # report commit status to GitHub
          GH_TOKEN="$(get_secret gh-public-token)"
          GIT_COMMIT="$(get_env commit-id "${COMMIT_SHA:-}")"
          PIPELINE_RUN_URL="$(get_env PIPELINE_RUN_URL "")"
          if [ -n "$GIT_COMMIT" ]; then
            STATUS="success"
            if [ $LAST_EXIT -ne 0 ]; then STATUS="failure"; fi
            CURL_RESPONSE=$(curl -s -w "\n%{http_code}" \
              -X POST "https://api.github.com/repos/instana/nodejs/statuses/$GIT_COMMIT" \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -H "Content-Type: application/json" \
              -d "{\"state\":\"$STATUS\",\"target_url\":\"$PIPELINE_RUN_URL\",\"description\":\"Main pipeline $STATUS (Node ${node_version%%.*})\",\"context\":\"sps/main/${node_version%%.*}/$TASK_NAME\"}")
            CURL_HTTP=$(echo "$CURL_RESPONSE" | tail -1)
            CURL_BODY=$(echo "$CURL_RESPONSE" | sed '$d')
            if [ "$CURL_HTTP" = "201" ]; then
              echo "Commit status set to '$STATUS' for $GIT_COMMIT."
            else
              echo "WARNING: Failed to set commit status (non-fatal). HTTP $CURL_HTTP: $CURL_BODY"
            fi
          else
            echo "WARNING: commit-id not set — skipping commit status."
          fi
          exit $LAST_EXIT
      - name: sign-artifact
        when: 'false'
      - name: build-artifact
        when: 'false'
      - name: scan-artifact
        when: 'false'

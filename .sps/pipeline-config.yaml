version: '2'

tasks:
  # Skip all compliance checks
  code-checks:
    steps:
      - name: detect-secrets
        when: false
      - name: compliance-checks
        when: false
      - name: peer-review
        when: false
      - name: static-scan
        when: false

  # CI build — npm install only
  code-build:
    steps:
      - name: build-artifact
        displayName: npm-install
        image: public.ecr.aws/docker/library/node:24
        script: |
          #!/bin/bash
          echo "Node: $(node --version)"
          echo "npm:  $(npm --version)"
          npm install --loglevel warn --foreground-scripts
          echo "npm install done."
      - name: unit-test
        when: false
      - name: scan-artifact
        when: false
      - name: sign-artifact
        when: false

  # Skip deploy stages
  deploy-checks:
    when: false

  deploy-release:
    when: false

  code-ci-finish:
    steps:
      - name: run-stage
        when: false


Precompiled Native Add-on Packs
==============================

Runs `npm install @instana/collector` for different Node.js versions and extracts the native dependencies of `@instana/collector` for repackaging.
